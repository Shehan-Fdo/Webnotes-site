---
title: "FAQ and Exam Focus"
description: "Key concepts for AWS SAA-C03 exam prep, with high-yield FAQs, service comparisons, and practical architecture patterns."
author: Webnotes
order: 1
image: /content/assets/faq-and-exam-focus.png
keywords:
  - AWS Certified Solutions Architect – Associate Complete Course (Exam SAA-C03)
  - exam prep
  - study notes
canonical: https://webnotes.site/content/aws-certified-solutions-architect-associate-complete-course-exam-saa-c03/faq-and-exam-focus/
twitterCard: summary_large_image
twitterSite: "@webnotes"
schemaType: TechArticle
date: 2026-04-26
lastModified: 2026-04-26
---
## **Course:** AWS Certified Solutions Architect – Associate Complete Course (Exam SAA-C03)

**How to approach the SAA-C03 exam**

The AWS Certified Solutions Architect – Associate exam tests whether you can choose the *best-fit* AWS design for a scenario, not whether you can recite every service feature. Most questions reward you for understanding tradeoffs: cost versus resilience, simplicity versus control, and managed services versus operational overhead.

A strong exam strategy is to identify the requirement type first:

- **Availability**: design for failure, use multiple AZs, and remove single points of failure.
- **Scalability**: use elastic services such as Auto Scaling, managed databases, and decoupled queues.
- **Security**: apply least privilege, encryption, private connectivity, and segmentation.
- **Cost optimization**: prefer managed services, right-size resources, and use storage tiers.
- **Operational excellence**: automate deployments, monitoring, and recovery.

When a question includes multiple valid answers, the correct one is usually the simplest option that satisfies all stated requirements.

**High-yield FAQ: what AWS services should you compare most often?**

Many exam questions are really comparisons between a small set of services. You should know when each one is the best choice and when it is not.

- **EC2 vs Lambda**
  - Use **Amazon EC2** when you need full OS control, custom runtimes, long-running processes, or specialized networking.
  - Use **AWS Lambda** for event-driven, short-lived, stateless workloads with variable traffic.
  - Lambda reduces operational work, but it is not ideal for persistent connections or workloads that need local state.

- **ALB vs NLB**
  - Use an **Application Load Balancer (ALB)** for HTTP/HTTPS, path-based routing, host-based routing, and modern web applications.
  - Use a **Network Load Balancer (NLB)** for ultra-low latency, TCP/UDP/TLS traffic, static IP needs, or preserving source IP.
  - If the question mentions Layer 7 routing, think ALB. If it mentions Layer 4 performance or static IPs, think NLB.

- **S3 vs EBS vs EFS**
  - Use **Amazon S3** for object storage, static content, backups, data lakes, and durable shared storage for applications that can read objects.
  - Use **Amazon EBS** for block storage attached to a single EC2 instance at a time, such as boot volumes and databases on EC2.
  - Use **Amazon EFS** for shared file storage across multiple EC2 instances, especially when POSIX file semantics are needed.
  - If multiple instances need the same mounted filesystem, EFS is usually the answer.

- **RDS vs DynamoDB**
  - Use **Amazon RDS** for relational data, joins, transactions, and SQL-based applications.
  - Use **Amazon DynamoDB** for key-value or document workloads that need single-digit millisecond performance at scale.
  - If the question emphasizes flexible schema and massive scale with predictable access patterns, DynamoDB is often the better fit.

- **CloudFront vs S3 static website hosting**
  - Use **Amazon CloudFront** when you need global caching, lower latency, TLS termination at the edge, or protection from origin load.
  - Use S3 static website hosting for simple public static sites, but note that CloudFront is usually the stronger production choice.
  - If the question mentions edge caching or global users, CloudFront is likely required.

- **Security Group vs Network ACL**
  - Use a **security group** as a stateful instance-level firewall.
  - Use a **network ACL (NACL)** as a stateless subnet-level filter.
  - Security groups are usually the primary control; NACLs are used for additional subnet-level restrictions.

**Common architecture patterns you should recognize**

The exam often describes a business problem in plain language. You should map that language to a standard AWS pattern.

- **Highly available web application**
  - Route 53 for DNS
  - CloudFront for caching and edge delivery
  - ALB across multiple AZs
  - Auto Scaling group for EC2 or containers
  - RDS Multi-AZ or DynamoDB for data persistence
  - S3 for static assets and backups

- **Decoupled processing**
  - S3 or API Gateway as the ingestion point
  - SQS for buffering and smoothing traffic spikes
  - Lambda or EC2 workers for processing
  - SNS for fanout notifications
  - DLQ (dead-letter queue) for failed messages

- **Private access to AWS services**
  - VPC endpoints for S3, DynamoDB, and interface endpoint services
  - NAT Gateway only when private subnets need outbound internet access
  - Security groups and route tables to control traffic flow

- **Disaster recovery**
  - Backup and restore for lowest cost, slowest recovery
  - Pilot light for minimal core services running
  - Warm standby for reduced recovery time
  - Multi-site active/active for highest availability and complexity

**Exam focus: what the exam usually asks you to do**

The exam is scenario-based. You are rarely asked for a definition alone. Instead, you are asked to choose the architecture that best satisfies constraints.

- **Choose the right storage**
  - If the workload needs shared file access, choose EFS.
  - If it needs durable object storage, choose S3.
  - If it needs low-latency block storage for one instance, choose EBS.
  - If it needs relational queries, choose RDS.
  - If it needs massive scale with simple access patterns, choose DynamoDB.

- **Choose the right compute**
  - If the workload is event-driven and intermittent, choose Lambda.
  - If it needs persistent servers or custom software, choose EC2.
  - If it needs container orchestration, consider ECS or EKS, depending on the scenario.
  - If the question emphasizes minimal management, prefer serverless or managed services.

- **Choose the right network design**
  - Put public-facing load balancers in public subnets.
  - Put application and database tiers in private subnets.
  - Use NAT Gateway for private subnet egress to the internet.
  - Use VPC endpoints to keep traffic to AWS services off the public internet.

- **Choose the right security controls**
  - Use IAM roles for AWS service access instead of long-lived access keys.
  - Encrypt data at rest with KMS-managed keys when required.
  - Encrypt data in transit with TLS.
  - Use least privilege and separate duties by role and account when possible.

**Practical CLI examples you should understand**

The exam does not require memorizing every command, but practical familiarity helps you understand how AWS components fit together.

```bash
# Inspect an EC2 instance's security groups and networking
aws ec2 describe-instances --instance-ids i-0123456789abcdef0

# Check S3 bucket policy and encryption settings
aws s3api get-bucket-policy --bucket my-example-bucket
aws s3api get-bucket-encryption --bucket my-example-bucket

# Review an Auto Scaling group
aws autoscaling describe-auto-scaling-groups --auto-scaling-group-names my-asg

# View RDS instance details
aws rds describe-db-instances --db-instance-identifier mydb

# Inspect a DynamoDB table
aws dynamodb describe-table --table-name Orders
```

A few configuration patterns are especially useful to recognize:

```json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "AllowReadOnlyFromAppRole",
      "Effect": "Allow",
      "Principal": { "AWS": "arn:aws:iam::123456789012:role/AppRole" },
      "Action": ["s3:GetObject"],
      "Resource": "arn:aws:s3:::my-example-bucket/*"
    }
  ]
}
```

This kind of bucket policy is common when you want an application role to read objects without exposing the bucket publicly.

```yaml
Resources:
  AppSecurityGroup:
    Type: AWS::EC2::SecurityGroup
    Properties:
      GroupDescription: Allow web traffic
      VpcId: vpc-12345678
      SecurityGroupIngress:
        - IpProtocol: tcp
          FromPort: 443
          ToPort: 443
          CidrIp: 0.0.0.0/0
```

This pattern shows a security group allowing HTTPS traffic to a public web tier.

**FAQ: what are the most common exam traps?**

The exam often includes distractors that are technically possible but not the best answer.

- **Overengineering**
  - Do not choose a complex multi-service design when one managed service solves the problem.
  - Example: use S3 + CloudFront for static content instead of EC2 hosting a web server unless server-side logic is required.

- **Wrong storage type**
  - Do not use EBS for shared access across multiple instances.
  - Do not use S3 when the application needs a mounted filesystem with POSIX behavior.
  - Do not use DynamoDB when the question clearly requires SQL joins and relational transactions.

- **Ignoring AZ design**
  - A single-AZ design is usually not enough for high availability.
  - Multi-AZ is a common requirement for production workloads.

- **Using public internet unnecessarily**
  - Prefer VPC endpoints for S3 and DynamoDB access from private subnets.
  - Use private connectivity when the question emphasizes security or compliance.

- **Choosing the wrong load balancer**
  - ALB is usually the right answer for web apps.
  - NLB is usually the right answer for TCP performance or static IP requirements.

**How to read scenario keywords quickly**

Certain phrases strongly suggest a service or pattern. These are not absolute rules, but they are useful exam shortcuts.

| Keyword or phrase | Likely AWS answer | Why it matters |
|---|---|---|
| “shared file system” | EFS | Multiple instances need the same mounted filesystem |
| “object storage” | S3 | Durable, scalable object storage |
| “single-digit millisecond latency” | DynamoDB | Fast key-value/document access at scale |
| “relational database” | RDS | SQL, joins, transactions |
| “burst traffic” | Auto Scaling, SQS, Lambda | Elastic or buffered processing |
| “global users” | CloudFront, Route 53 | Edge caching and DNS routing |
| “private access to S3” | VPC endpoint | Avoids public internet path |
| “stateless application” | ALB, Auto Scaling, Lambda | Easier horizontal scaling |
| “preserve source IP” | NLB | Layer 4 load balancing behavior |
| “multi-AZ failover” | RDS Multi-AZ | High availability for relational databases |

**Security and identity focus**

Security questions often test whether you understand the difference between authentication, authorization, and encryption.

- **IAM users** are for human identities, but long-lived access keys are not ideal for workloads.
- **IAM roles** are the preferred way for AWS services and applications to obtain temporary credentials.
- **KMS (Key Management Service)** is used to manage encryption keys for many AWS services.
- **Secrets Manager** is for rotating and storing secrets such as database credentials.
- **Parameter Store** is useful for configuration values and can also store secure strings.

A common pattern is to attach an IAM role to EC2, Lambda, or ECS tasks so the workload can access S3, DynamoDB, or other services without embedded credentials.

```bash
# Example: retrieve temporary credentials through the instance metadata service
curl http://169.254.169.254/latest/meta-data/iam/security-credentials/
```

This illustrates the idea that AWS workloads should use temporary role-based credentials rather than hard-coded secrets.

**Reliability and disaster recovery focus**

The exam frequently asks how to recover from failure with the right balance of cost and speed.

- **Backup and restore**
  - Lowest cost.
  - Slowest recovery.
  - Suitable when downtime is acceptable.

- **Pilot light**
  - Core components are always on.
  - Faster recovery than backup and restore.
  - Useful when you need a minimal warm environment.

- **Warm standby**
  - A scaled-down version of the full environment runs continuously.
  - Faster failover and higher cost.
  - Good for business-critical systems.

- **Multi-site active/active**
  - Traffic is served from multiple sites at once.
  - Highest availability and complexity.
  - Best for stringent uptime requirements.

For databases, remember that **Multi-AZ** is about high availability, while **Read Replicas** are primarily for read scaling and can sometimes support disaster recovery use cases, depending on the service and scenario.

**Performance and scaling focus**

Scaling questions usually ask whether you should scale vertically, horizontally, or by decoupling components.

- **Vertical scaling** means choosing a larger instance type.
- **Horizontal scaling** means adding more instances behind a load balancer or using a distributed service.
- **Decoupling** means using queues, streams, or event buses to absorb spikes and isolate failures.

Practical examples:

- Use **Auto Scaling** with an ALB for web tiers that experience variable traffic.
- Use **SQS** to buffer requests when downstream processing is slower than incoming traffic.
- Use **CloudFront** to reduce origin load and improve latency for global users.
- Use **DynamoDB on-demand** when traffic is unpredictable and you want to avoid capacity planning.

**Cost optimization focus**

The exam often rewards the answer that meets requirements with the least operational overhead and reasonable cost.

- Use **S3 lifecycle policies** to move infrequently accessed data to cheaper storage classes.
- Use **Reserved Instances** or **Savings Plans** for steady-state compute usage.
- Use **Spot Instances** for fault-tolerant batch or interruption-tolerant workloads.
- Use **managed services** when they reduce administrative effort and improve reliability.
- Use **serverless** when the workload is intermittent and event-driven.

Example lifecycle policy idea:

```json
{
  "Rules": [
    {
      "ID": "ArchiveOldObjects",
      "Status": "Enabled",
      "Filter": { "Prefix": "logs/" },
      "Transitions": [
        { "Days": 30, "StorageClass": "STANDARD_IA" },
        { "Days": 90, "StorageClass": "GLACIER" }
      ]
    }
  ]
}
```

This is a common pattern for log retention and archival.

**What to remember about VPC design**

A VPC (Virtual Private Cloud) question usually tests subnet placement and traffic flow.

- **Public subnets**
  - Contain resources that need direct internet access, such as internet-facing load balancers.
  - Have a route to an Internet Gateway.

- **Private subnets**
  - Contain application servers and databases.
  - Do not route directly to the Internet Gateway.

- **NAT Gateway**
  - Allows instances in private subnets to initiate outbound internet traffic.
  - Does not allow inbound connections from the internet.

- **VPC endpoints**
  - Keep traffic to AWS services private.
  - Reduce NAT usage and improve security posture.

A common exam topology is:

```text
Internet
   |
Route 53
   |
CloudFront
   |
ALB (public subnets in 2 AZs)
   |
App tier (private subnets in 2 AZs)
   |
RDS Multi-AZ or DynamoDB
```

This pattern is popular because it combines availability, security, and scalability.

**Final exam-day checklist**

Before you select an answer, ask yourself:

1. What is the primary requirement: availability, performance, security, or cost?
2. Is the workload stateful or stateless?
3. Does the data need SQL, key-value access, object storage, or shared files?
4. Does the design need one AZ or multiple AZs?
5. Can a managed service replace custom infrastructure?
6. Is there a simpler architecture that still satisfies the requirement?

If two answers seem correct, choose the one that is more aligned with AWS best practices: managed, scalable, secure, and resilient.

**Quick memory anchors**

- **ALB** for HTTP routing, **NLB** for TCP performance.
- **S3** for objects, **EBS** for single-instance block storage, **EFS** for shared files.
- **RDS** for relational data, **DynamoDB** for massive scale and low latency.
- **Security groups** are stateful, **NACLs** are stateless.
- **IAM roles** are preferred over long-lived credentials.
- **Multi-AZ** improves availability; **Read Replicas** improve read scaling.

End of Notes