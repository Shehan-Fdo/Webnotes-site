---
title: "FAQ and Exam Focus — AWS Certified Solutions Architect – Associate (SAA-C03)"
description: "Key concepts for SAA-C03 exam prep, with high-yield FAQs, common traps, and practical AWS architecture patterns."
author: Webnotes
order: 1
image: /content/assets/faq-and-exam-focus.png
keywords:
  - AWS Certified Solutions Architect – Associate (Exam SAA-C03)
  - exam prep
  - study notes
  - FAQ
  - exam focus
canonical: https://webnotes.site/content/aws-certified-solutions-architect-associate-complete-course-exam-saa-c03/faq-and-exam-focus/
twitterCard: summary_large_image
twitterSite: "@webnotes"
schemaType: TechArticle
date: 2026-04-26
lastModified: 2026-04-26
---
## **Course:** AWS Certified Solutions Architect – Associate Complete Course (Exam SAA-C03)

**What this topic covers**

This section focuses on the kinds of questions that commonly appear on the AWS Certified Solutions Architect – Associate exam and the architectural decisions behind them. The goal is not memorization alone; you need to recognize the service that best fits a scenario, understand trade-offs, and avoid distractors that look correct but fail on scale, durability, security, or cost.

High-yield exam prep usually centers on:
- **Service selection** under constraints such as latency, durability, availability, and budget.
- **Architecture patterns** for multi-AZ, multi-Region, decoupling, and serverless designs.
- **Security controls** including IAM, KMS, security groups, and least privilege.
- **Storage choices** across S3, EBS, EFS, and FSx.
- **Database fit** across RDS, Aurora, DynamoDB, and ElastiCache.
- **Operational behavior** such as backups, failover, scaling, and monitoring.

**How to approach SAA-C03 questions**

The exam often gives you a business requirement, then asks for the best architecture. Read for the constraint first:
- If the requirement says **lowest operational overhead**, prefer managed services.
- If it says **high availability**, look for Multi-AZ or multi-Region designs depending on the failure domain.
- If it says **lowest latency**, consider caching, edge services, or data locality.
- If it says **cost optimization**, avoid overprovisioned always-on compute when serverless or autoscaling fits.
- If it says **durable object storage**, S3 is usually the first candidate.
- If it says **shared file system**, EFS is often the right answer for Linux workloads.

A useful exam habit is to eliminate answers that solve the wrong problem. For example, a question about durable object storage is not asking for block storage, and a question about shared access from multiple EC2 instances is not asking for a single-instance EBS volume.

**Core FAQ themes you should know**

**1. When do you choose S3 vs EBS vs EFS?**

These three storage services solve different problems:
- **S3** is object storage for durable, scalable, internet-accessible or application-accessible objects.
- **EBS** is block storage attached to a single EC2 instance at a time, suited for boot volumes and low-latency disk use.
- **EFS** is a managed NFS file system that multiple EC2 instances can mount concurrently.

Practical rule:
- Use **S3** for backups, static assets, logs, data lakes, and object-based applications.
- Use **EBS** for databases on EC2, transactional workloads, and OS disks.
- Use **EFS** when multiple Linux instances need shared file access.

Example CLI for S3 upload:
```bash
aws s3 cp ./site-assets s3://my-static-site-bucket/ --recursive
```

Example EBS attachment workflow:
```bash
aws ec2 create-volume --availability-zone us-east-1a --size 100 --volume-type gp3
aws ec2 attach-volume --volume-id vol-0123456789abcdef0 --instance-id i-0123456789abcdef0 --device /dev/sdf
```

Example EFS mount on Linux:
```bash
sudo yum install -y amazon-efs-utils
sudo mkdir -p /mnt/efs
sudo mount -t efs fs-12345678:/ /mnt/efs
```

**2. When do you choose RDS vs Aurora vs DynamoDB?**

These databases differ by data model and operational style:
- **RDS** is managed relational database service for standard SQL engines.
- **Aurora** is AWS-optimized relational database with higher performance and built-in replication features.
- **DynamoDB** is a serverless NoSQL key-value and document database with predictable low-latency access.

Use:
- **RDS** when you need a familiar relational engine and standard managed database features.
- **Aurora** when you need relational compatibility with better scaling and availability characteristics.
- **DynamoDB** when you need massive scale, single-digit millisecond latency, and flexible schema.

Exam clue:
- If the question emphasizes **joins, transactions, and relational schema**, think RDS or Aurora.
- If it emphasizes **massive scale, key-based access, and serverless operation**, think DynamoDB.

DynamoDB example:
```bash
aws dynamodb create-table \
  --table-name Orders \
  --attribute-definitions AttributeName=OrderId,AttributeType=S \
  --key-schema AttributeName=OrderId,KeyType=HASH \
  --billing-mode PAY_PER_REQUEST
```

**3. When do you use Auto Scaling and Load Balancers?**

Use **Auto Scaling** to adjust EC2 capacity based on demand or health.
Use **Elastic Load Balancing (ELB)** to distribute traffic across healthy targets.

Common pairing:
- **Application Load Balancer (ALB)** for HTTP/HTTPS, path-based routing, host-based routing, and modern web apps.
- **Network Load Balancer (NLB)** for ultra-low latency, TCP/UDP, and static IP requirements.
- **Gateway Load Balancer** for third-party network appliances.

Exam pattern:
- If the workload is web-based and needs routing by URL path, choose **ALB**.
- If the workload needs static IPs or TCP pass-through, choose **NLB**.

Example target group registration:
```bash
aws elbv2 create-target-group \
  --name app-tg \
  --protocol HTTP \
  --port 80 \
  --vpc-id vpc-0123456789abcdef0
```

**4. When do you choose Multi-AZ vs Multi-Region?**

These are not interchangeable:
- **Multi-AZ** protects against Availability Zone failure within one Region.
- **Multi-Region** protects against Region-wide failure and supports global latency or disaster recovery needs.

Use **Multi-AZ** for high availability of most production systems.
Use **Multi-Region** when the business explicitly requires regional disaster recovery, global users, or regulatory separation.

For databases:
- RDS Multi-AZ provides synchronous standby replication for failover.
- Aurora supports fast failover and read replicas across AZs, with additional global database options for cross-Region needs.

> If the question says “survive an AZ outage,” Multi-AZ is usually enough. If it says “survive a Region outage,” you need multi-Region design.

**5. When do you use CloudFront, Route 53, and API Gateway?**

These services sit at different layers:
- **Route 53** is DNS and traffic routing.
- **CloudFront** is a CDN (Content Delivery Network) for caching and edge delivery.
- **API Gateway** exposes and manages APIs with authentication, throttling, and integration options.

Use:
- **Route 53** for DNS failover, weighted routing, latency-based routing, and health checks.
- **CloudFront** for global caching, reduced latency, and origin shielding.
- **API Gateway** for serverless API front doors, request validation, and throttling.

Typical pattern:
- Route 53 points users to CloudFront.
- CloudFront caches static and dynamic content where appropriate.
- API Gateway fronts Lambda-based APIs.

**6. When do you use IAM roles, policies, and STS?**

**IAM** (Identity and Access Management) controls authentication and authorization.
- **Roles** are assumed by AWS services, applications, or federated users.
- **Policies** define permissions.
- **STS** (Security Token Service) issues temporary credentials.

Exam focus:
- Prefer **roles** over long-term access keys for EC2, Lambda, ECS, and cross-account access.
- Use **least privilege** and scope permissions to specific resources when possible.
- Use **resource-based policies** when a service needs to grant access directly to another principal.

Example EC2 instance profile concept:
```bash
aws iam create-role \
  --role-name EC2S3ReadOnlyRole \
  --assume-role-policy-document file://trust-policy.json
```

**7. When do you use KMS, SSE, and client-side encryption?**

**KMS** (Key Management Service) manages encryption keys and integrates with many AWS services.
- **SSE** (server-side encryption) means AWS encrypts data at rest for you.
- **Client-side encryption** means you encrypt before sending data to AWS.

Use:
- **SSE-S3** for simple S3 encryption with minimal management.
- **SSE-KMS** when you need auditability, key control, or tighter compliance.
- **Client-side encryption** when you must control encryption before data reaches AWS.

Example S3 bucket encryption configuration:
```json
{
  "Rules": [
    {
      "ApplyServerSideEncryptionByDefault": {
        "SSEAlgorithm": "aws:kms",
        "KMSMasterKeyID": "arn:aws:kms:us-east-1:123456789012:key/abcd-1234"
      }
    }
  ]
}
```

**8. When do you use SQS, SNS, and EventBridge?**

These services decouple systems in different ways:
- **SQS** (Simple Queue Service) buffers messages for asynchronous processing.
- **SNS** (Simple Notification Service) pushes messages to multiple subscribers.
- **EventBridge** routes events between AWS services and SaaS integrations using rules.

Use:
- **SQS** when you need durable queueing and backpressure.
- **SNS** when one event should fan out to many consumers.
- **EventBridge** when you need event-driven integration with filtering and routing.

Common pattern:
- Application publishes to SNS.
- SNS fans out to SQS queues and Lambda functions.
- Workers consume from SQS at their own pace.

**9. When do you use ECS, EKS, and Lambda?**

These compute options differ in control and operational overhead:
- **Lambda** is serverless functions for event-driven workloads.
- **ECS** (Elastic Container Service) is managed container orchestration.
- **EKS** (Elastic Kubernetes Service) is managed Kubernetes.

Use:
- **Lambda** for short-lived, event-driven, stateless tasks.
- **ECS** when you want containers without Kubernetes complexity.
- **EKS** when you need Kubernetes compatibility or ecosystem tooling.

Exam clue:
- If the question emphasizes minimal management and event triggers, choose **Lambda**.
- If it emphasizes container orchestration with AWS-native simplicity, choose **ECS**.
- If it explicitly requires Kubernetes, choose **EKS**.

**10. When do you use backups, snapshots, and replication?**

These terms are related but not identical:
- **Backups** are recoverability mechanisms.
- **Snapshots** are point-in-time copies, often incremental.
- **Replication** copies data to another location for availability or disaster recovery.

Examples:
- EBS snapshots back up block volumes.
- RDS automated backups support point-in-time recovery.
- S3 versioning and replication support object recovery and cross-Region copies.

Practical note:
- Snapshots help with recovery, but they do not automatically provide active failover.
- Replication helps availability, but it does not replace a tested backup strategy.

**Common exam traps**

- Choosing **EBS** when the question needs shared file access.
- Choosing **RDS** when the workload is clearly key-value and at massive scale.
- Choosing **CloudFront** when the problem is DNS failover rather than caching.
- Choosing **Multi-AZ** when the requirement explicitly says Region failure.
- Choosing **NLB** for a standard HTTP application that needs path-based routing.
- Choosing **Lambda** for long-running workloads that exceed practical execution patterns.
- Confusing **security groups** with **network ACLs**:
  - Security groups are stateful and attached to ENIs.
  - Network ACLs are stateless and apply at the subnet level.

**Security and networking FAQ**

**Security groups vs network ACLs**
- **Security groups** allow or deny traffic at the instance or ENI level and are stateful.
- **Network ACLs** filter traffic at the subnet level and are stateless.

Use security groups for most instance-level access control.
Use network ACLs when you need an additional subnet boundary or explicit deny rules.

**Public subnet vs private subnet**
- A **public subnet** has a route to an Internet Gateway.
- A **private subnet** does not have direct internet routing.

Typical design:
- Put ALB in public subnets.
- Put application servers and databases in private subnets.
- Use NAT Gateway for outbound internet access from private subnets when needed.

Example route table concept:
```text
0.0.0.0/0 -> igw-0123456789abcdef0   # public subnet
0.0.0.0/0 -> nat-0123456789abcdef0   # private subnet outbound
```

**NAT Gateway vs Internet Gateway**
- **Internet Gateway** enables internet connectivity for public subnets.
- **NAT Gateway** enables outbound internet access for private subnet instances without exposing them directly.

**High-yield architecture patterns**

**Web application with static assets**
- Store static content in **S3**.
- Deliver content through **CloudFront**.
- Use **Route 53** for DNS.
- Host dynamic app tier on **EC2**, **ECS**, or **Lambda** depending on requirements.
- Place databases in private subnets.

**Highly available three-tier app**
- ALB in public subnets.
- App servers in private subnets across at least two AZs.
- RDS Multi-AZ for relational data.
- S3 for backups and static files.
- CloudWatch for metrics and alarms.

**Event-driven processing**
- Client uploads file to S3.
- S3 event triggers Lambda or sends event to SQS.
- Worker processes asynchronously.
- Results stored in DynamoDB or RDS depending on access pattern.

**Disaster recovery**
- Use backups and snapshots for restore.
- Use cross-Region replication for critical data.
- Use Route 53 health checks and failover routing.
- Keep infrastructure as code so you can rebuild quickly.

**CLI and configuration snippets worth recognizing**

Create an S3 bucket policy allowing CloudFront origin access is a common pattern, but the exact policy varies by setup. More generally, you should recognize how AWS CLI commands map to resources:

```bash
aws s3 mb s3://my-company-assets
aws s3 sync ./dist s3://my-company-assets --delete
aws s3api put-bucket-versioning \
  --bucket my-company-assets \
  --versioning-configuration Status=Enabled
```

```bash
aws ec2 describe-instances --filters "Name=instance-state-name,Values=running"
aws rds describe-db-instances
aws lambda list-functions
aws cloudwatch put-metric-alarm \
  --alarm-name HighCPU \
  --metric-name CPUUtilization \
  --namespace AWS/EC2 \
  --statistic Average \
  --period 300 \
  --threshold 80 \
  --comparison-operator GreaterThanThreshold \
  --evaluation-periods 2
```

**Exam strategy checklist**

1. Identify the primary requirement: availability, durability, performance, security, or cost.
2. Identify the failure domain: instance, AZ, Region, or global.
3. Identify the data access pattern: object, block, file, relational, key-value, or event.
4. Prefer managed services when the question asks for lower operational burden.
5. Eliminate answers that solve a different problem than the one asked.
6. Watch for words like **must**, **requires**, **lowest cost**, **least operational overhead**, and **highly available**; these usually determine the correct answer.

**Quick FAQ answers to remember**

- **What is the safest default for production compute?**
  - Use managed services and Multi-AZ where applicable.

- **What is the safest default for storage?**
  - Use S3 for objects, EBS for single-instance block storage, and EFS for shared file access.

- **What is the safest default for databases?**
  - Use RDS or Aurora for relational workloads and DynamoDB for key-value workloads.

- **What is the safest default for decoupling?**
  - Use SQS for buffering and SNS for fan-out.

- **What is the safest default for global delivery?**
  - Use CloudFront with Route 53.

- **What is the safest default for permissions?**
  - Use IAM roles with least privilege and temporary credentials.

**Final exam focus**

The SAA-C03 exam rewards architectural judgment. You should be able to explain not just which service fits, but why it fits better than the alternatives. The most common mistakes come from matching a keyword too quickly instead of reading the full scenario. If you consistently map requirements to failure domains, data models, and operational goals, you will eliminate most distractors with confidence.

End of Notes