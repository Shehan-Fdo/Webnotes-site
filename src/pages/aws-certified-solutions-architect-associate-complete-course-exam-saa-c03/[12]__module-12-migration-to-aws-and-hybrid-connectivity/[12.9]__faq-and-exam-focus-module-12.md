---
title: "FAQ and Exam Focus — AWS Certified Solutions Architect – Associate (SAA-C03)"
description: "Key concepts for SAA-C03 exam prep, with practical AWS architecture FAQs, service tradeoffs, and high-yield decision points."
author: Webnotes
order: 1
image: /content/assets/faq-and-exam-focus.png
keywords:
  - AWS Certified Solutions Architect – Associate (Exam SAA-C03)
  - exam prep
  - study notes
  - FAQ
  - exam focus
canonical: https://webnotes.site/content/aws-certified-solutions-architect-associate-complete-course-exam-saa-c03/faq-and-exam-focus/
twitterCard: summary_large_image
twitterSite: "@webnotes"
schemaType: TechArticle
date: 2026-04-26
lastModified: 2026-04-26
---
## **Course:** AWS Certified Solutions Architect – Associate Complete Course (Exam SAA-C03)

**What this topic covers**

This section focuses on the kinds of questions that repeatedly appear in the AWS Certified Solutions Architect – Associate exam: service selection, architecture tradeoffs, resilience, security, performance, and cost optimization. The goal is not memorization of every feature, but understanding how to choose the right AWS service for a scenario.

For exam prep, you should think in terms of:
- **Requirements first**: availability, durability, latency, throughput, compliance, and budget.
- **Managed over self-managed** when the managed option meets the requirement.
- **Decoupling** to improve resilience and scale.
- **Least privilege** and defense in depth for security decisions.
- **Right storage class** and **right compute model** for cost and performance.

**How to approach SAA-C03 questions**

Most questions are scenario-based. You are usually given a business need, a constraint, and several plausible answers. The best answer is often the one that satisfies all constraints with the least operational overhead.

Use this mental checklist:
1. Identify the primary goal: high availability, low latency, migration, security, or cost reduction.
2. Identify the constraint: no downtime, encryption required, cross-region recovery, on-prem integration, or minimal code changes.
3. Eliminate answers that add unnecessary management burden.
4. Prefer AWS-native patterns that are scalable and fault tolerant.
5. Watch for keywords such as `multi-AZ`, `multi-Region`, `event-driven`, `serverless`, `durable`, `ephemeral`, and `eventual consistency`.

**High-yield exam themes**

The exam often tests the same architectural ideas in different forms:
- **Compute**
  - EC2 for full control and custom OS-level needs.
  - Auto Scaling for variable demand.
  - Lambda for event-driven workloads and reduced operations.
  - ECS or EKS when containers are required.
- **Storage**
  - S3 for object storage, static content, backups, and data lakes.
  - EBS for block storage attached to EC2.
  - EFS for shared file storage across multiple instances.
  - FSx for specialized file systems such as Windows or high-performance workloads.
- **Databases**
  - RDS for relational workloads with managed administration.
  - Aurora for higher performance and cloud-native relational patterns.
  - DynamoDB for key-value and document access at scale.
  - ElastiCache for caching hot data and reducing database load.
- **Networking**
  - VPC design, subnets, route tables, security groups, and NACLs.
  - ALB for HTTP/HTTPS routing and path-based routing.
  - NLB for low-latency TCP/UDP and static IP use cases.
  - CloudFront for global edge caching and content delivery.
- **Security**
  - IAM roles instead of long-lived credentials.
  - KMS for encryption key management.
  - Secrets Manager or Parameter Store for secret handling.
  - WAF for web application filtering.
- **Resilience**
  - Multi-AZ for high availability within a Region.
  - Multi-Region for disaster recovery or global availability.
  - Backups, snapshots, and replication for recovery objectives.

**Common FAQ-style exam questions**

**When should you choose S3 over EBS or EFS?**

Choose **S3** when you need object storage, massive scale, durability, and access over HTTP-based APIs. It is ideal for static websites, backups, logs, media, and data lakes.

Choose **EBS** when an EC2 instance needs block storage with low-latency access, such as a database volume or boot disk.

Choose **EFS** when multiple EC2 instances need shared POSIX-style file access.

Practical rule:
- If the data is a file object and does not need a mounted filesystem, use S3.
- If the data must be attached to one instance as a block device, use EBS.
- If multiple instances must mount the same filesystem, use EFS.

**When should you use ALB versus NLB?**

Use **Application Load Balancer (ALB)** for HTTP/HTTPS traffic, content-based routing, host-based routing, and integration with modern web apps and microservices.

Use **Network Load Balancer (NLB)** for TCP/UDP traffic, extreme performance, static IP requirements, or when you need to preserve source IP with minimal latency.

Example:
- A web app with `/api` and `/images` routing belongs behind an ALB.
- A gaming backend or custom TCP service often fits an NLB better.

**When is Lambda the right answer?**

Choose **AWS Lambda** when the workload is event-driven, short-lived, and benefits from automatic scaling without server management.

Typical use cases:
- File processing after an S3 upload.
- API backends through API Gateway.
- Scheduled jobs with EventBridge.
- Stream processing from Kinesis or DynamoDB Streams.

Avoid Lambda when:
- The workload runs too long for the function timeout.
- You need persistent local state.
- You require specialized OS-level control.
- The application depends on a traditional server model.

**When should you use DynamoDB instead of RDS?**

Use **DynamoDB** when you need:
- Single-digit millisecond latency at scale.
- Predictable access patterns.
- A managed NoSQL database.
- Automatic scaling with minimal administration.

Use **RDS** when you need:
- SQL queries and joins.
- Strong relational modeling.
- Existing relational applications.
- Transactional patterns that fit a relational engine.

A common exam trap is choosing a relational database for a workload that only needs key-based lookups and high scale. If the scenario emphasizes low-latency reads/writes and simple access patterns, DynamoDB is often the better fit.

**What does Multi-AZ actually solve?**

**Multi-AZ** improves availability by placing resources in separate Availability Zones within the same Region. It protects against an AZ failure, not a full Region failure.

Examples:
- RDS Multi-AZ provides synchronous standby replication and automatic failover.
- An ALB spanning multiple AZs improves application availability.
- Auto Scaling groups across multiple AZs help maintain capacity during an AZ outage.

Do not confuse:
- **Multi-AZ** with disaster recovery across Regions.
- **Read replicas** with Multi-AZ failover. Read replicas are primarily for read scaling and can help with DR, but they are not the same as synchronous HA.

**What is the difference between backups, snapshots, and replication?**

- **Backups** are point-in-time recovery mechanisms, often used for long-term retention and restore.
- **Snapshots** are incremental backups commonly associated with EBS and RDS.
- **Replication** copies data to another location for availability or disaster recovery.

Exam focus:
- Use snapshots for restoring volumes or databases to a previous state.
- Use replication when you need a secondary copy ready for failover or regional recovery.
- Use backups for compliance, retention, and recovery from accidental deletion.

**How do you choose between security groups and network ACLs?**

**Security groups** are stateful and attached to ENIs (Elastic Network Interfaces), instances, and some load balancers. They are the primary control for instance-level traffic.

**Network ACLs (NACLs)** are stateless and apply at the subnet level. They are useful for coarse-grained subnet filtering.

Practical guidance:
- Use security groups for most allow-listing.
- Use NACLs when you need an additional subnet-level control or explicit deny rules.
- Remember that stateless NACLs require both inbound and outbound rules for return traffic.

**Why is IAM role-based access preferred?**

Use **IAM roles** instead of long-lived access keys whenever possible because roles provide temporary credentials and reduce credential leakage risk.

Common patterns:
- EC2 instance profile for applications running on EC2.
- Lambda execution role for function permissions.
- Cross-account role assumption for delegated access.

Avoid embedding access keys in code, AMIs, or user data. The exam often rewards designs that remove static credentials.

**What is the best way to store secrets?**

Use **Secrets Manager** for secrets that need rotation, such as database passwords or API keys. Use **Systems Manager Parameter Store** for configuration values and simpler secret storage needs, especially when cost or simplicity matters.

General rule:
- If rotation and secret lifecycle management matter, choose Secrets Manager.
- If you need parameter storage with hierarchical naming and lower cost, choose Parameter Store.

**When should you use CloudFront?**

Use **CloudFront** when you need:
- Global content delivery.
- Edge caching to reduce latency.
- Reduced origin load.
- Integration with S3, ALB, or custom origins.

CloudFront is especially useful for:
- Static websites hosted in S3.
- Media distribution.
- Dynamic content acceleration in some cases.
- Protecting origins with signed URLs or origin access controls.

**How do you think about S3 security?**

S3 security questions often combine access control, encryption, and public access prevention.

Key points:
- Use bucket policies and IAM policies for access control.
- Use SSE-S3 or SSE-KMS for encryption at rest.
- Use Block Public Access to prevent accidental exposure.
- Use presigned URLs for temporary access to private objects.

Example bucket policy pattern for read access from a specific role:
```json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "AllowReadFromAppRole",
      "Effect": "Allow",
      "Principal": {
        "AWS": "arn:aws:iam::123456789012:role/AppRole"
      },
      "Action": ["s3:GetObject"],
      "Resource": "arn:aws:s3:::my-bucket/*"
    }
  ]
}
```

**What is the difference between EBS and instance store?**

**EBS** is persistent block storage that survives instance stop/start and is suitable for durable application data.

**Instance store** is ephemeral storage physically attached to the host. It is fast, but data is lost when the instance stops, terminates, or the host fails.

Use instance store only when:
- You need temporary scratch space.
- You can tolerate data loss.
- The workload benefits from very high local I/O.

**How do you choose between RDS and Aurora?**

Choose **RDS** when you want managed relational databases with standard engines and straightforward administration.

Choose **Aurora** when you need:
- Higher performance and availability characteristics.
- Cloud-native architecture with fast failover.
- Read scaling with Aurora Replicas.
- Compatibility with MySQL or PostgreSQL ecosystems.

Exam tip:
- If the question emphasizes performance, availability, and managed relational scale, Aurora is often the stronger answer.
- If the question emphasizes a standard managed database with familiar engine support, RDS may be enough.

**What is the role of Auto Scaling?**

**Auto Scaling** adjusts capacity based on demand or health checks. It helps maintain availability and cost efficiency.

Common use cases:
- Web applications with variable traffic.
- Fault recovery when an instance becomes unhealthy.
- Scheduled scaling for predictable peaks.

A typical EC2 Auto Scaling setup includes:
- Launch template or launch configuration.
- Target group behind an ALB.
- Scaling policies based on CPU, request count, or custom metrics.

Example AWS CLI commands:
```bash
aws autoscaling create-auto-scaling-group \
  --auto-scaling-group-name web-asg \
  --launch-template LaunchTemplateName=web-lt,Version='$Latest' \
  --min-size 2 \
  --max-size 6 \
  --desired-capacity 2 \
  --vpc-zone-identifier "subnet-aaa,subnet-bbb"
```

```bash
aws autoscaling put-scaling-policy \
  --auto-scaling-group-name web-asg \
  --policy-name cpu-target-tracking \
  --policy-type TargetTrackingScaling \
  --target-tracking-configuration '{
    "PredefinedMetricSpecification": {
      "PredefinedMetricType": "ASGAverageCPUUtilization"
    },
    "TargetValue": 50.0
  }'
```

**What networking concepts are most important?**

You should understand the following:
- **VPC**: isolated virtual network boundary.
- **Subnet**: a segment of a VPC in one Availability Zone.
- **Route table**: determines where traffic goes.
- **Internet Gateway**: enables internet access for public subnets.
- **NAT Gateway**: allows private subnet instances to reach the internet outbound only.
- **VPC endpoints**: private connectivity to AWS services without traversing the public internet.

Practical example:
- Put web servers in public subnets only if they need direct internet exposure.
- Put application and database tiers in private subnets.
- Use a NAT Gateway for patching or package downloads from private subnets.
- Use VPC endpoints for S3 or DynamoDB access to reduce NAT usage and improve security.

**How do you handle on-premises connectivity?**

Use:
- **Site-to-Site VPN** for encrypted connectivity over the internet.
- **Direct Connect** for dedicated private connectivity with more consistent latency and bandwidth.

Exam guidance:
- Choose VPN for faster setup and lower cost.
- Choose Direct Connect for stable, high-throughput enterprise connectivity.
- Use both together when you need resilience and private connectivity.

**What is the best way to design for disaster recovery?**

Disaster recovery choices usually trade cost for recovery speed:
- **Backup and restore**: lowest cost, slowest recovery.
- **Pilot light**: minimal core services running.
- **Warm standby**: scaled-down but functional environment.
- **Multi-site active-active**: highest availability, highest cost and complexity.

Choose based on recovery time objective (RTO) and recovery point objective (RPO):
- **RTO** is how long you can tolerate downtime.
- **RPO** is how much data loss you can tolerate.

**Common exam traps**

- Confusing **high availability** with **disaster recovery**.
- Choosing a custom-managed solution when a managed AWS service fits better.
- Using a database when object storage or NoSQL is enough.
- Selecting a single-AZ design when the question asks for resilience.
- Ignoring encryption, logging, or least privilege in security scenarios.
- Overlooking cost-effective options like S3 lifecycle policies or Spot Instances when appropriate.

**Practical CLI examples to remember**

Create an S3 bucket and enable versioning:
```bash
aws s3api create-bucket \
  --bucket my-study-notes-bucket \
  --region us-east-1

aws s3api put-bucket-versioning \
  --bucket my-study-notes-bucket \
  --versioning-configuration Status=Enabled
```

Launch an EC2 instance with an IAM role attached through an instance profile:
```bash
aws ec2 run-instances \
  --image-id ami-0123456789abcdef0 \
  --instance-type t3.micro \
  --iam-instance-profile Name=AppInstanceProfile \
  --subnet-id subnet-aaa \
  --security-group-ids sg-1234567890abcdef0
```

Create an RDS snapshot:
```bash
aws rds create-db-snapshot \
  --db-instance-identifier mydb \
  --db-snapshot-identifier mydb-snapshot-2026-04-26
```

**Quick decision patterns**

- Need static website hosting with global caching: **S3 + CloudFront**
- Need shared file storage for multiple EC2 instances: **EFS**
- Need low-latency block storage for one instance: **EBS**
- Need serverless event processing: **Lambda**
- Need relational SQL database: **RDS** or **Aurora**
- Need high-scale key-value access: **DynamoDB**
- Need internet-facing HTTP routing: **ALB**
- Need TCP/UDP performance or static IPs: **NLB**
- Need AZ-level resilience: **Multi-AZ**
- Need Region-level recovery: **Multi-Region DR**

**Final exam mindset**

When you read a question, translate it into architecture requirements before looking at the answers. The correct choice usually aligns with the simplest AWS service that satisfies the stated need while improving availability, security, and operational efficiency.

If two answers both seem possible, prefer the one that:
- reduces management overhead,
- scales automatically,
- uses managed services,
- and matches the exact traffic, storage, or recovery pattern described.

End of Notes