---
title: "FAQ and Exam Focus — AWS Certified Solutions Architect – Associate (SAA-C03)"
description: "Key concepts for SAA-C03 exam prep, with high-yield FAQs, service tradeoffs, and practical AWS architecture patterns."
author: Webnotes
order: 1
image: /content/assets/faq-and-exam-focus.png
keywords:
  - SAA-C03
  - exam prep
  - study notes
  - AWS Solutions Architect Associate
canonical: https://webnotes.site/content/aws-certified-solutions-architect-associate-complete-course-exam-saa-c03/faq-and-exam-focus/
twitterCard: summary_large_image
twitterSite: "@webnotes"
schemaType: TechArticle
date: 2026-04-26
lastModified: 2026-04-26
---
## **Course:** AWS Certified Solutions Architect – Associate Complete Course (Exam SAA-C03)

**What the exam is really testing**

The SAA-C03 exam checks whether you can choose the most appropriate AWS service or architecture for a business requirement. It is less about memorizing every feature and more about recognizing tradeoffs among cost, availability, durability, performance, security, and operational effort.

A strong answer usually:
- Meets the requirement with the simplest viable design.
- Uses managed services when they reduce operational overhead.
- Chooses the right storage, compute, and networking pattern for the workload.
- Balances resilience and cost instead of defaulting to the most expensive option.

**How to approach scenario questions**

Most questions describe a business problem, then ask for the best solution. Read for constraints first:
- **Availability** requirements often point to Multi-AZ, load balancing, and managed failover.
- **Performance** requirements often point to caching, right storage class, or scaling compute.
- **Security** requirements often point to IAM, KMS, VPC endpoints, encryption, and least privilege.
- **Cost** requirements often point to S3 lifecycle policies, Spot Instances, Auto Scaling, or serverless.
- **Operational simplicity** often points to managed services such as RDS, DynamoDB, Lambda, SQS, or ECS/Fargate.

If two answers both seem correct, prefer the one that:
- Uses a managed AWS service instead of a self-managed alternative.
- Avoids unnecessary custom code.
- Matches the stated requirement exactly, not a broader or more expensive design.

**High-yield exam themes**

These are the areas that commonly drive correct answers:

- **S3** for object storage, static websites, backups, and data lake foundations.
- **EC2** for flexible compute, legacy apps, and workloads needing OS-level control.
- **EBS** for block storage attached to a single EC2 instance.
- **EFS** for shared file storage across multiple Linux instances.
- **RDS/Aurora** for relational databases with managed backups, patching, and Multi-AZ.
- **DynamoDB** for serverless NoSQL with predictable low-latency access.
- **Lambda** for event-driven, short-running, serverless compute.
- **VPC** for network isolation, routing, security groups, and subnet design.
- **CloudFront** for global content delivery and edge caching.
- **Route 53** for DNS, health checks, and routing policies.
- **ELB** for distributing traffic across targets and improving availability.
- **IAM** for authentication, authorization, roles, and policies.
- **KMS** for encryption key management.
- **CloudWatch** for metrics, logs, alarms, and operational visibility.
- **Auto Scaling** for elasticity and fault tolerance.
- **SQS/SNS/EventBridge** for decoupling and event-driven integration.

**FAQ: Which storage service should you choose?**

Use the storage service that matches the access pattern.

- **S3** is for objects, not mounted file systems or block devices.
- **EBS** is for low-latency block storage attached to one EC2 instance at a time.
- **EFS** is for shared POSIX-style file access from multiple EC2 instances.
- **FSx** is for specialized file systems such as Windows File Server or Lustre.
- **Instance store** is temporary and lost when the instance stops or terminates.

Practical rule:
- If the workload needs shared access from multiple Linux servers, think **EFS**.
- If it needs a database volume or boot volume, think **EBS**.
- If it needs durable object storage with lifecycle management, think **S3**.

**FAQ: When do you use Multi-AZ versus Multi-Region?**

Use **Multi-AZ** for high availability within a Region. Use **Multi-Region** for disaster recovery, global latency reduction, or regulatory separation.

- **Multi-AZ** is the default resilience pattern for many managed services.
- **Multi-Region** adds complexity and cost, but improves regional fault tolerance.
- For most exam questions, Multi-AZ is the correct answer when the requirement is “high availability.”
- Use Multi-Region only when the question explicitly mentions regional disaster recovery, global users, or active-active/active-passive across Regions.

**FAQ: What is the difference between scaling up and scaling out?**

- **Scaling up** means using a larger instance or more powerful resource.
- **Scaling out** means adding more instances or nodes.
- The exam usually prefers scaling out for web tiers because it improves availability and elasticity.
- Scaling up may be acceptable for databases or workloads that are harder to distribute.

Example:
- A stateless web application behind an Application Load Balancer should usually scale out with Auto Scaling.
- A memory-intensive legacy application may need a larger EC2 instance instead.

**FAQ: When should you use an Application Load Balancer versus a Network Load Balancer?**

Use the load balancer that matches the protocol and routing needs.

| Feature | Application Load Balancer (ALB) | Network Load Balancer (NLB) |
|---|---|---|
| Layer | 7 | 4 |
| Best for | HTTP/HTTPS, path-based routing, host-based routing | TCP/UDP/TLS, extreme performance, static IP use cases |
| Routing | Content-aware | Connection-aware |
| Common use | Web apps, microservices, APIs | Non-HTTP traffic, low latency, high throughput |

Choose **ALB** when you need:
- Path-based routing such as `/api` and `/images`.
- Host-based routing such as `app.example.com` and `admin.example.com`.
- WebSocket or HTTP-aware features.

Choose **NLB** when you need:
- Static IP addresses.
- Very high throughput and low latency.
- TCP or UDP workloads.

**FAQ: When is Lambda the right answer?**

Use **Lambda** when the workload is event-driven, short-lived, and does not need server management.

Good fits:
- File processing after an S3 upload.
- Lightweight API backends.
- Scheduled tasks with EventBridge.
- Stream or queue consumers.

Not ideal when:
- The workload runs for a long time.
- You need full OS control.
- You need persistent local state.
- The application has heavy startup or dependency requirements that make cold starts problematic.

Example event flow:
```text
S3 upload -> Event notification -> Lambda -> DynamoDB update
```

**FAQ: How do IAM roles differ from IAM users?**

- **IAM users** represent long-term identities for people or applications, but are often avoided for workloads.
- **IAM roles** provide temporary credentials and are the preferred way for AWS services and federated users to access resources.
- EC2 instances should usually use an **instance profile** attached to an IAM role.
- Lambda functions should use an execution role.
- Cross-account access is commonly implemented with roles.

Practical example:
```json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Allow",
      "Action": ["s3:GetObject"],
      "Resource": ["arn:aws:s3:::my-bucket/*"]
    }
  ]
}
```

**FAQ: What is the difference between security groups and network ACLs?**

- **Security groups** are stateful and attached to ENIs (Elastic Network Interfaces).
- **Network ACLs** are stateless and apply at the subnet level.
- Security groups are usually the primary control for instance traffic.
- Network ACLs are used for broader subnet-level filtering or explicit deny requirements.

Practical rule:
- If you need to allow inbound HTTPS to a web server, use a security group.
- If you need subnet-wide deny rules or additional boundary filtering, consider a network ACL.

**FAQ: When should you use SQS versus SNS?**

Use **SQS** for queue-based decoupling and buffering. Use **SNS** for fanout notifications.

- **SQS** stores messages until consumers process them.
- **SNS** pushes messages to multiple subscribers.
- SQS is useful when consumers may be slow or temporarily unavailable.
- SNS is useful when multiple systems need the same event immediately.

Common pattern:
```text
Producer -> SNS -> SQS queues -> multiple consumers
```

This pattern gives you fanout plus durable buffering.

**FAQ: What is the purpose of CloudFront?**

**CloudFront** is a content delivery network (CDN) that caches content at edge locations close to users.

Use it for:
- Static website acceleration.
- Lower latency for global users.
- Offloading origin traffic.
- Adding security features such as integration with AWS WAF and origin access controls.

CloudFront is often paired with:
- **S3** for static content.
- **ALB** for dynamic web applications.
- **API Gateway** for API acceleration in some designs.

**FAQ: What is the most common database choice?**

The exam often expects you to choose the database based on access pattern.

- **RDS** is the default choice for relational workloads that need SQL and transactions.
- **Aurora** is a strong choice when the question emphasizes higher performance or managed MySQL/PostgreSQL compatibility.
- **DynamoDB** is the default choice for key-value or document workloads with massive scale and low latency.
- **ElastiCache** is used to reduce database load and improve read performance.
- **Redshift** is for analytics and data warehousing, not transactional OLTP workloads.

If the question says:
- “transactions,” “joins,” or “normalized data” → think **RDS/Aurora**.
- “millisecond latency at scale,” “serverless NoSQL,” or “key-value access” → think **DynamoDB**.
- “cache frequently read data” → think **ElastiCache**.

**FAQ: How do you improve availability for EC2-based applications?**

A resilient EC2 architecture usually includes:
- Multiple Availability Zones.
- An Application Load Balancer.
- Auto Scaling groups.
- Stateless application design.
- Externalized session state, often in ElastiCache or DynamoDB.
- Managed database failover, such as RDS Multi-AZ.

Example architecture:
```text
Users -> ALB -> Auto Scaling Group in AZ-a and AZ-b -> RDS Multi-AZ
```

This design survives instance failure and improves zone-level resilience.

**FAQ: What is the difference between RDS Multi-AZ and Read Replicas?**

- **Multi-AZ** is primarily for high availability and automatic failover.
- **Read Replicas** are primarily for read scaling and sometimes disaster recovery.
- Multi-AZ synchronously replicates to a standby in another AZ.
- Read replicas are typically asynchronous and can be promoted in some scenarios.

Exam shortcut:
- If the question says “failover” or “high availability,” choose **Multi-AZ**.
- If it says “offload read traffic,” choose **Read Replica**.

**FAQ: When should you use EBS snapshots?**

Use **EBS snapshots** for point-in-time backups of EBS volumes.

- Snapshots are stored in S3-managed infrastructure.
- They are incremental after the first snapshot.
- They support disaster recovery and volume restoration.
- They are useful for creating new volumes in another AZ or Region.

Typical workflow:
```bash
aws ec2 create-snapshot --volume-id vol-0123456789abcdef0 --description "Daily backup"
aws ec2 create-volume --snapshot-id snap-0123456789abcdef0 --availability-zone us-east-1a
```

**FAQ: What is the difference between encryption at rest and in transit?**

- **Encryption at rest** protects stored data, such as S3 objects, EBS volumes, RDS storage, and DynamoDB tables.
- **Encryption in transit** protects data moving across networks, typically using TLS (Transport Layer Security).
- The exam often expects both when handling sensitive data.

Common examples:
- S3 with SSE-S3 or SSE-KMS for at-rest encryption.
- HTTPS for browser-to-CloudFront or browser-to-ALB traffic.
- TLS for database connections when supported.

**FAQ: What are the most common CLI commands to remember?**

You do not need to memorize every command, but you should recognize the service and action names.

```bash
# S3 upload
aws s3 cp app.zip s3://my-deploy-bucket/releases/

# List EC2 instances
aws ec2 describe-instances

# Create an Auto Scaling group
aws autoscaling create-auto-scaling-group \
  --auto-scaling-group-name web-asg \
  --launch-template LaunchTemplateName=my-template,Version=1 \
  --min-size 2 --max-size 6 --desired-capacity 2 \
  --vpc-zone-identifier "subnet-aaa,subnet-bbb"

# Create an RDS snapshot
aws rds create-db-snapshot \
  --db-instance-identifier mydb \
  --db-snapshot-identifier mydb-snap-2026-04-26

# Put a message on SQS
aws sqs send-message \
  --queue-url https://sqs.us-east-1.amazonaws.com/123456789012/my-queue \
  --message-body "job-123"
```

These commands are useful for understanding operational workflows, not just memorizing syntax.

**FAQ: What is the best way to think about VPC design?**

A VPC is your isolated network boundary in AWS. The exam often tests whether you can place resources correctly and control traffic flow.

Key components:
- **Subnets** divide the VPC into smaller network segments.
- **Route tables** control where traffic goes.
- **Internet gateways** provide internet access for public subnets.
- **NAT gateways** allow private subnets to initiate outbound internet access without being directly reachable from the internet.
- **Security groups** and **network ACLs** control traffic filtering.

Typical layout:
```text
Public subnet: ALB, NAT Gateway, bastion if required
Private subnet: app servers, databases, internal services
```

A common exam pattern is:
- Put internet-facing components in public subnets.
- Put application and database tiers in private subnets.
- Use NAT for outbound updates or package downloads from private instances.

**FAQ: When do you use a bastion host?**

A **bastion host** is a hardened jump server used for administrative access to private instances.

Use it only when direct administrative access is required and no better managed alternative exists. In many cases, **AWS Systems Manager Session Manager** is preferred because it removes the need to open inbound SSH or RDP ports.

Prefer:
- Session Manager for secure shell access without public exposure.
- Bastion host only when the question explicitly requires SSH/RDP jump access.

**FAQ: What is the difference between S3 versioning and lifecycle policies?**

- **Versioning** preserves multiple versions of an object and helps protect against accidental deletion or overwrite.
- **Lifecycle policies** automate transitions and expirations, such as moving objects to cheaper storage classes or deleting old versions.

Example lifecycle use:
- Move logs from S3 Standard to S3 Glacier after 30 days.
- Expire incomplete multipart uploads.
- Delete old object versions after a retention period.

**FAQ: What should you remember about Route 53?**

Route 53 is AWS DNS and traffic management.

Use it for:
- Domain registration and hosted zones.
- Alias records to AWS resources.
- Health checks and failover routing.
- Weighted, latency-based, geolocation, and multivalue routing.

Exam tip:
- Use **alias records** when pointing to AWS resources like CloudFront, ALB, or S3 website endpoints.
- Use health checks and failover routing when the question asks for automatic DNS-level failover.

**FAQ: What is the difference between public and private subnets?**

- A **public subnet** has a route to an internet gateway.
- A **private subnet** does not have a direct route to an internet gateway.
- Public subnets host internet-facing resources.
- Private subnets host internal application and data tiers.

Important nuance:
- A private subnet can still reach the internet through a NAT gateway for outbound-only traffic.
- A resource is not “public” just because it has a public IP; routing and security rules also matter.

**FAQ: What are the most common exam traps?**

Watch for these patterns:
- Choosing **S3** when the requirement actually needs a file system.
- Choosing **EBS** when multiple instances need shared access.
- Choosing **Multi-Region** when the question only asks for high availability.
- Choosing **NLB** when the workload is HTTP and needs path-based routing.
- Choosing **Read Replica** when the question asks for failover.
- Choosing **bastion host** when Session Manager is a better fit.
- Choosing **self-managed EC2** when a managed service would reduce operational burden.

**FAQ: How do you decide between managed and self-managed services?**

Prefer managed services when the question does not require OS-level control or custom infrastructure behavior.

Examples:
- Use **RDS** instead of running MySQL on EC2.
- Use **Lambda** instead of a small always-on worker if the workload is event-driven.
- Use **ECS/Fargate** instead of managing container hosts if the question emphasizes reduced operations.
- Use **DynamoDB** instead of building a custom key-value store.

Choose self-managed EC2 when the workload needs:
- Custom OS packages or kernel-level control.
- Specialized software not supported by managed services.
- Legacy application constraints.

**FAQ: What is a good mental model for exam answers?**

Think in layers:
- **Identity and access**: IAM, roles, policies, KMS.
- **Network**: VPC, subnets, route tables, security groups, NACLs, NAT, IGW.
- **Compute**: EC2, Auto Scaling, Lambda, ECS/Fargate.
- **Storage**: S3, EBS, EFS, FSx.
- **Database**: RDS, Aurora, DynamoDB, ElastiCache, Redshift.
- **Edge and delivery**: CloudFront, Route 53.
- **Integration and messaging**: SQS, SNS, EventBridge.
- **Operations**: CloudWatch, CloudTrail, Systems Manager.

If a question is ambiguous, identify the layer with the strongest requirement and choose the simplest service that satisfies it.

**FAQ: What should you do during the exam when two answers look similar?**

1. Eliminate answers that do not satisfy a stated constraint.
2. Prefer the service that is managed and operationally simpler.
3. Check whether the question asks for availability, durability, performance, or cost.
4. Match the exact use case, not a broader architecture.
5. If the question mentions “least administrative effort,” avoid self-managed designs.

**Exam focus summary**

The highest-value exam skill is service selection under constraints. You should be able to explain why one service is better than another in a specific scenario.

Focus on:
- Storage type selection: object, block, file, or ephemeral.
- Database selection: relational, NoSQL, cache, or analytics.
- Resilience patterns: Multi-AZ, Auto Scaling, failover, backups.
- Networking patterns: public/private subnets, NAT, security groups, load balancers.
- Decoupling patterns: SQS, SNS, EventBridge.
- Security patterns: IAM roles, KMS, encryption, least privilege.

If you can map a business requirement to the right AWS building block quickly, you are aligned with the exam’s intent.

End of Notes