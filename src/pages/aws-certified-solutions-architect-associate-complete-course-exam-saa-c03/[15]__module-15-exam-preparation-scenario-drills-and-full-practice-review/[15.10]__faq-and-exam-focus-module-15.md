---
title: "FAQ and Exam Focus — AWS Certified Solutions Architect – Associate (SAA-C03)"
description: "Key concepts for SAA-C03 exam prep, with practical AWS architecture FAQs, service tradeoffs, and high-yield decision patterns."
author: Webnotes
order: 1
image: /content/assets/faq-and-exam-focus.png
keywords:
  - AWS Certified Solutions Architect – Associate (Exam SAA-C03)
  - exam prep
  - study notes
canonical: https://webnotes.site/content/aws-certified-solutions-architect-associate-complete-course-exam-saa-c03/faq-and-exam-focus/
twitterCard: summary_large_image
twitterSite: "@webnotes"
schemaType: TechArticle
date: 2026-04-26
lastModified: 2026-04-26
---
## **Course:** AWS Certified Solutions Architect – Associate Complete Course (Exam SAA-C03)

**What the exam emphasizes**

The SAA-C03 exam tests whether you can choose the right AWS service for a scenario, not whether you can recite every feature. Most questions describe a business need, then ask for the most secure, cost-effective, highly available, or operationally efficient design. You should expect tradeoff questions where multiple answers look plausible.

High-yield exam themes include:

- **Availability** across multiple Availability Zones (AZs)
- **Durability** and backup strategy for data
- **Security** using least privilege, encryption, and network isolation
- **Scalability** with managed services and decoupled architectures
- **Cost optimization** through right-sizing, storage tiers, and serverless patterns
- **Operational simplicity** by preferring managed services over self-managed infrastructure

A useful exam habit is to identify the primary requirement first. If the question says “lowest operational overhead,” prefer managed services. If it says “lowest latency,” think about caching, edge services, or placement close to users. If it says “strict compliance,” look for encryption, logging, and network segmentation.

**How to read scenario questions**

SAA-C03 questions often include distractors that are technically possible but not the best fit. You should compare answers using these filters:

- **Does it meet the stated requirement directly?**
- **Does it avoid unnecessary management overhead?**
- **Does it preserve security and fault tolerance?**
- **Does it scale without redesign?**
- **Does it minimize cost for the expected usage pattern?**

When two answers both work, the better answer is usually the one that is:
- More managed
- More resilient
- More secure by default
- Less expensive at the stated scale

**Common AWS architecture FAQs**

**1. When should you use an Auto Scaling group?**

Use an Auto Scaling group when you need EC2 instances to scale in or out based on demand, health checks, or scheduled changes. It is a core answer for web tiers, stateless application tiers, and bursty workloads.

Typical pattern:

- Put instances in at least two AZs
- Attach an Application Load Balancer (ALB)
- Use a launch template
- Scale on CPU, request count, or custom CloudWatch metrics

Example CLI:

```bash
aws autoscaling create-auto-scaling-group \
  --auto-scaling-group-name web-asg \
  --launch-template LaunchTemplateName=web-lt,Version=1 \
  --min-size 2 \
  --max-size 6 \
  --desired-capacity 2 \
  --vpc-zone-identifier "subnet-aaa,subnet-bbb"
```

**2. When is an ALB better than a Network Load Balancer (NLB)?**

Use an **ALB** for HTTP/HTTPS traffic when you need path-based routing, host-based routing, TLS termination, or integration with containers and modern web apps. Use an **NLB** when you need very high performance, static IP support, or TCP/UDP/TLS pass-through behavior.

Quick comparison:

| Requirement | Better choice |
|---|---|
| Path-based routing | ALB |
| Host-based routing | ALB |
| WebSocket / HTTP app routing | ALB |
| Static IPs | NLB |
| Extreme throughput / low latency | NLB |
| TCP/UDP workloads | NLB |

**3. When should you choose S3 over EBS or EFS?**

Choose **Amazon S3** for object storage, backups, static content, logs, data lakes, and durable shared storage accessed over HTTP APIs. Choose **EBS** for block storage attached to a single EC2 instance. Choose **EFS** for shared file storage across multiple Linux instances.

Use S3 when you need:
- Massive durability
- Lifecycle policies
- Event-driven processing
- Static website hosting
- Cross-Region replication for object copies

Example lifecycle policy idea:
- Move objects to Standard-IA after 30 days
- Move to Glacier Instant Retrieval or Glacier Flexible Retrieval later for archival use cases

**4. When should you use EBS snapshots?**

Use **EBS snapshots** for point-in-time backups of EBS volumes. Snapshots are stored in S3-managed infrastructure and are incremental after the first full snapshot. They are useful for disaster recovery, migration, and restoring volumes.

Important exam points:
- Snapshots back up the volume, not the running instance
- You can create a new volume from a snapshot
- You can copy snapshots across Regions
- Encryption is preserved when copying encrypted snapshots correctly

**5. When should you use EFS?**

Use **Amazon EFS** when multiple EC2 instances need shared file access with POSIX-style semantics. It is common for content management systems, shared application assets, and lift-and-shift Linux workloads that expect a file system.

EFS is a good fit when:
- Multiple instances need the same files
- You want managed scaling
- You need regional availability across multiple AZs

EFS is not the best fit for:
- High-performance block storage
- Windows file sharing scenarios that are better served by FSx
- Object storage use cases that belong in S3

**6. When should you use RDS instead of running a database on EC2?**

Use **Amazon RDS** when you want a managed relational database with automated backups, patching, Multi-AZ support, and easier failover. Use EC2-hosted databases only when you need deep OS-level control or unsupported database configurations.

RDS is favored in exam answers because it reduces operational burden:
- Automated backups
- Point-in-time restore
- Multi-AZ high availability
- Read replicas for scaling reads
- Managed maintenance windows

**7. What is the difference between Multi-AZ and read replicas?**

They solve different problems:

- **Multi-AZ** provides high availability and failover for the primary database
- **Read replicas** improve read scaling and can support disaster recovery patterns

Use Multi-AZ when you need:
- Synchronous replication
- Automatic failover
- High availability for writes

Use read replicas when you need:
- Read scaling
- Asynchronous replication
- Offloading reporting queries

> A common exam trap is choosing read replicas for failover. Read replicas are not the primary answer for automatic HA failover in most scenarios.

**8. When should you use DynamoDB?**

Use **Amazon DynamoDB** for serverless NoSQL workloads that need single-digit millisecond latency at scale. It is a strong choice for session data, key-value access patterns, gaming, IoT, and high-traffic applications with predictable access patterns.

DynamoDB is often the best answer when the question mentions:
- No servers to manage
- Massive scale
- Low-latency key-value access
- Flexible schema
- On-demand or provisioned capacity

Example CLI:

```bash
aws dynamodb create-table \
  --table-name Sessions \
  --attribute-definitions AttributeName=SessionId,AttributeType=S \
  --key-schema AttributeName=SessionId,KeyType=HASH \
  --billing-mode PAY_PER_REQUEST
```

**9. When should you use ElastiCache?**

Use **Amazon ElastiCache** for caching frequently accessed data, reducing database load, and improving response times. Choose Redis when you need advanced data structures, persistence options, or pub/sub features. Choose Memcached for simple distributed caching.

Common exam use cases:
- Session caching
- Leaderboards
- Query result caching
- Hot data acceleration

**10. When should you use CloudFront?**

Use **Amazon CloudFront** when you need a content delivery network (CDN) to cache content at edge locations close to users. It improves latency, reduces origin load, and can help with security through AWS WAF integration and origin access controls.

CloudFront is a strong answer for:
- Static websites
- Global content delivery
- API acceleration
- Media distribution
- Reducing load on S3 or ALB origins

Example origin pattern:
- S3 bucket as origin for static assets
- ALB as origin for dynamic web traffic

**Security patterns the exam expects**

Security questions usually reward layered controls. You should think in terms of identity, network, encryption, and logging.

**Identity and access management**

Use **IAM** to control who can do what. Prefer:
- Roles over long-term access keys
- Least privilege policies
- Temporary credentials through STS (Security Token Service)
- Resource-based policies when sharing access across accounts or services

Example IAM policy snippet:

```json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "AllowReadOnlyS3",
      "Effect": "Allow",
      "Action": [
        "s3:GetObject",
        "s3:ListBucket"
      ],
      "Resource": [
        "arn:aws:s3:::example-bucket",
        "arn:aws:s3:::example-bucket/*"
      ]
    }
  ]
}
```

**Encryption**

Expect questions about encryption at rest and in transit:
- Use **KMS** (Key Management Service) for managed encryption keys
- Use TLS for data in transit
- Enable encryption by default where possible
- Rotate keys when required by policy

Common services with encryption support:
- S3
- EBS
- RDS
- EFS
- DynamoDB

**Network isolation**

Use:
- **VPC** subnets to segment workloads
- Security groups for instance-level stateful filtering
- Network ACLs for subnet-level stateless filtering
- Private subnets for internal services
- VPC endpoints to keep traffic on the AWS network

A frequent exam answer is to place databases in private subnets and expose only the load balancer in a public subnet.

**Logging and monitoring**

Use:
- **CloudTrail** for API activity auditing
- **CloudWatch** for metrics, logs, and alarms
- **AWS Config** for configuration compliance tracking
- **GuardDuty** for threat detection

If the question asks how to investigate who changed a resource, CloudTrail is usually the right service.

**High availability and disaster recovery**

The exam often distinguishes between high availability and disaster recovery.

- **High availability (HA)** means the system keeps running during component failure
- **Disaster recovery (DR)** means the system can be restored after a major outage

Common DR strategies:
- Backup and restore
- Pilot light
- Warm standby
- Multi-site active-active

General guidance:
- Use Multi-AZ for HA
- Use cross-Region replication and backups for DR
- Choose the simplest strategy that meets recovery time objective (RTO) and recovery point objective (RPO)

<details>
<summary>Quick DR strategy comparison</summary>
<p>

- **Backup and restore**: lowest cost, highest recovery time
- **Pilot light**: core services running, faster recovery
- **Warm standby**: scaled-down secondary environment, faster still
- **Active-active**: highest cost, best availability

</p>
</details>

**Storage and data transfer exam traps**

Many questions hinge on choosing the right storage class or transfer method.

**S3 storage classes**
- **Standard** for frequent access
- **Standard-IA** for infrequent access with rapid retrieval
- **One Zone-IA** when you can tolerate single-AZ storage
- **Glacier classes** for archival data

Use lifecycle policies to automate transitions based on age or access patterns.

**Data transfer**
- Use **AWS DataSync** for online data movement between on-premises and AWS storage
- Use **Snowball** for large offline transfers when network transfer is impractical
- Use **Transfer Family** for managed SFTP/FTPS/FTP access to S3 or EFS

**Compute selection**

Choose the compute service that matches the workload:

- **EC2** for full control, custom software, and legacy workloads
- **Lambda** for event-driven, short-lived, stateless functions
- **ECS/EKS** for containerized applications
- **Fargate** when you want containers without managing servers

A common exam pattern is:
- If the workload is event-driven and intermittent, choose Lambda
- If the workload needs long-running processes or custom OS control, choose EC2
- If the workload is containerized and you want less infrastructure management, choose ECS with Fargate

**Lambda example**

```python
import json

def lambda_handler(event, context):
    return {
        "statusCode": 200,
        "body": json.dumps({"message": "ok"})
    }
```

**Networking concepts to remember**

**VPC design**
- Use at least two AZs for resilience
- Put public-facing components behind a load balancer
- Keep databases and internal services private
- Use route tables to control traffic flow

**Route 53**
- Use **Route 53** for DNS, health checks, and routing policies
- Common routing policies include simple, weighted, latency-based, failover, and geolocation
- Use alias records for AWS resources like CloudFront, ALB, and S3 website endpoints

**VPC endpoints**
- Use gateway endpoints for S3 and DynamoDB
- Use interface endpoints for many other AWS services
- Prefer endpoints when you want private connectivity without internet traversal

**Exam focus by question style**

**“Most cost-effective”**
- Prefer managed services
- Use storage lifecycle policies
- Use serverless where it fits
- Avoid overprovisioning
- Consider reserved capacity only when usage is steady and predictable

**“Most operationally efficient”**
- Prefer RDS over self-managed databases
- Prefer S3 over custom file servers
- Prefer Lambda or Fargate over managing EC2 when appropriate
- Use managed monitoring and automation

**“Most secure”**
- Use private subnets
- Encrypt data at rest and in transit
- Use IAM roles and least privilege
- Add logging and detection
- Avoid public exposure unless required

**“Most available”**
- Deploy across multiple AZs
- Use load balancers and Auto Scaling
- Use Multi-AZ databases
- Consider multi-Region only when the requirement justifies the complexity

**Practical architecture examples**

**Static website with global delivery**
- Store assets in S3
- Serve through CloudFront
- Use Route 53 for DNS
- Add AWS WAF if you need web protection

**Highly available web application**
- Route 53 to ALB
- ALB across two or more AZs
- EC2 Auto Scaling group in private subnets
- RDS Multi-AZ
- ElastiCache for session caching
- CloudWatch alarms for scaling and health

**Serverless API**
- API Gateway in front of Lambda
- DynamoDB for persistence
- S3 for uploads
- CloudWatch Logs for observability
- IAM roles for service permissions

**Migration-friendly file sharing**
- EFS for shared Linux file access
- DataSync for moving data from on-premises
- Security groups to restrict access
- Backup strategy for recovery

**CLI and configuration snippets you should recognize**

Create an S3 bucket with versioning:

```bash
aws s3api create-bucket --bucket my-example-bucket --region us-east-1
aws s3api put-bucket-versioning \
  --bucket my-example-bucket \
  --versioning-configuration Status=Enabled
```

Create an RDS read replica:

```bash
aws rds create-db-instance-read-replica \
  --db-instance-identifier my-replica \
  --source-db-instance-identifier my-primary
```

Create a CloudFront distribution conceptually:
- Origin: S3 bucket or ALB
- Default cache behavior: GET/HEAD
- Viewer protocol policy: redirect HTTP to HTTPS
- Optional: signed URLs or cookies for restricted content

**Common misconceptions**

- **S3 is not a file system** in the traditional POSIX sense
- **Read replicas are not the same as Multi-AZ**
- **Security groups are stateful**, while network ACLs are stateless
- **Lambda is not ideal for long-running jobs**
- **EBS is attached to one instance at a time** in typical use
- **CloudFront is not a replacement for origin storage**
- **Route 53 is DNS, not a load balancer**
- **VPC endpoints do not replace IAM permissions**

**How to eliminate wrong answers quickly**

Use this mental checklist:
1. Identify the workload type: web, batch, analytics, storage, database, or event-driven.
2. Identify the top requirement: cost, availability, security, performance, or simplicity.
3. Eliminate answers that add unnecessary management.
4. Eliminate answers that do not meet the requirement directly.
5. Prefer AWS-native managed services when they fit the use case.

**Final exam reminders**

The exam rewards architecture judgment. You do not need to memorize every feature, but you do need to know the common service boundaries and the usual best-fit patterns. If you can map a scenario to the right storage, compute, networking, and security choices, you can usually eliminate distractors confidently.

Focus on:
- Service purpose and limitations
- HA versus DR
- Managed versus self-managed tradeoffs
- Security defaults
- Cost and scale patterns

End of Notes