---
title: "FAQ and Exam Focus — AWS Certified Solutions Architect – Associate (SAA-C03)"
description: "Key concepts for SAA-C03 exam prep, with practical AWS FAQs, common traps, and high-yield architecture patterns."
author: Webnotes
order: 1
image: /content/assets/faq-and-exam-focus.png
keywords:
  - AWS Certified Solutions Architect – Associate (Exam SAA-C03)
  - study notes
  - exam prep
  - FAQ
canonical: https://webnotes.site/content/aws-certified-solutions-architect-associate-complete-course-exam-saa-c03/faq-and-exam-focus/
twitterCard: summary_large_image
twitterSite: "@webnotes"
schemaType: TechArticle
date: 2026-04-26
lastModified: 2026-04-26
---
## **Course:** AWS Certified Solutions Architect – Associate Complete Course (Exam SAA-C03)

**What this topic covers**

This section focuses on the kinds of questions that repeatedly appear in AWS Certified Solutions Architect – Associate exams: service selection, architecture tradeoffs, cost, resilience, security, and operational fit. The goal is not memorization alone; you need to recognize the best answer from several plausible options.

For SAA-C03, the exam often tests whether you can:
- Choose the right managed service for a workload.
- Design for high availability and fault tolerance.
- Reduce operational overhead without sacrificing security.
- Balance performance, cost, and durability.
- Identify when a solution is over-engineered or under-secured.

**How to approach exam questions**

Read the scenario for the business goal first, then map it to the technical constraint. Many questions include distractors that are technically valid but do not satisfy the stated requirement.

- **Availability** usually means Multi-AZ, load balancing, and automatic failover.
- **Scalability** usually means horizontal scaling, Auto Scaling, or serverless services.
- **Durability** usually points to Amazon S3, Amazon EBS snapshots, or managed replication.
- **Low operational overhead** usually favors managed services such as Amazon RDS, Amazon DynamoDB, AWS Lambda, or Amazon ECS with Fargate.
- **Security** usually means least privilege, encryption, private connectivity, and centralized identity.

A useful exam habit is to ask:
1. What is the primary requirement?
2. What is the simplest AWS-native service that satisfies it?
3. What failure mode is the question trying to avoid?

**High-yield service selection patterns**

Many exam questions are really service-matching questions in disguise. If you can map the workload to the right AWS service family, you eliminate most wrong answers quickly.

- **Amazon S3** is the default for object storage, static content, backups, and data lakes.
- **Amazon EBS** is block storage attached to a single EC2 instance in one Availability Zone.
- **Amazon EFS** is shared file storage for Linux workloads across multiple instances and AZs.
- **Amazon RDS** is relational database service when you want managed backups, patching, and Multi-AZ options.
- **Amazon Aurora** is the preferred relational choice when the question emphasizes performance, availability, or MySQL/PostgreSQL compatibility.
- **Amazon DynamoDB** is the default for serverless NoSQL with predictable low-latency access at scale.
- **Amazon CloudFront** is the edge delivery option for global caching and lower latency.
- **Elastic Load Balancing (ELB)** is used to distribute traffic across targets and improve availability.
- **AWS Lambda** is the event-driven compute choice when you want no server management.
- **Amazon ECS with Fargate** is useful when you want containers without managing EC2 hosts.

> If a question says “minimize management” and “scale automatically,” the answer is often a managed or serverless service rather than self-managed EC2.

**Common architecture questions and the likely answer shape**

The exam frequently asks you to improve an existing design. The best answer usually changes only the necessary component rather than redesigning the whole system.

- If a single EC2 instance is a single point of failure, place it behind an Application Load Balancer and run it in multiple AZs.
- If a database must survive an AZ failure, use Multi-AZ deployment or a managed replication strategy.
- If users worldwide need faster access to static assets, place content in S3 and serve it through CloudFront.
- If an application needs shared storage across instances, use EFS rather than EBS.
- If a workload needs decoupling between producers and consumers, use Amazon SQS or Amazon SNS depending on whether you need queueing or fanout.
- If you need event-driven processing from AWS services, use EventBridge, Lambda, or SQS depending on the trigger and delivery model.

**Availability and resilience FAQ**

**Q: What is the difference between high availability and fault tolerance?**

High availability means the system remains accessible with minimal downtime, usually by removing single points of failure. Fault tolerance goes further and continues operating even when a component fails, often with active-active or redundant design.

- **High availability** is the more common exam target.
- **Fault tolerance** is more expensive and more complex.
- Multi-AZ architectures are the standard AWS answer for availability.

**Q: When should you use Multi-AZ versus Multi-Region?**

Use Multi-AZ when you need resilience to an Availability Zone failure within one Region. Use Multi-Region when you need disaster recovery, geographic redundancy, or lower latency for global users.

- **Multi-AZ** is usually enough for most exam scenarios.
- **Multi-Region** is chosen when the question explicitly mentions regional disaster recovery or global failover.
- Multi-Region designs add complexity, data replication concerns, and higher cost.

**Q: What is the best way to make an application stateless?**

Move session state and user data out of the application tier.

- Store sessions in ElastiCache, DynamoDB, or a database if needed.
- Store files in S3.
- Keep EC2 instances replaceable and horizontally scalable.

**Security FAQ**

**Q: What is the most common security mistake in exam scenarios?**

Exposing resources publicly when private access is possible. The exam often rewards designs that use private subnets, security groups, IAM roles, VPC endpoints, and encryption.

- Use **IAM roles** for AWS service access instead of long-lived access keys on instances.
- Use **security groups** for stateful instance-level filtering.
- Use **network ACLs** for stateless subnet-level filtering when needed.
- Use **KMS** for encryption key management.
- Use **VPC endpoints** to keep traffic to AWS services inside the AWS network.

**Q: When do you use a security group versus a network ACL?**

Use security groups for most instance and load balancer traffic control. Use network ACLs when you need subnet-level stateless rules or an additional layer of control.

- **Security groups** are stateful and easier to manage.
- **Network ACLs** are stateless and evaluate inbound and outbound rules separately.
- The exam usually prefers security groups unless a subnet boundary is specifically relevant.

**Q: When should you use AWS KMS?**

Use AWS Key Management Service (KMS) when you need to encrypt data at rest with managed keys, control key usage, or integrate encryption with AWS services.

- S3, EBS, RDS, and many other services support KMS integration.
- Use customer managed keys when you need tighter control and auditability.
- Use AWS managed keys when you want simpler default encryption.

**Storage FAQ**

**Q: How do you choose between S3, EBS, and EFS?**

Choose based on access pattern and attachment model.

| Service | Best for | Key constraint |
|---|---|---|
| S3 | Object storage, backups, static content | Not mounted like a file system |
| EBS | Block storage for one EC2 instance in one AZ | Single-instance attachment model |
| EFS | Shared file storage across multiple instances | Linux-focused shared file system |

- **S3** is the default answer for durable object storage.
- **EBS** is the default answer for boot volumes and low-latency block storage.
- **EFS** is the default answer for shared POSIX-style file access.

**Q: When should you use S3 Glacier or archival storage?**

Use archival tiers when data is rarely accessed but must be retained for compliance or long-term backup.

- Choose archival storage when retrieval time is acceptable.
- Do not use archival tiers for active application data.
- The exam may mention retention, compliance, or infrequent access.

**Database FAQ**

**Q: When is RDS better than running a database on EC2?**

Use Amazon RDS when you want managed backups, patching, monitoring, and Multi-AZ support without managing the database host yourself.

- RDS reduces operational burden.
- EC2-hosted databases are only appropriate when you need OS-level control or unsupported configurations.
- The exam usually prefers managed databases unless a specific limitation requires self-management.

**Q: When is Aurora the better choice?**

Choose Aurora when the question emphasizes high performance, fast failover, or compatibility with MySQL/PostgreSQL while staying managed.

- Aurora is often the preferred relational answer in modern AWS architectures.
- It supports read scaling and high availability patterns.
- If the question only says “managed relational database,” RDS may still be acceptable.

**Q: When should you use DynamoDB instead of RDS?**

Use DynamoDB when the workload is key-value or document-oriented, needs very low latency, and benefits from serverless scaling.

- DynamoDB is not a relational database.
- It is a strong fit for session data, metadata, and high-scale application state.
- If the question requires joins, complex transactions, or relational reporting, RDS is usually better.

**Networking FAQ**

**Q: What is the difference between public and private subnets?**

A public subnet has a route to an Internet Gateway. A private subnet does not have direct internet routing.

- Put internet-facing load balancers in public subnets.
- Put application and database tiers in private subnets.
- Use NAT Gateway when private instances need outbound internet access for updates or package downloads.

**Q: When do you use a NAT Gateway?**

Use a NAT Gateway when instances in a private subnet need outbound internet access but should not be reachable from the internet.

- NAT Gateway supports outbound-only internet access.
- It is managed and highly available within an AZ.
- For exam questions, NAT Gateway is usually preferred over NAT instances unless cost or custom routing is explicitly emphasized.

**Q: What is the role of Route 53?**

Amazon Route 53 provides DNS, health checks, and routing policies.

- Use simple routing for straightforward name resolution.
- Use weighted routing for gradual traffic shifting.
- Use latency-based routing for global performance.
- Use failover routing for active-passive disaster recovery.
- Use geolocation routing when user location matters.

**Compute FAQ**

**Q: When should you choose Lambda over EC2?**

Choose Lambda when the workload is event-driven, short-lived, and you want to avoid server management.

- Lambda is ideal for file processing, API backends, automation, and scheduled tasks.
- EC2 is better when you need full OS control, long-running processes, or specialized software.
- The exam often favors Lambda if the workload can be decomposed into events.

**Q: When should you choose ECS Fargate over EC2?**

Choose Fargate when you want containers without managing the underlying servers.

- Fargate reduces operational overhead.
- EC2-backed ECS gives more control and may be cheaper at scale in some cases.
- If the question emphasizes “no server management,” Fargate is usually the better answer.

**Q: When should you use Auto Scaling?**

Use Auto Scaling when demand changes and you want the fleet size to adjust automatically.

- Scale out for increased load.
- Scale in when demand drops.
- Pair Auto Scaling with a load balancer for stateless web tiers.

**Messaging and decoupling FAQ**

**Q: When do you use SQS versus SNS?**

Use SQS for queue-based decoupling and buffering. Use SNS for pub/sub fanout to multiple subscribers.

- **SQS** is for one consumer or competing consumers.
- **SNS** is for broadcasting messages to multiple endpoints.
- SNS can deliver to SQS, Lambda, HTTP endpoints, and email subscriptions.

**Q: When should you use EventBridge?**

Use EventBridge for event routing across AWS services and SaaS integrations, especially when you need rules-based filtering.

- EventBridge is useful for loosely coupled event-driven architectures.
- It is often the best fit for application events and service events.
- If the question asks for “react to AWS events,” EventBridge is a strong candidate.

**Exam traps and distractors**

The exam often includes answers that are technically possible but not the best fit. Watch for these patterns:

- Choosing EC2 when a managed service would reduce operational burden.
- Choosing a single-AZ design when the requirement implies resilience.
- Choosing Multi-Region when Multi-AZ is sufficient.
- Choosing EBS when multiple instances need shared access.
- Choosing RDS when the workload is clearly NoSQL.
- Choosing a public subnet when private connectivity is possible.
- Choosing a custom solution when AWS already provides a managed service.

**CLI commands you should recognize**

You do not need to memorize every command, but you should recognize the purpose of common AWS CLI operations.

```bash
# List S3 buckets
aws s3 ls

# Copy a file to S3
aws s3 cp app.zip s3://my-deployment-bucket/releases/

# Describe EC2 instances
aws ec2 describe-instances

# Create an Auto Scaling group from a launch template
aws autoscaling create-auto-scaling-group \
  --auto-scaling-group-name web-asg \
  --launch-template LaunchTemplateName=web-lt,Version='$Latest' \
  --min-size 2 \
  --max-size 6 \
  --desired-capacity 2 \
  --vpc-zone-identifier subnet-aaa,subnet-bbb

# Describe RDS DB instances
aws rds describe-db-instances

# Put an object into S3 with server-side encryption using KMS
aws s3 cp report.csv s3://my-secure-bucket/ \
  --sse aws:kms \
  --sse-kms-key-id alias/my-key
```

These commands matter because they reinforce service intent:
- `aws s3 cp` suggests object storage and deployment artifacts.
- `aws ec2 describe-instances` suggests instance inventory and troubleshooting.
- `aws autoscaling create-auto-scaling-group` suggests horizontal scaling.
- `aws rds describe-db-instances` suggests managed relational database operations.

**Practical architecture examples**

A common web application pattern is:
- Route 53 for DNS.
- CloudFront for global caching.
- Application Load Balancer in public subnets.
- EC2 Auto Scaling group in private subnets.
- RDS Multi-AZ for the database.
- S3 for static assets and backups.
- IAM roles for instance permissions.

A common serverless pattern is:
- API Gateway or an event source.
- Lambda for compute.
- DynamoDB for state.
- S3 for object storage.
- EventBridge or SQS for decoupling.

A common batch-processing pattern is:
- S3 upload triggers an event.
- EventBridge or S3 event notification sends work to Lambda or SQS.
- Workers process the file.
- Results are stored in S3, DynamoDB, or RDS depending on the data model.

**How to eliminate wrong answers quickly**

Use the requirement keywords to narrow the answer set.

- If you see **lowest operational overhead**, prefer managed services.
- If you see **shared storage**, think EFS.
- If you see **object storage**, think S3.
- If you see **block storage**, think EBS.
- If you see **global caching**, think CloudFront.
- If you see **decouple components**, think SQS, SNS, or EventBridge.
- If you see **private access to AWS services**, think VPC endpoints.
- If you see **relational with failover**, think RDS Multi-AZ or Aurora.
- If you see **NoSQL at scale**, think DynamoDB.

**Final exam mindset**

The best SAA-C03 answer is usually the one that is:
- Secure by default.
- Highly available without unnecessary complexity.
- Managed rather than self-managed.
- Cost-aware without sacrificing the stated requirement.
- Simple enough to operate in real life.

If two answers both seem correct, choose the one that most directly satisfies the business requirement with the fewest moving parts.

End of Notes