---
title: "FAQ and Exam Focus — AWS Certified Solutions Architect – Associate (SAA-C03)"
description: "Key concepts for SAA-C03 exam prep, with practical AWS architecture FAQs, service tradeoffs, and high-yield decision points."
author: Webnotes
order: 1
image: /content/assets/faq-and-exam-focus.png
keywords:
  - AWS Certified Solutions Architect – Associate (Exam SAA-C03)
  - exam prep
  - study notes
canonical: https://webnotes.site/content/aws-certified-solutions-architect-associate-complete-course-exam-saa-c03/faq-and-exam-focus/
twitterCard: summary_large_image
twitterSite: "@webnotes"
schemaType: TechArticle
date: 2026-04-26
lastModified: 2026-04-26
---
## **Course:** AWS Certified Solutions Architect – Associate Complete Course (Exam SAA-C03)

**What the exam usually tests**

The SAA-C03 exam focuses on choosing the right AWS service for a scenario, not memorizing every feature. You are expected to understand tradeoffs around availability, durability, scalability, security, cost, and operational effort. Many questions describe a business need and ask for the most appropriate architecture.

High-yield exam themes include:

- **High availability** across multiple Availability Zones (AZs)
- **Fault tolerance** and automatic recovery
- **Decoupling** with queues, topics, and event-driven patterns
- **Storage selection** for object, block, file, and archival use cases
- **Identity and access** with least privilege
- **Network design** using subnets, route tables, gateways, and endpoints
- **Cost optimization** without sacrificing requirements
- **Migration** and hybrid connectivity patterns

A useful exam habit is to identify the constraint first:

- If the question emphasizes lowest latency, think about caching, placement, and regional proximity.
- If it emphasizes durability, think about Amazon S3, backups, replication, and managed storage.
- If it emphasizes elasticity, think about Auto Scaling, managed services, and serverless options.
- If it emphasizes security, think about IAM, KMS, security groups, and private connectivity.

**How to approach scenario questions**

Read the last line of the question first. It often tells you what the architecture must optimize for. Then scan for keywords that narrow the answer:

- **Public access** versus private access
- **Synchronous** versus asynchronous processing
- **Single-AZ** versus Multi-AZ
- **RTO** (Recovery Time Objective) and **RPO** (Recovery Point Objective)
- **Burst traffic** versus steady traffic
- **Shared storage** versus isolated storage
- **Managed** versus self-managed operations

A practical elimination strategy:

1. Remove answers that violate a stated requirement, such as public exposure when private access is required.
2. Remove answers that add unnecessary operational burden when a managed service exists.
3. Remove answers that solve the wrong problem, such as using a database when the workload is object storage.
4. Prefer the simplest architecture that meets the requirements.

> If two answers both seem correct, the exam often favors the one that is more managed, more scalable, and more aligned with AWS best practices.

**Core service selection patterns**

Many exam questions are really service-matching exercises. The following patterns appear frequently.

- **Amazon S3** for durable object storage, static website hosting, backups, logs, and data lakes
- **Amazon EBS** for block storage attached to a single EC2 instance
- **Amazon EFS** for shared file storage across multiple Linux instances
- **Amazon FSx** for specialized file systems such as Windows file shares or high-performance workloads
- **Amazon RDS** for relational databases with managed backups, patching, and Multi-AZ options
- **Amazon DynamoDB** for serverless NoSQL with predictable low-latency access
- **Amazon EC2 Auto Scaling** for variable compute demand
- **Elastic Load Balancing** for distributing traffic across targets
- **Amazon CloudFront** for global caching and edge delivery
- **Amazon Route 53** for DNS, health checks, and routing policies
- **AWS Lambda** for event-driven, short-running, serverless compute
- **Amazon SQS** for decoupling and buffering
- **Amazon SNS** for fan-out notifications
- **Amazon EventBridge** for event routing and SaaS integration

**Storage FAQ and exam focus**

Storage questions often ask you to choose between durability, performance, and sharing.

- **S3** is object storage, not a file system.
- **EBS** is attached to one EC2 instance at a time in a single AZ, though snapshots are stored in S3-managed infrastructure.
- **EFS** is shared, elastic, and mounted by multiple instances concurrently.
- **S3 Glacier** classes are for archival and infrequent retrieval, not active application data.
- **Instance store** is temporary and lost when the instance stops or terminates.

A common comparison:

| Service | Best for | Key limitation |
|---|---|---|
| S3 | Durable objects, static content, backups | Not mounted like a traditional file system |
| EBS | Low-latency block storage for EC2 | Tied to an instance and AZ |
| EFS | Shared Linux file storage | Higher latency than local block storage |
| FSx | Specialized managed file systems | Use case-specific and not universal |

Practical examples:

```bash
aws s3 cp backup.sql s3://my-backup-bucket/db/
aws s3 sync ./site s3://my-static-site-bucket --delete
```

```bash
# Example: mount EFS on Linux
sudo yum install -y amazon-efs-utils
sudo mkdir -p /mnt/efs
sudo mount -t efs fs-12345678:/ /mnt/efs
```

**Compute FAQ and exam focus**

For compute, the exam often asks whether you should use EC2, containers, or Lambda.

- **EC2** gives you the most control and is appropriate when you need custom OS-level configuration.
- **Auto Scaling** helps maintain availability and match capacity to demand.
- **Lambda** is ideal for event-driven workloads, short tasks, and reduced operational overhead.
- **Containers** are useful when you need portability and consistent runtime packaging.
- **Spot Instances** can reduce cost for fault-tolerant workloads, but they can be interrupted.

Typical exam cues:

- Use **Lambda** when the workload is triggered by events and does not need persistent servers.
- Use **EC2 Auto Scaling** when you need long-running applications with variable traffic.
- Use **Spot Instances** when interruption is acceptable and cost matters.
- Use **Reserved Instances** or Savings Plans when usage is steady and predictable.

Example Auto Scaling launch configuration concepts:

```bash
aws autoscaling create-auto-scaling-group \
  --auto-scaling-group-name web-asg \
  --launch-template LaunchTemplateName=web-lt,Version='$Latest' \
  --min-size 2 \
  --max-size 6 \
  --desired-capacity 2 \
  --vpc-zone-identifier "subnet-aaa,subnet-bbb"
```

**Database FAQ and exam focus**

Database questions usually test whether you understand relational versus non-relational design and managed availability.

- **RDS** is the default choice for relational workloads that need SQL, transactions, and managed operations.
- **Aurora** is a high-performance relational option compatible with MySQL and PostgreSQL engines.
- **DynamoDB** is best for key-value and document access patterns with massive scale.
- **ElastiCache** is for in-memory caching, not primary durable storage.
- **Redshift** is for analytics and data warehousing, not OLTP (Online Transaction Processing).

Important distinctions:

- Use **Multi-AZ** for high availability and automatic failover.
- Use **Read Replicas** to scale read-heavy workloads and offload reads.
- Use **DynamoDB Global Tables** when you need multi-region active-active access for NoSQL data.
- Use **RDS snapshots** for backups and point-in-time recovery when supported.

Example RDS backup and read replica concepts:

```bash
aws rds create-db-snapshot \
  --db-instance-identifier mydb \
  --db-snapshot-identifier mydb-snapshot-2026-04-26
```

**Networking FAQ and exam focus**

Networking questions often center on VPC design, routing, and private connectivity.

- **Security groups** are stateful and attached to ENIs (Elastic Network Interfaces).
- **Network ACLs** are stateless and operate at the subnet level.
- **Public subnets** have a route to an Internet Gateway.
- **Private subnets** do not route directly to the internet.
- **NAT Gateways** allow private subnet instances to initiate outbound internet access without being publicly reachable.
- **VPC endpoints** keep traffic private when accessing AWS services.

Common routing components:

- **Internet Gateway** for internet access from public subnets
- **NAT Gateway** for outbound-only internet access from private subnets
- **Route tables** to control traffic paths
- **Transit Gateway** for hub-and-spoke connectivity across many VPCs
- **Direct Connect** for dedicated private network connectivity from on-premises to AWS

Example route table logic:

```text
Public subnet route table:
0.0.0.0/0 -> Internet Gateway

Private subnet route table:
0.0.0.0/0 -> NAT Gateway
```

A common CLI check:

```bash
aws ec2 describe-route-tables --filters Name=vpc-id,Values=vpc-12345678
```

**Load balancing and traffic management**

The exam often asks which load balancer to use based on protocol and layer.

- **Application Load Balancer (ALB)** is best for HTTP/HTTPS and path-based routing.
- **Network Load Balancer (NLB)** is best for ultra-high performance, static IPs, and TCP/UDP traffic.
- **Gateway Load Balancer** is used for third-party network appliances.

Route 53 routing policies are also common:

- **Simple routing** for one endpoint
- **Weighted routing** for gradual traffic shifting
- **Latency-based routing** for lower response times
- **Failover routing** for active-passive designs
- **Geolocation routing** for region-based control

**Security FAQ and exam focus**

Security questions usually reward least privilege and defense in depth.

- **IAM roles** are preferred for AWS service-to-service access.
- **IAM users** are for human identities, but federation is often better at scale.
- **Security groups** should allow only required ports and sources.
- **KMS** (Key Management Service) is used for encryption key management.
- **Secrets Manager** is for rotating and storing secrets.
- **Systems Manager Parameter Store** can store configuration values and secrets, depending on requirements.

Practical IAM policy example:

```json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "AllowReadOnlyS3Bucket",
      "Effect": "Allow",
      "Action": [
        "s3:GetObject",
        "s3:ListBucket"
      ],
      "Resource": [
        "arn:aws:s3:::my-bucket",
        "arn:aws:s3:::my-bucket/*"
      ]
    }
  ]
}
```

Encryption patterns to remember:

- Use **SSE-S3** for simple server-side encryption managed by S3.
- Use **SSE-KMS** when you need key control, auditability, or tighter governance.
- Use **client-side encryption** when data must be encrypted before it reaches AWS services.
- Use **TLS** for data in transit.

**High availability and disaster recovery**

The exam frequently tests whether you can distinguish HA from DR.

- **High availability** keeps a workload running during component failure.
- **Disaster recovery** restores service after a major outage or data loss event.

Common DR strategies:

| Strategy | Cost | Recovery speed | Typical use |
|---|---:|---:|---|
| Backup and restore | Low | Slow | Non-critical workloads |
| Pilot light | Low-medium | Faster | Core services kept minimal |
| Warm standby | Medium | Fast | Reduced downtime tolerance |
| Multi-site active-active | High | Fastest | Mission-critical systems |

Key ideas:

- Multi-AZ is usually for high availability within a region.
- Multi-Region is usually for disaster recovery or global low-latency access.
- Backups should be automated and tested.
- Replication does not replace backups.

**Monitoring and operational visibility**

Monitoring questions often ask how to detect issues and trigger response.

- **CloudWatch metrics** track performance and utilization.
- **CloudWatch alarms** notify or automate actions when thresholds are crossed.
- **CloudWatch Logs** store application and system logs.
- **CloudTrail** records API activity for auditing and investigation.
- **AWS Config** tracks resource configuration changes and compliance.

Example alarm creation concept:

```bash
aws cloudwatch put-metric-alarm \
  --alarm-name HighCPU \
  --metric-name CPUUtilization \
  --namespace AWS/EC2 \
  --statistic Average \
  --period 300 \
  --threshold 80 \
  --comparison-operator GreaterThanThreshold \
  --dimensions Name=InstanceId,Value=i-1234567890abcdef0 \
  --evaluation-periods 2
```

**Migration FAQ and exam focus**

Migration questions often focus on minimizing downtime and choosing the right transfer method.

- **AWS Application Migration Service** helps lift-and-shift servers.
- **Database Migration Service (DMS)** helps migrate databases with low downtime.
- **Snowball** is for large offline data transfers.
- **DataSync** is for online data transfer between on-premises storage and AWS.
- **Storage Gateway** bridges on-premises environments with AWS storage.

When to use each:

- Use **DMS** when you need ongoing replication and cutover planning for databases.
- Use **Snowball** when network bandwidth is too limited for large transfers.
- Use **DataSync** when you need scheduled or accelerated file transfers.
- Use **Storage Gateway** when you need hybrid storage integration.

**Common exam traps**

The exam often includes distractors that are technically possible but not best practice.

- Choosing **self-managed EC2** when a managed service fits better
- Using **S3** as if it were a mounted file system
- Using **RDS** when the workload is clearly key-value access
- Using **NAT Instance** instead of **NAT Gateway** without a specific reason
- Using **Single-AZ** when the requirement clearly asks for high availability
- Using **CloudFront** when the problem is private internal access, not global caching

> Be careful with words like “must,” “only,” “lowest cost,” “least operational overhead,” and “high availability.” These usually determine the correct answer more than the service name itself.

**Quick FAQ**

<details>
<summary>Should you memorize every AWS service?</summary>
<p>No. Focus on the core services and the patterns they solve. The exam rewards correct service selection and architecture reasoning more than exhaustive memorization.</p>
</details>

<details>
<summary>Is the cheapest option always correct?</summary>
<p>No. The cheapest option is only correct if it still meets all stated requirements for availability, performance, security, and durability.</p>
</details>

<details>
<summary>When should you choose Multi-AZ?</summary>
<p>Choose Multi-AZ when the workload needs high availability and automatic failover within a region.</p>
</details>

<details>
<summary>When should you choose a read replica?</summary>
<p>Choose a read replica when you need to scale read traffic or offload reporting queries from the primary database.</p>
</details>

**Practical exam-day checklist**

1. Identify the workload type: web, batch, analytics, storage, database, or hybrid.
2. Identify the dominant requirement: cost, availability, durability, latency, security, or scale.
3. Match the requirement to the simplest managed AWS service.
4. Eliminate answers that add unnecessary complexity or violate constraints.
5. Re-read the question for hidden details such as region scope, access pattern, or recovery target.

**Final high-yield reminders**

- **S3** is the default for durable object storage.
- **EBS** is for EC2 block storage.
- **EFS** is for shared file access.
- **RDS** is for managed relational databases.
- **DynamoDB** is for serverless NoSQL.
- **Lambda** is for event-driven compute.
- **ALB** is for HTTP/HTTPS routing.
- **NAT Gateway** enables private subnet outbound internet access.
- **IAM roles** are preferred for AWS workloads.
- **Multi-AZ** is for availability, not just backup.

End of Notes