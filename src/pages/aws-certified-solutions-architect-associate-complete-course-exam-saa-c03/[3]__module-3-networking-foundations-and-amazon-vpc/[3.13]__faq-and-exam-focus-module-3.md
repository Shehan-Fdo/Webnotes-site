---
title: "FAQ and Exam Focus — AWS Certified Solutions Architect – Associate (SAA-C03)"
description: "Key concepts for SAA-C03 exam prep, with high-yield FAQs, service comparisons, and scenario guidance for architecture decisions."
author: Webnotes
order: 1
image: /content/assets/faq-and-exam-focus.png
keywords:
  - AWS Certified Solutions Architect – Associate (Exam SAA-C03)
  - exam prep
  - study notes
  - FAQ and Exam Focus
canonical: https://webnotes.site/content/aws-certified-solutions-architect-associate-complete-course-exam-saa-c03/faq-and-exam-focus/
twitterCard: summary_large_image
twitterSite: "@webnotes"
schemaType: TechArticle
date: 2026-04-26
lastModified: 2026-04-26
---
## **Course:** AWS Certified Solutions Architect – Associate Complete Course (Exam SAA-C03)

**How to use these notes**

SAA-C03 questions are usually scenario-based. You are rarely asked for a definition alone; instead, you choose the service that best fits availability, durability, performance, security, and cost constraints. The exam rewards clear service boundaries and punishes vague “it depends” thinking.

Focus on:
- **Workload fit**: pick the service that matches the access pattern and operational model.
- **Failure domain**: understand what breaks, what fails over, and what you must design around.
- **Cost tradeoffs**: know when a managed service reduces ops overhead enough to justify the price.
- **Security posture**: distinguish authentication, authorization, encryption, and network isolation.

**Exam domains at a glance**

The exam is broad, but the questions cluster around a few repeatable decision areas. If you can reason through these, you can eliminate distractors quickly.

- **Design secure architectures**
  - IAM, KMS, ACM, WAF, Shield, security groups, NACLs, and cross-account access.
- **Design resilient architectures**
  - Multi-AZ, backups, replication, Route 53 routing, and disaster recovery patterns.
- **Design high-performing architectures**
  - Caching, load balancing, storage choices, database selection, and compute fit.
- **Design cost-optimized architectures**
  - Right-sizing, managed services, storage tiers, Spot, Savings Plans, and lifecycle policies.
- **Operational excellence**
  - Monitoring, logging, automation, patching, and incident response.

**Core decision rules you should know**

These are the kinds of distinctions the exam tests repeatedly.

- **S3** is object storage for durable, scalable, low-cost data access.
- **EBS** is block storage for a single EC2 instance in normal use; **Multi-Attach** exists only for specific `io1`/`io2` volumes in supported AZs with Nitro-based instances.
- **EFS** is shared file storage for Linux workloads that need concurrent access.
- **FSx** is specialized file storage when you need Windows file shares, Lustre performance, or NetApp ONTAP features.
- **RDS** is managed relational database service; **Aurora** is the AWS-optimized relational option with higher performance and better scaling characteristics.
- **DynamoDB** is serverless NoSQL for predictable low-latency key-value and document access.
- **Lambda** is event-driven compute for short-lived functions; it is not a general replacement for long-running servers.
- **ECS/EKS/Fargate** are container options; choose them when you need container orchestration rather than raw VM management.
- **ALB** handles Layer 7 routing; **NLB** handles Layer 4 performance and static IP use cases; **CLB** is legacy and rarely the best answer.
- **CloudFront** is a CDN for edge caching and global delivery.
- **Route 53** handles DNS and routing policies.
- **SNS** is pub/sub fanout; **SQS** is decoupled queueing; **EventBridge** is event routing and integration.
- **Systems Manager** helps with patching, inventory, automation, and secure instance access without opening SSH broadly.

**Compute: choosing the right service**

Compute questions often ask you to balance control, elasticity, and operational overhead.

- **EC2**
  - Use when you need full OS control, custom agents, legacy software, or long-running processes.
  - Good for lift-and-shift, specialized networking, and workloads that do not fit serverless or containers.
- **Lambda**
  - Use for event-driven tasks, short execution, and bursty workloads.
  - Good for file processing, API backends, scheduled jobs, and automation.
  - Not ideal for long-running tasks, heavy local state, or workloads that need persistent connections for extended periods.
- **ECS**
  - Use when you want container orchestration with AWS-native integration.
  - Good for microservices and batch jobs.
- **EKS**
  - Use when you need Kubernetes compatibility, portability, or existing Kubernetes tooling.
  - More operationally complex than ECS.
- **Fargate**
  - Use when you want containers without managing EC2 instances.
  - Good for reducing infrastructure operations.
- **Elastic Beanstalk**
  - Use when you want a PaaS-style deployment experience for common web apps without managing the underlying stack directly.

**Scenario cues for compute**
- Choose **Lambda** for “run code when an object lands in S3.”
- Choose **ECS/Fargate** for “containerized service with minimal server management.”
- Choose **EC2** for “custom kernel module,” “special agent,” or “persistent daemon.”
- Choose **Elastic Beanstalk** for “simple app deployment with managed platform updates.”
- Choose **EKS** only when Kubernetes is explicitly required or strongly implied.

**Example: event-driven image processing**

A common pattern is S3 upload triggering Lambda, which writes metadata to DynamoDB and stores transformed output back to S3.

```text
User upload -> S3 bucket -> Lambda trigger -> resize/validate -> S3 output
                                   |
                                   v
                              DynamoDB metadata
```

This pattern is attractive because it scales automatically and avoids idle servers. If the processing becomes long-running or memory-heavy, move the worker to ECS or EC2.

**Storage: S3, EBS, EFS, FSx, and backup choices**

Storage questions are usually about access pattern, durability, and sharing.

**S3**
- Object storage with very high durability.
- Best for backups, static assets, logs, data lakes, and media.
- Use lifecycle policies to move data to cheaper classes.
- Use versioning to protect against accidental overwrite or deletion.
- Use encryption at rest with SSE-S3, SSE-KMS, or client-side encryption.
- Use presigned URLs for temporary object access.

**EBS**
- Block storage attached to EC2.
- Best for boot volumes, databases on EC2, and low-latency block access.
- Volume types matter:
  - `gp3` for general purpose with predictable baseline performance.
  - `io1`/`io2` for high IOPS and mission-critical workloads.
  - `st1` for throughput-oriented HDD workloads.
  - `sc1` for cold, infrequently accessed throughput workloads.
- Snapshots back up EBS volumes to S3-managed storage.
- Multi-Attach is limited and should not be treated as the default design.

**EFS**
- Managed NFS file system for Linux.
- Multiple EC2 instances can mount it concurrently.
- Good for shared content, web tiers, home directories, and container shared storage.
- Scales automatically and is regional.

**FSx**
- Use when the exam explicitly calls for Windows file shares, Lustre, or advanced file-system features.
- **FSx for Windows File Server** fits SMB and Windows workloads.
- **FSx for Lustre** fits HPC and high-throughput workloads.
- **FSx for NetApp ONTAP** fits enterprise file features and hybrid patterns.

**AWS Backup**
- Centralized backup orchestration for supported AWS services.
- Useful when you need policy-based backup management across accounts and services.
- Helps standardize retention, vaulting, and compliance.

**Storage comparison**
| Service | Best for | Shared access | Typical exam clue |
|---|---|---:|---|
| S3 | Objects, backups, static content | Yes, via API | “Highly durable, low-cost, internet-scale” |
| EBS | Block storage for EC2 | No, normally one instance | “Boot volume,” “database on EC2” |
| EFS | Shared Linux file system | Yes | “Many EC2 instances need the same files” |
| FSx | Specialized file systems | Yes | “Windows SMB,” “Lustre,” “enterprise file features” |

**Database selection: RDS, Aurora, DynamoDB, and when to avoid each**

Database questions often hinge on schema, consistency, scaling, and operational burden.

**RDS**
- Managed relational database service for engines like MySQL, PostgreSQL, MariaDB, Oracle, and SQL Server.
- Good when you need SQL, joins, transactions, and familiar relational tooling.
- Multi-AZ is the primary high availability pattern.
- Read replicas help with read scaling and can support some disaster recovery designs, but they are not the primary HA mechanism.

**Aurora**
- AWS-optimized relational database compatible with MySQL and PostgreSQL.
- Better fit when you need higher performance, faster failover, and managed scaling features.
- Often the best answer when the question says “compatible with MySQL/PostgreSQL but more scalable and resilient.”

**DynamoDB**
- NoSQL key-value/document database with single-digit millisecond latency at scale.
- Best for serverless apps, gaming, IoT, session data, and high-scale transactional access by key.
- Use on-demand capacity for unpredictable traffic and provisioned capacity for predictable workloads.
- Global Tables support multi-region active-active patterns.

**Decision boundaries**
- Choose **RDS** when you need standard relational features and moderate scale.
- Choose **Aurora** when you need relational compatibility plus better managed performance and availability.
- Choose **DynamoDB** when access is key-based, scale is high, and schema flexibility matters.
- Avoid forcing SQL into DynamoDB or forcing DynamoDB into relational reporting patterns.

**High-yield database distinctions**
- **Multi-AZ** is for availability and failover.
- **Read replicas** are for read scaling and some DR use cases, but they do not replace Multi-AZ.
- **Backups** are for recovery, not for immediate failover.
- **Encryption at rest** protects stored data; **TLS** protects data in transit.

**Networking: VPC, routing, endpoints, and load balancing**

Networking is one of the most tested areas because many answers look similar until you inspect the traffic flow.

**VPC fundamentals**
- A VPC is your isolated network boundary in AWS.
- Subnets are tied to one Availability Zone each.
- Public subnets route to an Internet Gateway.
- Private subnets do not have direct internet routing.
- NAT Gateway allows private subnet instances to initiate outbound internet access without being directly reachable from the internet.

**Security groups and NACLs**
- **Security groups** are stateful and attach to ENIs, instances, and some managed services.
- **Network ACLs** are stateless and apply at the subnet level.
- Security group referencing is useful when one tier should only accept traffic from another tier’s security group.

**VPC endpoints**
- **Gateway endpoints** are used for S3 and DynamoDB.
- **Interface endpoints** use AWS PrivateLink for many other services.
- Use endpoints when you want private access to AWS services without traversing the public internet.

**Peering vs Transit Gateway**
- **VPC peering** connects two VPCs directly.
- It does not support transitive routing.
- **Transit Gateway** centralizes connectivity across many VPCs and on-premises networks.
- Choose Transit Gateway when the network is growing beyond a few simple peerings.

**VPC sharing**
- Lets multiple AWS accounts use subnets in a shared VPC.
- Useful for centralized network control with decentralized application ownership.

**Load balancers**
| Load balancer | Layer | Best for | Exam clue |
|---|---:|---|---|
| ALB | 7 | HTTP/HTTPS, path-based routing, host-based routing, microservices | “Route by URL path” |
| NLB | 4 | Extreme performance, static IPs, TCP/UDP/TLS pass-through | “Millions of requests,” “preserve source IP” |
| CLB | 4/7 | Legacy workloads | Usually not the preferred modern answer |

**Route 53 routing policies**
- **Simple**: one endpoint or basic aliasing.
- **Weighted**: split traffic by percentage.
- **Latency-based**: send users to the lowest-latency region.
- **Failover**: active-passive with health checks.
- **Geolocation**: route based on user location.
- **Multivalue answer**: return multiple healthy records for simple resilience.

**Scenario cues for networking**
- Choose **ALB** for HTTP path routing to multiple services.
- Choose **NLB** for TCP/UDP, static IPs, or very high throughput.
- Choose **Route 53 failover** for active-passive DNS-based resilience.
- Choose **Transit Gateway** for hub-and-spoke enterprise networking.
- Choose **interface endpoints** when the service is not S3/DynamoDB and you need private access.

**Example: private access to S3 from a private subnet**

```text
Private EC2 -> Route table -> S3 Gateway Endpoint -> S3 bucket
```

This avoids NAT Gateway data processing charges for S3 traffic and keeps traffic on the AWS network.

**Security: IAM, encryption, certificates, and perimeter controls**

Security questions usually test whether you know which layer solves which problem.

**IAM**
- Identity and Access Management controls authentication and authorization.
- Use roles for AWS services and temporary credentials.
- Use policies to grant least privilege.
- Use resource-based policies for cross-account access where supported.

**S3 bucket policy example**
Use the correct ARN for each action:
- `s3:ListBucket` applies to the bucket ARN.
- `s3:GetObject` applies to the object ARN.

```json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "ListBucket",
      "Effect": "Allow",
      "Action": "s3:ListBucket",
      "Resource": "arn:aws:s3:::example-bucket"
    },
    {
      "Sid": "GetObjects",
      "Effect": "Allow",
      "Action": "s3:GetObject",
      "Resource": "arn:aws:s3:::example-bucket/*"
    }
  ]
}
```

**Encryption**
- **At rest**: data stored on disk or in a service is encrypted.
- **In transit**: data moving between systems is protected, usually with TLS.
- Use **KMS** for managed encryption keys and auditability.
- Use **ACM** (AWS Certificate Manager) for TLS certificate provisioning on supported AWS services.

**WAF and Shield**
- **WAF** protects web applications from common Layer 7 attacks and bad request patterns.
- **Shield** helps protect against DDoS (Distributed Denial of Service) attacks.
- Use WAF with CloudFront or ALB when the question mentions HTTP filtering or bot-like traffic.

**Cross-account access**
- Use IAM roles with trust policies for temporary access across accounts.
- Use resource policies when the service supports them and the access pattern is service-specific.
- Prefer role assumption over long-lived access keys.

**Monitoring, logging, and operations**

Monitoring questions often ask you to distinguish metrics, logs, events, and alerts.

**CloudWatch**
- Collects metrics and alarms.
- Logs must be explicitly published or ingested; they are not automatically present for every service.
- Use CloudWatch Logs for application and system logs.
- Use CloudWatch Alarms to trigger actions when thresholds are crossed.

**SNS vs EventBridge**
- **SNS** is best for simple pub/sub notifications and fanout.
- **EventBridge** is best for event routing, SaaS integrations, and rule-based event-driven architectures.
- Use SNS when you want “send this alert to email, SMS, and a Lambda function.”
- Use EventBridge when you want “react to AWS service events or application events with filtering.”

**Systems Manager**
- Use for patching, inventory, automation, parameter storage, and secure shell-less access.
- Session Manager is useful when you want to avoid opening inbound SSH or RDP.
- Run Command helps execute commands across fleets.

**Example: operational alerting pattern**

```text
CloudWatch Alarm -> SNS topic -> email / Lambda / incident workflow
```

If the event needs richer filtering or service-to-service orchestration, route it through EventBridge first.

**Disaster recovery: RTO, RPO, and common patterns**

Disaster recovery is frequently tested through recovery objectives rather than service names alone.

**Key terms**
- **RTO** (Recovery Time Objective): how long you can tolerate downtime.
- **RPO** (Recovery Point Objective): how much data loss you can tolerate.

**Common DR strategies**
- **Backup and restore**
  - Lowest cost, highest RTO/RPO.
  - Good when downtime is acceptable and data loss tolerance is higher.
- **Pilot light**
  - Core components are always on; scale up during disaster.
  - Lower RTO/RPO than backup and restore.
- **Warm standby**
  - A scaled-down but functional environment runs continuously.
  - Better RTO/RPO, higher cost.
- **Multi-site active-active**
  - Highest resilience and lowest RTO/RPO.
  - Highest cost and complexity.

**Service mapping**
- Use **AWS Backup** for centralized backup policy management.
- Use **S3 versioning** and cross-region replication when object durability and regional recovery matter.
- Use **RDS Multi-AZ** for high availability.
- Use **Route 53 failover** for DNS-based failover.
- Use **DynamoDB Global Tables** for multi-region active-active data access.

**Cost optimization: what the exam expects**

Cost questions are not just about “cheapest service.” They are about the cheapest service that still meets the requirement.

- Use **managed services** when they reduce operational overhead enough to justify the price.
- Use **S3 lifecycle policies** to move infrequently accessed data to cheaper storage classes.
- Use **Spot Instances** for fault-tolerant batch and interruption-tolerant workloads.
- Use **Savings Plans** or Reserved Instances when usage is steady and predictable.
- Use **Auto Scaling** to match capacity to demand.
- Use **CloudFront** to reduce origin load and improve global performance.
- Use **NAT Gateway** carefully because it can become expensive for high-volume egress; use VPC endpoints for AWS service traffic where possible.

**High-yield service comparisons**

| Question asks for | Best fit | Why |
|---|---|---|
| Shared file system for Linux EC2 | EFS | Concurrent mounts, managed NFS |
| Windows shared file system | FSx for Windows File Server | SMB and Windows integration |
| Object storage for backups | S3 | Durable, cheap, scalable |
| Block storage for EC2 database | EBS | Low-latency attached storage |
| Relational DB with HA | RDS Multi-AZ or Aurora | Managed failover |
| Key-based high-scale app data | DynamoDB | Low-latency NoSQL |
| HTTP routing to multiple services | ALB | Layer 7 routing |
| TCP/UDP with static IPs | NLB | Layer 4 performance |
| Global edge caching | CloudFront | CDN |
| Private AWS service access | VPC endpoints | Avoid public internet |
| Centralized multi-VPC routing | Transit Gateway | Hub-and-spoke |

**Scenario-based exam thinking**

When you read a question, identify the constraint first.

- If the question says **lowest operational overhead**, think managed services, serverless, and PaaS.
- If it says **lowest latency at scale**, think caching, DynamoDB, NLB, or Aurora depending on the layer.
- If it says **private connectivity**, think VPC endpoints, VPN, Direct Connect, or private subnets.
- If it says **multi-region active-active**, think Route 53, DynamoDB Global Tables, and replicated stateless tiers.
- If it says **Windows file sharing**, think FSx for Windows File Server.
- If it says **Linux shared storage**, think EFS.
- If it says **burst traffic with unpredictable demand**, think Lambda, DynamoDB on-demand, or Auto Scaling.
- If it says **long-running custom software**, think EC2 or containers rather than Lambda.

**Mini decision examples**

1. A web app needs path-based routing to `/api` and `/images`.
   - Choose **ALB**.
2. A batch job runs for 20 minutes and processes files from S3.
   - Choose **ECS on Fargate** or **EC2**, not Lambda.
3. Multiple EC2 instances need the same shared directory.
   - Choose **EFS**.
4. A private subnet needs to read from S3 without internet access.
   - Choose an **S3 gateway endpoint**.
5. A company needs a central network hub for many VPCs and on-premises links.
   - Choose **Transit Gateway**.
6. A service needs low-latency key-value lookups at massive scale.
   - Choose **DynamoDB**.

**Common traps to avoid**

> Do not assume a service solves a problem it only partially addresses.

- **Read replicas** are not the same as Multi-AZ failover.
- **Lambda** is not a drop-in replacement for every backend workload.
- **EBS** is not shared file storage.
- **Security groups** are stateful; NACLs are not.
- **CloudWatch** does not magically collect every log without configuration.
- **VPC peering** does not provide transitive routing.
- **NAT Gateway** is for outbound internet access from private subnets, not inbound access.
- **ALB** is not the best choice for raw TCP/UDP.
- **S3 bucket policies** must use the correct bucket and object ARNs for the action.

**Quick exam checklist**

Before you answer, ask:
- What is the access pattern: object, block, file, relational, or NoSQL?
- Is the workload stateless, stateful, or event-driven?
- Do you need single-region HA or multi-region DR?
- Is the traffic HTTP, TCP, or UDP?
- Do you need private access to AWS services?
- Is the priority cost, performance, resilience, or operational simplicity?
- Is the requirement about encryption at rest, in transit, or both?

**FAQ-style reminders**

- **When do you choose Aurora over RDS?**
  - When the question emphasizes better performance, faster failover, or AWS-optimized relational scaling.
- **When do you choose EFS over EBS?**
  - When multiple instances need concurrent shared file access.
- **When do you choose ALB over NLB?**
  - When you need HTTP/HTTPS routing features like host/path-based routing.
- **When do you choose EventBridge over SNS?**
  - When you need event filtering and routing across AWS services or SaaS integrations.
- **When do you choose Transit Gateway over VPC peering?**
  - When you need scalable hub-and-spoke connectivity across many networks.
- **When do you choose Multi-AZ over read replicas?**
  - When availability and automatic failover are the primary goal.
- **When do you choose WAF?**
  - When you need to inspect and filter web requests at Layer 7.

**Final exam mindset**

The best answer is usually the one that satisfies the requirement with the least unnecessary complexity. If two services both seem possible, compare them on:
- **fit**
- **operational burden**
- **failure behavior**
- **cost**
- **security**

If you can explain why the wrong option fails one of those dimensions, you are usually close to the correct answer.

End of Notes