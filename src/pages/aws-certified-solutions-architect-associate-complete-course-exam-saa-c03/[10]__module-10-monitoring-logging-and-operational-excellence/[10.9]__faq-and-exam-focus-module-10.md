---
title: "FAQ and Exam Focus"
description: "Key concepts for SAA-C03 exam prep, with high-yield FAQs, common traps, and practical AWS architecture patterns."
author: Webnotes
order: 1
image: /content/assets/faq-and-exam-focus.png
keywords:
  - AWS Certified Solutions Architect – Associate Complete Course (Exam SAA-C03)
  - exam prep
  - study notes
canonical: https://webnotes.site/content/aws-certified-solutions-architect-associate-complete-course-exam-saa-c03/faq-and-exam-focus/
twitterCard: summary_large_image
twitterSite: "@webnotes"
schemaType: TechArticle
date: 2026-04-26
lastModified: 2026-04-26
---
## **Course:** AWS Certified Solutions Architect – Associate Complete Course (Exam SAA-C03)

**What this topic covers**

This section focuses on the kinds of questions that repeatedly appear on the AWS Certified Solutions Architect – Associate exam. The goal is not memorization alone; you need to recognize the AWS service that best matches a requirement, understand tradeoffs, and avoid distractors that are technically possible but not the best fit.

For SAA-C03, the exam often tests:
- **Availability** and fault tolerance
- **Scalability** and elasticity
- **Security** and least privilege
- **Cost optimization**
- **Operational excellence**
- **Performance efficiency**

A strong answer usually balances these pillars rather than maximizing only one.

**How to approach exam questions**

Read the requirement first, then identify the constraint:
- If the question says **lowest cost**, prefer managed services and avoid overprovisioning.
- If it says **minimal operational overhead**, prefer fully managed AWS services.
- If it says **high availability**, look for Multi-AZ, replication, or decoupling.
- If it says **global users**, consider CloudFront, Route 53, and regional placement.
- If it says **secure access**, think IAM, KMS, security groups, and private connectivity.

A practical method is to eliminate answers that:
- Add unnecessary servers or manual administration
- Use the wrong storage class or database engine
- Break the stated latency, durability, or compliance requirement
- Solve the problem with a custom build when AWS has a managed option

**High-yield service selection patterns**

Many exam questions are really service-matching exercises.

- **EC2** is for full control over compute, custom OS-level configuration, or legacy workloads.
- **Lambda** is for event-driven, short-lived, stateless code with minimal ops.
- **ECS/Fargate** is for containers when you want orchestration without managing servers.
- **EKS** is for Kubernetes-specific requirements.
- **S3** is for object storage, static assets, backups, and data lakes.
- **EBS** is for block storage attached to a single EC2 instance.
- **EFS** is for shared file storage across multiple instances.
- **RDS/Aurora** is for relational databases with managed backups, patching, and HA options.
- **DynamoDB** is for serverless NoSQL with predictable low-latency access at scale.
- **SQS** is for decoupling producers and consumers with durable queues.
- **SNS** is for pub/sub fanout and notifications.
- **EventBridge** is for event routing and SaaS/AWS integration patterns.
- **CloudFront** is for CDN acceleration and edge caching.
- **Route 53** is for DNS, routing policies, and health checks.
- **ALB** is for Layer 7 routing, path-based routing, and HTTP/HTTPS workloads.
- **NLB** is for Layer 4 performance, static IPs, and TCP/UDP workloads.

**Common exam traps**

The exam often includes answers that are close, but not best.

- **S3 vs EBS vs EFS**
  - Use **S3** for objects and static content.
  - Use **EBS** for a single instance’s block storage.
  - Use **EFS** when multiple Linux instances need shared file access.
- **RDS Multi-AZ vs Read Replicas**
  - Use **Multi-AZ** for high availability and automatic failover.
  - Use **Read Replicas** for read scaling and offloading read traffic.
- **Security group vs NACL**
  - Use **security groups** for instance-level stateful filtering.
  - Use **network ACLs** for subnet-level stateless filtering.
- **CloudFront vs Global Accelerator**
  - Use **CloudFront** for caching and content delivery.
  - Use **Global Accelerator** for improving availability and performance of TCP/UDP applications with static anycast IPs.
- **SQS vs SNS**
  - Use **SQS** when consumers should pull messages and process them independently.
  - Use **SNS** when you need push-based fanout to multiple subscribers.
- **KMS vs Secrets Manager vs SSM Parameter Store**
  - Use **KMS** to manage encryption keys.
  - Use **Secrets Manager** for rotating secrets such as database credentials.
  - Use **Parameter Store** for configuration values and simpler secret storage patterns.

**Security and identity focus**

Identity and access management is a frequent exam theme. You should know how AWS evaluates permissions and where to place controls.

- **IAM policies** define permissions for users, groups, and roles.
- **IAM roles** are preferred for AWS services and temporary credentials.
- **Resource-based policies** are common for S3, SNS, SQS, Lambda, and KMS.
- **STS** (Security Token Service) issues temporary credentials for assumed roles and federation.
- **MFA** (Multi-Factor Authentication) is used for stronger sign-in protection.
- **KMS key policies** and IAM permissions both matter for encrypted resources.

A common pattern is cross-account access:
- Create a role in the target account.
- Trust the source account or principal.
- Allow the needed actions in the role policy.
- Assume the role using STS.

Example CLI flow:
```bash
aws sts assume-role \
  --role-arn arn:aws:iam::123456789012:role/CrossAccountReadOnly \
  --role-session-name exam-practice
```

**Networking focus**

Networking questions usually test whether you can place the right control in the right layer.

- **VPC** is your isolated network boundary.
- **Subnets** are tied to one Availability Zone.
- **Public subnets** route to an Internet Gateway.
- **Private subnets** do not route directly to the internet.
- **NAT Gateway** allows outbound internet access from private subnets.
- **VPC endpoints** keep traffic private to AWS services.
- **Transit Gateway** simplifies hub-and-spoke connectivity across many VPCs and on-premises networks.
- **Direct Connect** provides dedicated private connectivity from on-premises to AWS.

A common architecture pattern:
- Put web servers behind an **ALB** in public subnets.
- Put application and database tiers in private subnets.
- Use a **NAT Gateway** for patching and package downloads.
- Use **security groups** to allow only required east-west traffic.

Example security group rule concept:
```bash
aws ec2 authorize-security-group-ingress \
  --group-id sg-0123456789abcdef0 \
  --protocol tcp \
  --port 443 \
  --source-group sg-0fedcba9876543210
```

**Storage and data durability focus**

Storage questions often ask about durability, performance, and access patterns.

- **S3 Standard** is for frequently accessed data.
- **S3 Intelligent-Tiering** is for unknown or changing access patterns.
- **S3 Standard-IA** and **One Zone-IA** are for infrequent access with lower storage cost.
- **S3 Glacier** classes are for archival data.
- **Versioning** protects against accidental deletion and overwrites.
- **Lifecycle policies** automate transitions between storage classes.
- **EBS snapshots** are incremental backups stored in S3-managed infrastructure.
- **EFS** scales elastically and supports shared file access.
- **FSx** is used when you need specialized file systems such as Windows File Server or Lustre.

A useful exam rule:
- If the question says **static website**, think **S3 + CloudFront**.
- If it says **shared Linux file system**, think **EFS**.
- If it says **high-performance block storage**, think **EBS**.

Example S3 lifecycle configuration:
```json
{
  "Rules": [
    {
      "ID": "ArchiveOldObjects",
      "Status": "Enabled",
      "Filter": {
        "Prefix": "logs/"
      },
      "Transitions": [
        {
          "Days": 30,
          "StorageClass": "STANDARD_IA"
        },
        {
          "Days": 90,
          "StorageClass": "GLACIER"
        }
      ]
    }
  ]
}
```

**Database focus**

Database questions usually test whether you choose the right engine and scaling model.

- **RDS** is the default choice for managed relational workloads.
- **Aurora** is preferred when you need higher performance and cloud-native HA features within the relational family.
- **DynamoDB** is best for key-value or document access at scale with predictable latency.
- **ElastiCache** is used to reduce database load and improve read performance with Redis or Memcached.
- **Redshift** is for analytics and data warehousing, not transactional OLTP workloads.

Important distinctions:
- Use **Multi-AZ** for failover.
- Use **Read Replicas** for read scaling.
- Use **DynamoDB Global Tables** for multi-region active-active patterns.
- Use **DAX** (DynamoDB Accelerator) when you need microsecond caching for DynamoDB reads.

Example DynamoDB CLI read:
```bash
aws dynamodb get-item \
  --table-name Orders \
  --key '{"OrderId":{"S":"A1001"}}'
```

**Compute and scaling focus**

The exam often asks how to scale without overengineering.

- **Auto Scaling groups** add or remove EC2 instances based on demand.
- **Launch templates** standardize instance configuration.
- **Spot Instances** reduce cost for fault-tolerant workloads.
- **Reserved Instances** and **Savings Plans** reduce cost for steady usage.
- **Lambda** scales automatically per event source.
- **Fargate** removes the need to manage container hosts.

When choosing compute:
- Use **EC2** if you need persistent servers, custom agents, or specialized networking.
- Use **Lambda** if the workload is event-driven and fits execution limits.
- Use **containers** if you want portability and service decomposition.
- Use **Auto Scaling** when traffic varies and instances are still the right compute model.

Example Auto Scaling launch template snippet:
```bash
aws ec2 create-launch-template \
  --launch-template-name web-tier \
  --launch-template-data '{
    "ImageId":"ami-0123456789abcdef0",
    "InstanceType":"t3.micro",
    "SecurityGroupIds":["sg-0123456789abcdef0"]
  }'
```

**High availability and disaster recovery**

You should recognize the difference between availability, backup, and disaster recovery.

- **High availability (HA)** keeps a service running through component failure.
- **Backup and restore** is the simplest disaster recovery strategy.
- **Pilot light** keeps a minimal core running.
- **Warm standby** keeps a scaled-down but functional environment ready.
- **Multi-site active-active** provides the fastest recovery but is usually the most complex and expensive.

Exam guidance:
- If the question asks for the **fastest recovery**, choose a more active DR strategy.
- If it asks for **lowest cost**, choose backup and restore or pilot light.
- If it asks for **automatic failover**, look for Multi-AZ, Route 53 health checks, or managed service replication.

**Monitoring and operations focus**

Operational questions usually involve visibility, alarms, and logs.

- **CloudWatch Metrics** track service and custom metrics.
- **CloudWatch Logs** centralize application and system logs.
- **CloudWatch Alarms** trigger actions based on thresholds.
- **CloudTrail** records API activity for auditing and investigation.
- **AWS Config** tracks configuration changes and compliance.
- **Systems Manager** helps with patching, inventory, and remote operations.

A practical monitoring pattern:
- Send application logs to **CloudWatch Logs**.
- Create alarms on latency, error rate, and CPU.
- Use **CloudTrail** to investigate who changed infrastructure.
- Use **AWS Config** to detect drift from approved configurations.

Example CloudWatch alarm creation:
```bash
aws cloudwatch put-metric-alarm \
  --alarm-name HighCPU \
  --metric-name CPUUtilization \
  --namespace AWS/EC2 \
  --statistic Average \
  --period 300 \
  --threshold 80 \
  --comparison-operator GreaterThanThreshold \
  --dimensions Name=InstanceId,Value=i-0123456789abcdef0 \
  --evaluation-periods 2
```

**Cost optimization focus**

Cost questions are common and often subtle.

- Prefer **managed services** when they reduce operational overhead.
- Use **S3 lifecycle policies** for aging data.
- Use **Spot Instances** for interruptible workloads.
- Use **Savings Plans** for flexible compute discounts.
- Use **Graviton** instances when compatible for better price-performance.
- Use **right-sizing** instead of overprovisioning.
- Use **CloudFront** to reduce origin load and data transfer for global content.

A good answer often combines:
- Caching
- Tiering
- Autoscaling
- Managed services
- Appropriate storage classes

**Exam FAQ**

**Q: Should you always choose the newest AWS service?**

No. Choose the service that best fits the requirement. The exam rewards fit, not novelty.

**Q: Is the cheapest option always correct?**

Not if it breaks availability, performance, or security requirements. Cost is one factor, not the only factor.

**Q: When do you use a NAT Gateway?**

Use it when private subnet resources need outbound internet access, such as package updates or external API calls, without exposing them directly.

**Q: When do you use a VPC endpoint instead of NAT?**

Use a VPC endpoint when traffic is going to supported AWS services and you want to keep it private and avoid internet traversal.

**Q: What is the default choice for a relational database?**

Usually **RDS**, unless the question specifically needs Aurora features or a different engine.

**Q: How do you decide between SQS and SNS?**

Use **SQS** for buffering and decoupling. Use **SNS** for broadcasting to multiple subscribers.

**Q: What is the best way to share files across multiple EC2 instances?**

Use **EFS** for shared Linux file access. Use **FSx** if the question specifies Windows or a specialized file system.

**Q: What is the best way to store static website content?**

Use **S3**, often with **CloudFront** in front for caching and global delivery.

**Q: What is the best way to protect against accidental S3 deletion?**

Enable **versioning** and consider **MFA Delete** where appropriate, though MFA Delete has operational constraints.

**Q: What is the difference between security groups and NACLs?**

Security groups are stateful and instance-level. NACLs are stateless and subnet-level.

**Practical exam decision framework**

When you see a scenario, work through it in this order:
1. Identify the workload type: web, batch, analytics, transactional, streaming, or file sharing.
2. Identify the constraint: cost, latency, availability, compliance, or scale.
3. Identify the access pattern: read-heavy, write-heavy, bursty, global, or internal.
4. Choose the simplest AWS service that satisfies all constraints.
5. Check whether the answer introduces unnecessary management or cost.

This framework helps you avoid distractors that are technically valid but not optimal.

**Quick reference patterns**

- **Static content + global users** → S3 + CloudFront
- **Private subnet outbound access** → NAT Gateway
- **Private AWS service access** → VPC endpoint
- **Shared file storage** → EFS
- **Single-instance block storage** → EBS
- **Relational HA** → RDS Multi-AZ or Aurora
- **Read scaling** → Read Replicas
- **Queue-based decoupling** → SQS
- **Fanout notifications** → SNS
- **Event routing** → EventBridge
- **DNS and failover routing** → Route 53
- **Layer 7 load balancing** → ALB
- **Layer 4 load balancing** → NLB
- **Temporary credentials** → STS assumed roles
- **Encryption keys** → KMS
- **Secrets rotation** → Secrets Manager

**CLI and architecture practice**

Use CLI examples to reinforce service behavior and permissions.

```bash
# List S3 buckets
aws s3 ls

# Describe an RDS instance
aws rds describe-db-instances

# View CloudTrail events
aws cloudtrail lookup-events --max-results 5

# Check EC2 security groups
aws ec2 describe-security-groups --group-ids sg-0123456789abcdef0
```

A simple three-tier architecture often looks like this:
- **Route 53** resolves the domain.
- **CloudFront** caches static and dynamic edge-optimized content where appropriate.
- **ALB** distributes HTTP traffic to application instances.
- **Application servers** run in private subnets.
- **RDS Multi-AZ** stores transactional data.
- **S3** stores uploads, logs, and static assets.

**Final exam reminders**

- Read the requirement carefully before looking at the answers.
- Prefer the service that solves the problem with the least operational burden.
- Distinguish between availability, durability, and backup.
- Distinguish between caching, replication, and scaling.
- Watch for keywords like **global**, **private**, **low latency**, **shared**, **stateless**, and **managed**.
- If two answers seem possible, choose the one that best matches the stated constraint, not the one with the most features.

End of Notes