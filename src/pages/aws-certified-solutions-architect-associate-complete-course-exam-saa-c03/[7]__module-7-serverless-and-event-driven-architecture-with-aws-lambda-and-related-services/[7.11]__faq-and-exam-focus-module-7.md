---
title: "FAQ and Exam Focus — AWS Certified Solutions Architect – Associate (SAA-C03)"
description: "Key concepts for SAA-C03 exam prep, with practical AWS architecture FAQs, service tradeoffs, and high-yield decision patterns."
author: Webnotes
order: 1
image: /content/assets/faq-and-exam-focus.png
keywords:
  - AWS Certified Solutions Architect – Associate (Exam SAA-C03)
  - exam prep
  - study notes
  - FAQ
canonical: https://webnotes.site/content/aws-certified-solutions-architect-associate-complete-course-exam-saa-c03/faq-and-exam-focus/
twitterCard: summary_large_image
twitterSite: "@webnotes"
schemaType: TechArticle
date: 2026-04-26
lastModified: 2026-04-26
---
## **Course:** AWS Certified Solutions Architect – Associate Complete Course (Exam SAA-C03)

**What this topic covers**

This section focuses on the kinds of questions that commonly appear on the AWS Certified Solutions Architect – Associate exam and the decision patterns behind them. The exam emphasizes choosing the most appropriate AWS service for a scenario, not memorizing isolated facts. You should be able to compare services, identify constraints, and select designs that are secure, highly available, scalable, and cost-aware.

A strong exam strategy is to translate each question into:
- the workload type
- the availability and durability target
- the latency or performance requirement
- the security boundary
- the operational burden you are willing to accept
- the cost tradeoff

**How to approach SAA-C03 questions**

Most questions are scenario-based. You are usually given a business goal, a technical constraint, and one or more distractors that are technically possible but not optimal. The best answer is often the one that meets the requirement with the least operational overhead.

- **Read for constraints first**: keywords like *low latency*, *multi-AZ*, *encryption at rest*, *no public internet*, *minimal management*, and *lowest cost* usually determine the answer.
- **Prefer managed services** when they satisfy the requirement.
- **Choose the simplest architecture** that meets the stated need.
- **Watch for hidden requirements** such as compliance, recovery time objective (RTO), recovery point objective (RPO), or cross-region resilience.
- **Do not over-engineer**: if the question does not require multi-region, do not add it.

**High-yield service selection patterns**

The exam often tests whether you know when to use one service over another.

| Need | Usually best fit | Why |
|---|---|---|
| Static website hosting | Amazon S3 + Amazon CloudFront | Low cost, scalable, global delivery |
| Private connectivity to AWS services | VPC endpoints | Keeps traffic off the public internet |
| Relational database | Amazon RDS or Amazon Aurora | Managed backups, patching, replication options |
| Key-value or document access at scale | Amazon DynamoDB | Serverless, low-latency, highly scalable |
| Shared file storage | Amazon EFS | NFS-compatible, shared across instances |
| Block storage for a single instance | Amazon EBS | Persistent block storage attached to EC2 |
| Decoupling components | Amazon SQS or Amazon SNS | Queueing or pub/sub patterns |
| Event-driven workflows | AWS Lambda with EventBridge | Serverless orchestration and triggers |
| Long-term archival | Amazon S3 Glacier storage classes | Lowest-cost archival options |

**Common exam traps**

Many wrong answers are technically valid but fail one of the exam’s priorities.

- **Using EC2 when a managed service is enough**: if the workload can run on Lambda, S3, RDS, or DynamoDB, that is often preferred.
- **Choosing a single-AZ design** when the question asks for high availability.
- **Using public subnets unnecessarily** when private subnets and load balancers are more appropriate.
- **Confusing security groups and network ACLs (NACLs)**:
  - security groups are stateful and attached to resources
  - NACLs are stateless and apply at the subnet level
- **Mixing up S3 and EBS**:
  - S3 is object storage
  - EBS is block storage
- **Confusing RTO and RPO**:
  - RTO is how long recovery can take
  - RPO is how much data loss is acceptable

**Security questions you should expect**

Security is a major exam theme. Questions often ask how to protect data, isolate workloads, or grant access with least privilege.

- **IAM roles** are preferred for AWS service access because they avoid long-term credentials.
- **IAM policies** should grant only the permissions required.
- **KMS (Key Management Service)** is the standard answer for encryption key management in AWS.
- **Secrets Manager** is typically used for rotating and storing secrets such as database credentials.
- **Systems Manager Parameter Store** is often used for configuration values and can also store secure strings.
- **Security groups** are the first place to look for instance-level access control.
- **VPC endpoints** help keep service traffic private.
- **AWS WAF** protects web applications from common exploits and malicious traffic patterns.
- **AWS Shield** helps with distributed denial-of-service (DDoS) protection.

> If a question asks for the *most secure* or *least exposed* design, prefer private networking, IAM roles, encryption, and managed secrets over custom credential handling.

**Availability and disaster recovery focus**

The exam frequently tests your ability to match a design to an availability target.

- **Multi-AZ** is the standard answer for high availability within a region.
- **Multi-Region** is used when the question explicitly requires regional resilience or global failover.
- **Auto Scaling** helps maintain capacity during demand spikes or instance failure.
- **Elastic Load Balancing (ELB)** distributes traffic across healthy targets.
- **Route 53 health checks and failover routing** are useful for DNS-based failover.
- **Backups and snapshots** are not the same as high availability:
  - backups help recovery
  - HA reduces downtime

A practical mental model:
- use **Multi-AZ** for HA
- use **cross-region replication** for disaster recovery
- use **backups** for point-in-time restore
- use **snapshots** for storage-level recovery

**Storage and data transfer questions**

Storage questions often hinge on access pattern and durability requirements.

- **S3** is best for object storage, backups, logs, media, and static content.
- **EBS** is best for boot volumes and low-latency block storage attached to one instance at a time.
- **EFS** is best when multiple EC2 instances need shared file access.
- **FSx** is used when you need specialized file systems such as Windows File Server or Lustre.
- **S3 lifecycle policies** move objects between storage classes to reduce cost.
- **S3 versioning** helps protect against accidental deletion or overwrite.
- **S3 replication** supports cross-region or same-region copy patterns.

Example S3 lifecycle configuration:

```json
{
  "Rules": [
    {
      "ID": "ArchiveLogs",
      "Status": "Enabled",
      "Filter": {
        "Prefix": "logs/"
      },
      "Transitions": [
        {
          "Days": 30,
          "StorageClass": "STANDARD_IA"
        },
        {
          "Days": 90,
          "StorageClass": "GLACIER"
        }
      ],
      "Expiration": {
        "Days": 365
      }
    }
  ]
}
```

**Networking questions to know cold**

Networking scenarios often ask you to place resources correctly inside a VPC (Virtual Private Cloud).

- **Public subnet**: has a route to an internet gateway and is used for internet-facing resources.
- **Private subnet**: no direct route to the internet; used for application and database tiers.
- **NAT gateway**: allows private subnet instances to initiate outbound internet access without being reachable from the internet.
- **Internet gateway**: enables internet connectivity for public subnets.
- **Route tables** determine where traffic goes.
- **VPC peering** connects two VPCs directly, but it is not transitive.
- **Transit Gateway** is better for hub-and-spoke network architectures with many VPCs.
- **Direct Connect** is used for dedicated private connectivity from on-premises to AWS.

A simple three-tier topology often looks like this:

```text
Internet
   |
[Route 53]
   |
[CloudFront]
   |
[ALB in public subnets]
   |
[App EC2/ECS in private subnets]
   |
[RDS in private subnets]
```

**Compute and scaling questions**

The exam often asks you to choose the right compute model for the workload.

- **EC2** gives the most control and is appropriate when you need custom OS-level configuration.
- **Lambda** is best for event-driven, short-running, stateless tasks.
- **ECS** and **EKS** are container orchestration options.
- **Fargate** removes the need to manage container hosts.
- **Auto Scaling groups** are used for EC2 fleet elasticity.
- **Spot Instances** are cost-effective for fault-tolerant workloads.
- **Reserved Instances** and **Savings Plans** are used for predictable steady-state usage.

Example Auto Scaling launch template snippet:

```bash
aws autoscaling create-auto-scaling-group \
  --auto-scaling-group-name web-asg \
  --launch-template LaunchTemplateName=web-template,Version='$Latest' \
  --min-size 2 \
  --max-size 6 \
  --desired-capacity 2 \
  --vpc-zone-identifier "subnet-aaa,subnet-bbb"
```

**Database questions and common distinctions**

Database questions usually test whether you can match the data model to the service.

- **RDS** is the default choice for managed relational databases.
- **Aurora** is often preferred when you need higher performance and AWS-native scaling features.
- **DynamoDB** is the go-to for serverless NoSQL at scale.
- **ElastiCache** is used to reduce database load and improve read performance.
- **Redshift** is for analytics and data warehousing, not transactional workloads.

Important distinctions:
- use **read replicas** to scale read-heavy relational workloads
- use **Multi-AZ** for failover
- use **DynamoDB Global Tables** for multi-region active-active patterns
- use **ElastiCache** for session storage or hot data caching when latency matters

**Messaging and decoupling**

Decoupling is a frequent exam theme because it improves resilience and scalability.

- **SQS** is a queue for asynchronous processing and buffering.
- **SNS** is a pub/sub service for fan-out notifications.
- **EventBridge** is useful for event routing and SaaS or AWS service integration.
- **Step Functions** orchestrate multi-step workflows with retries and branching.

Use these patterns:
- **SQS** when consumers process messages independently and at their own pace
- **SNS** when one event must notify multiple subscribers
- **EventBridge** when you need event filtering and routing across services
- **Step Functions** when workflow state and retries matter

Example SQS send command:

```bash
aws sqs send-message \
  --queue-url https://sqs.us-east-1.amazonaws.com/123456789012/orders \
  --message-body '{"orderId":"A1001","priority":"high"}'
```

**Monitoring and operational visibility**

The exam expects you to know the basic observability stack.

- **CloudWatch metrics** track resource and application performance.
- **CloudWatch logs** store log data from AWS services and custom applications.
- **CloudWatch alarms** trigger actions when thresholds are crossed.
- **CloudTrail** records API activity for auditing and investigation.
- **AWS Config** tracks configuration changes and compliance drift.

A common pattern is:
- use **CloudWatch** for performance and alerting
- use **CloudTrail** for API auditing
- use **AWS Config** for configuration compliance

**FAQ-style exam clarifications**

**Q: When should you choose S3 over EBS?**  
Choose S3 for object storage, shared access, backups, and static content. Choose EBS for low-latency block storage attached to EC2.

**Q: When is Multi-AZ enough?**  
Use Multi-AZ when the requirement is high availability within a region. If the question asks for regional disaster recovery, you need a cross-region design.

**Q: Is a NAT gateway the same as an internet gateway?**  
No. An internet gateway provides internet access for public subnets. A NAT gateway lets private subnet resources make outbound internet requests without being directly reachable.

**Q: When do you use Lambda instead of EC2?**  
Use Lambda for event-driven, stateless, short-duration tasks. Use EC2 when you need persistent servers, custom runtimes, or OS-level control.

**Q: What is the safest default for credentials?**  
Use IAM roles for AWS services and avoid embedding long-term access keys in code.

**Q: How do you reduce S3 cost?**  
Use lifecycle policies, storage class transitions, versioning only when needed, and the right storage class for access frequency.

**Practical CLI examples to remember**

These commands are not memorization targets by themselves, but they help reinforce service behavior.

```bash
# Inspect an S3 bucket policy
aws s3api get-bucket-policy --bucket my-bucket

# Check EC2 security groups
aws ec2 describe-security-groups --group-ids sg-0123456789abcdef0

# View CloudTrail events
aws cloudtrail lookup-events --lookup-attributes AttributeKey=EventName,AttributeValue=RunInstances

# List RDS DB instances
aws rds describe-db-instances

# Check DynamoDB tables
aws dynamodb list-tables
```

**Exam strategy checklist**

Before selecting an answer, ask yourself:
1. Does the question require high availability, disaster recovery, or both?
2. Is the workload stateful or stateless?
3. Is the data object, block, file, relational, or NoSQL?
4. Does the solution need private networking?
5. Can a managed service replace custom infrastructure?
6. Is the priority cost, performance, security, or operational simplicity?

**What to memorize versus what to reason through**

Memorize the core service roles and the most common AWS patterns. Reason through the scenario details rather than relying on keyword matching alone. The exam rewards understanding tradeoffs:
- managed versus self-managed
- single-AZ versus Multi-AZ
- public versus private access
- synchronous versus asynchronous processing
- backup versus replication versus failover

**Final exam focus**

The highest-value skill for SAA-C03 is service selection under constraints. If you can map a business requirement to the right AWS building blocks, you can eliminate distractors quickly and confidently. Focus on the default architecture patterns, the differences between similar services, and the operational implications of each choice.

End of Notes