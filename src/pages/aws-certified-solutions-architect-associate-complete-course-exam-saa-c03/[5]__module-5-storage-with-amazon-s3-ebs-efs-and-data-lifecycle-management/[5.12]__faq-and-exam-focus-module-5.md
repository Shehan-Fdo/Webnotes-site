---
title: "FAQ and Exam Focus — AWS Certified Solutions Architect – Associate (SAA-C03)"
description: "Key concepts for SAA-C03 exam prep, with high-yield service choices, common traps, and scenario-based decision guidance."
author: Webnotes
order: 1
image: /content/assets/faq-and-exam-focus.png
keywords:
  - SAA-C03
  - exam prep
  - study notes
  - AWS Solutions Architect Associate
canonical: https://webnotes.site/content/aws-certified-solutions-architect-associate-complete-course-exam-saa-c03/faq-and-exam-focus/
twitterCard: summary_large_image
twitterSite: "@webnotes"
schemaType: TechArticle
date: 2026-04-26
lastModified: 2026-04-26
---
## **Course:** AWS Certified Solutions Architect – Associate Complete Course (Exam SAA-C03)

**How to think about SAA-C03 questions**

SAA-C03 questions usually test service selection, not memorization. You are expected to identify the best fit for a workload based on constraints such as latency, durability, availability, cost, operational overhead, and recovery objectives.

A good exam answer often comes from matching the requirement to the service’s native strength:

- **Lowest operational effort** usually points to managed services.
- **Highest durability** often points to S3 or multi-AZ designs.
- **Lowest latency** often points to caching, in-memory services, or local storage.
- **Strict recovery targets** require explicit RTO and RPO thinking.
- **Cross-account or cross-VPC access** usually requires IAM, resource policies, peering, PrivateLink, or Transit Gateway depending on the scenario.

> If two answers both seem possible, choose the one that best satisfies the stated requirement with the least operational complexity.

**IAM fundamentals and identity decisions**

Identity and Access Management (IAM) is central to almost every SAA-C03 scenario. You need to know how identities, permissions, and temporary credentials work together.

- **IAM user**: a long-term identity for a person or application, usually avoided for workloads when roles are possible.
- **IAM group**: a collection of users that shares permissions.
- **IAM role**: an assumable identity with temporary credentials; preferred for AWS services, federated users, and cross-account access.
- **IAM policy**: JSON document that allows or denies actions on resources.
- **Permission boundary**: a maximum permissions guardrail for a user or role; it does not grant permissions by itself.
- **STS**: AWS Security Token Service, which issues temporary credentials through `AssumeRole`, federation, and related APIs.

A common exam pattern is to ask how to let one account access resources in another account. In most cases, you use a role in the target account and allow the source principal to assume it.

```json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "AllowCrossAccountAssumeRole",
      "Effect": "Allow",
      "Principal": {
        "AWS": "arn:aws:iam::111122223333:root"
      },
      "Action": "sts:AssumeRole"
    }
  ]
}
```

Practical IAM decision points:

- Use **roles** for EC2, Lambda, ECS tasks, and cross-account access.
- Use **groups** to manage permissions for people at scale.
- Use **resource-based policies** for services such as S3, SNS, SQS, Lambda, and KMS when the resource itself should grant access.
- Use **permission boundaries** when you need to limit what delegated admins can create or attach.
- Use **AWS Organizations SCPs** to set account-level guardrails across multiple accounts.

For federation, know that users can sign in through an identity provider and then assume a role. This is common in enterprise scenarios where AWS is not the primary identity store.

**Storage services and exam selection**

Storage questions often hinge on access pattern, durability, and performance. S3 is the default object storage answer when you need durability, scale, and low cost. EBS is for block storage attached to a single EC2 instance. EFS is shared file storage for Linux workloads. FSx is for specialized file systems.

**S3 storage class selection**

Choose the S3 storage class based on how often data is accessed and how quickly it must be retrieved.

| Storage class | Best for | Retrieval behavior | Exam cue |
|---|---|---:|---|
| S3 Standard | Frequently accessed data | Immediate | General-purpose hot data |
| S3 Intelligent-Tiering | Unknown or changing access patterns | Immediate | You do not know the access pattern |
| S3 Standard-IA | Infrequently accessed data that still needs rapid access | Immediate | Lower cost than Standard, but retrieval fee applies |
| S3 One Zone-IA | Re-creatable infrequent data in one AZ | Immediate | Lower cost, lower resilience |
| S3 Glacier Instant Retrieval | Archive data that still needs millisecond retrieval | Millisecond retrieval | Rarely accessed archive with fast reads |
| S3 Glacier Flexible Retrieval | Archive data with minutes-to-hours retrieval | Flexible retrieval | Long-term archive, occasional restore |
| S3 Glacier Deep Archive | Lowest-cost long-term archive | Hours retrieval | Compliance or very cold data |

Use caution with Glacier classes in exam wording:

- **Glacier Instant Retrieval** is for rarely accessed archive data that still needs millisecond retrieval.
- **Glacier Flexible Retrieval** is for archive data where retrieval time can be minutes or hours.
- **Glacier Deep Archive** is for the coldest, cheapest long-term retention.

Lifecycle transitions should use the current storage class names. For example:

```json
{
  "Rules": [
    {
      "ID": "ArchiveLogs",
      "Status": "Enabled",
      "Filter": {
        "Prefix": "logs/"
      },
      "Transitions": [
        {
          "Days": 30,
          "StorageClass": "GLACIER_IR"
        },
        {
          "Days": 90,
          "StorageClass": "DEEP_ARCHIVE"
        }
      ]
    }
  ]
}
```

**EBS, snapshots, and AMIs**

Elastic Block Store (EBS) is block storage for EC2. It is attached to one instance at a time in a single Availability Zone.

- Use **EBS** for boot volumes, databases on EC2, and workloads that need low-latency block access.
- Use **snapshots** for point-in-time backups of EBS volumes; snapshots are stored in S3-managed infrastructure and are incremental.
- Use an **AMI** when you want a reusable EC2 image that includes the operating system, configuration, and optionally attached EBS snapshots.

A common distinction:

- **Snapshot** = backup of a volume.
- **AMI** = launch template for an instance.

If a question asks how to quickly recreate a server with the same OS and software, an AMI is usually the better answer than a snapshot alone.

**EFS vs FSx**

Amazon Elastic File System (EFS) is a managed NFS file system for Linux workloads that need shared access across multiple instances and AZs.

Use EFS when:

- Multiple EC2 instances need shared file access.
- You want elastic scaling without managing file servers.
- Linux and NFS fit the workload.

Amazon FSx is a family of managed file systems for specialized needs:

- **FSx for Windows File Server**: Windows-native file shares, SMB, Active Directory integration.
- **FSx for Lustre**: high-performance compute workloads, HPC, and fast parallel file access.
- **FSx for NetApp ONTAP**: enterprise file features, snapshots, replication, and hybrid storage patterns.
- **FSx for OpenZFS**: ZFS-based workloads needing advanced data management features.

Exam shortcut:

- Choose **EFS** for general shared Linux file storage.
- Choose **FSx for Windows File Server** for SMB and Windows integration.
- Choose **FSx for Lustre** for HPC and throughput-heavy analytics.
- Choose **FSx for ONTAP** when the scenario mentions enterprise storage features or ONTAP familiarity.

**DynamoDB decision points**

Amazon DynamoDB is a managed NoSQL database for key-value and document workloads. It is often the right answer when you need single-digit millisecond latency at scale without managing servers.

Key design concepts:

- **Partition key**: determines data distribution and access efficiency.
- **Sort key**: enables range queries within a partition.
- **Primary key design**: the most important factor in DynamoDB performance and scalability.
- **GSI**: Global Secondary Index, used for alternate query patterns.
- **LSI**: Local Secondary Index, used when you need an alternate sort key on the same partition key.
- **On-demand capacity**: best for unpredictable traffic and minimal capacity planning.
- **Provisioned capacity**: best when traffic is predictable and cost optimization matters.
- **DAX**: DynamoDB Accelerator, an in-memory cache for read-heavy workloads.
- **Streams**: capture item-level changes for event-driven processing.
- **Global tables**: multi-Region replication for active-active patterns.

Exam decision guidance:

- Use **on-demand** when traffic is spiky or unknown.
- Use **provisioned** when you can forecast traffic and want lower cost.
- Use **DAX** when the bottleneck is repeated reads and you need microsecond cache access.
- Use **streams** when changes should trigger Lambda or downstream processing.
- Use **global tables** when you need multi-Region low-latency reads and writes.

A poor key design can create hot partitions. If many requests target the same partition key, throughput becomes uneven and performance suffers. Good partition key design spreads traffic across partitions.

```bash
aws dynamodb create-table \
  --table-name Orders \
  --attribute-definitions \
    AttributeName=OrderId,AttributeType=S \
    AttributeName=CustomerId,AttributeType=S \
  --key-schema \
    AttributeName=OrderId,KeyType=HASH \
  --billing-mode PAY_PER_REQUEST
```

**Networking and hybrid connectivity**

Networking questions often test whether you understand routing, segmentation, and connectivity options.

**Security groups vs network ACLs**

- **Security groups** are stateful and attached to ENIs, instances, and some managed services.
- **Network ACLs** are stateless and apply at the subnet level.

Use security groups for most instance-level filtering. Use NACLs when you need an additional subnet-level control or explicit deny behavior.

**VPC flow logs**

VPC Flow Logs capture metadata about IP traffic to and from network interfaces, subnets, or VPCs.

Use them for:

- Troubleshooting connectivity.
- Auditing traffic patterns.
- Detecting unexpected network behavior.

They do not capture packet payloads, only metadata.

**VPN, Direct Connect, and hybrid patterns**

- **Site-to-Site VPN**: encrypted connectivity over the public internet; quick to deploy.
- **AWS Direct Connect**: dedicated private network connection; better for consistent throughput and lower jitter.
- **VPN over Direct Connect**: sometimes used for encryption plus private connectivity.
- **Transit Gateway**: hub for connecting multiple VPCs and on-premises networks at scale.

Choose based on the scenario:

- Use **VPN** for fast setup and lower cost.
- Use **Direct Connect** for stable, high-throughput enterprise connectivity.
- Use **Transit Gateway** when many VPCs and networks need centralized routing.

**Route 53 routing policies**

Route 53 is AWS DNS. The routing policy should match the traffic goal.

| Policy | Use when | Example |
|---|---|---|
| Simple | One endpoint or no special routing | Single website |
| Weighted | Split traffic by percentage | Blue/green testing |
| Latency-based | Send users to the lowest-latency Region | Global app |
| Failover | Active/passive high availability | Primary/secondary site |
| Geolocation | Route by user location | Country-specific content |
| Geoproximity | Route based on proximity with bias | Traffic steering |
| Multivalue answer | Return multiple healthy records | Simple client-side failover |

Route 53 Resolver is important in hybrid DNS scenarios:

- Use **inbound endpoints** to let on-premises systems resolve private hosted zones.
- Use **outbound endpoints** to let VPC workloads resolve on-premises DNS names.
- Use **conditional forwarding** when only certain domains should be forwarded.

**Application integration patterns**

Messaging and eventing questions often ask you to decouple services, buffer spikes, or fan out work.

**SQS, SNS, and EventBridge**

- **SQS**: queue-based decoupling and buffering.
- **SNS**: pub/sub fanout to multiple subscribers.
- **EventBridge**: event bus for application events, SaaS integrations, and rule-based routing.

Use **SQS** when consumers should process messages independently and at their own pace. Use **SNS** when one event should notify many targets immediately. Use **EventBridge** when you need content-based routing, event buses, or integration with AWS services and SaaS providers.

A common pattern is SNS fanout to SQS queues:

- SNS receives the event.
- SNS publishes to multiple SQS queues.
- Each downstream service processes its own queue independently.

This is useful when different consumers need the same event but at different speeds.

**SQS visibility timeout and dead-letter queues**

The **visibility timeout** hides a message after a consumer receives it, giving the consumer time to process it without another consumer picking it up.

- Set the visibility timeout longer than the expected processing time.
- Extend it if processing can vary.
- Use a **dead-letter queue** (DLQ) for messages that fail repeatedly.

```bash
aws sqs create-queue \
  --queue-name orders-dlq

aws sqs create-queue \
  --queue-name orders-queue \
  --attributes RedrivePolicy='{"deadLetterTargetArn":"arn:aws:sqs:us-east-1:123456789012:orders-dlq","maxReceiveCount":"5"}'
```

**EventBridge rules and buses**

EventBridge is useful when you want to route events based on patterns such as source, detail type, or payload fields.

- Use a **custom event bus** for your application events.
- Use **rules** to match events and send them to targets.
- Use **archives and replays** when you need to recover or reprocess events.

Choose EventBridge over SNS when the scenario emphasizes event filtering, schema-aware routing, or application event buses rather than simple broadcast notification.

**Observability and troubleshooting**

Observability questions often involve knowing which service gives logs, metrics, traces, or query capability.

**CloudWatch basics**

Amazon CloudWatch provides metrics, logs, alarms, and dashboards.

- **Metrics**: time-series measurements such as CPU utilization.
- **Logs**: application and system log streams.
- **Alarms**: trigger actions when thresholds are crossed.
- **Dashboards**: visual summaries.

**CloudWatch Logs Insights**

Use Logs Insights when you need to query log data interactively.

```sql
fields @timestamp, @message
| filter @message like /ERROR/
| sort @timestamp desc
| limit 20
```

**Metric filters and alarm actions**

Metric filters turn log patterns into CloudWatch metrics. This is useful when you need an alarm based on a log event, such as repeated authentication failures.

Alarm actions can include:

- Sending notifications through SNS.
- Stopping, terminating, rebooting, or recovering EC2 instances.
- Triggering Auto Scaling actions.

**X-Ray**

AWS X-Ray helps you trace requests through distributed systems.

Use X-Ray when:

- You need to identify latency bottlenecks across microservices.
- You want to see service maps and request traces.
- You are debugging serverless or distributed application flows.

A typical serverless stack pairs API Gateway, Lambda, and X-Ray for request tracing.

**Security and governance**

Security questions often ask which service detects, audits, or enforces controls.

**ACM, Organizations, and Config**

- **ACM**: AWS Certificate Manager, used to provision and manage TLS certificates for services such as ALB, CloudFront, and API Gateway.
- **Organizations**: central multi-account management.
- **SCPs**: Service Control Policies, which set maximum permissions at the account or OU level.
- **AWS Config**: records resource configuration history and evaluates compliance against rules.

Use ACM when the scenario needs managed certificates without manual renewal. Use Config when you need to track drift or compliance over time. Use SCPs when you need guardrails that even administrators in child accounts cannot exceed.

**GuardDuty, Inspector, Macie, and Security Hub**

- **GuardDuty**: threat detection for suspicious activity, such as unusual API calls or compromised credentials.
- **Inspector**: vulnerability assessment for EC2, container images, and some Lambda workloads.
- **Macie**: discovers and classifies sensitive data in S3.
- **Security Hub**: aggregates and prioritizes security findings across services.

Exam cues:

- Use **GuardDuty** for threat detection.
- Use **Inspector** for vulnerability scanning.
- Use **Macie** for sensitive data discovery in S3.
- Use **Security Hub** for centralized security posture visibility.

**CloudTrail**

CloudTrail records AWS API activity and is essential for auditing who did what in AWS. It is especially useful for management events, and data events are needed for some resource-level access visibility.

Use CloudTrail when you need:

- API audit history.
- Change tracking.
- Investigation of control-plane activity.

Be careful with exam wording:

- CloudTrail is not the answer for every “who accessed my data” question.
- For S3 object-level access, you may need **data events**.
- For some access investigations, service logs or application logs may be more appropriate.

**Serverless architecture patterns**

Serverless questions often combine API Gateway, Lambda, and event-driven services.

**API Gateway + Lambda**

Use API Gateway when you need:

- A managed HTTP API or REST API front end.
- Authentication, throttling, and request validation.
- A serverless entry point for Lambda.

A common pattern is:

- Client calls API Gateway.
- API Gateway invokes Lambda.
- Lambda reads or writes DynamoDB or S3.

```yaml
Resources:
  OrdersApi:
    Type: AWS::ApiGatewayV2::Api
    Properties:
      Name: OrdersApi
      ProtocolType: HTTP
```

Use API Gateway instead of an Application Load Balancer when the scenario emphasizes serverless APIs, request transformation, or API management features.

**Disaster recovery and resilience**

Disaster recovery questions are often about matching architecture to **RTO** and **RPO**.

- **RTO**: Recovery Time Objective, how long you can be down.
- **RPO**: Recovery Point Objective, how much data loss you can tolerate.

| Pattern | RTO | RPO | When to use |
|---|---:|---:|---|
| Backup and restore | High | High | Lowest cost, can tolerate long recovery |
| Pilot light | Medium | Low to medium | Core services always on, rest scaled later |
| Warm standby | Low | Low | Smaller active environment ready to scale |
| Multi-site active-active | Very low | Very low | Mission-critical workloads with minimal downtime |

Scenario guidance:

- Choose **backup and restore** when cost matters most and downtime is acceptable.
- Choose **pilot light** when you need a minimal always-on footprint.
- Choose **warm standby** when you need faster recovery with moderate cost.
- Choose **multi-site active-active** when availability requirements are strict and the budget supports it.

A practical example:

- A reporting system that can be restored in hours may use backup and restore.
- An e-commerce checkout system may need warm standby or active-active depending on business impact.
- A regulated trading platform may require multi-site active-active if downtime is unacceptable.

**Common exam traps**

Many wrong answers are technically possible but do not match the requirement as closely as the best answer.

- Do not choose a self-managed solution when a managed service meets the need.
- Do not choose multi-AZ when the question explicitly requires multi-Region.
- Do not choose a cache when the problem is durable storage.
- Do not choose SQS when the requirement is broadcast fanout to many consumers.
- Do not choose SNS when the requirement is durable queue-based buffering.
- Do not choose EFS when the workload needs Windows SMB features.
- Do not choose Direct Connect when the scenario only needs a quick temporary connection.
- Do not choose Glacier Deep Archive when the data must be retrieved quickly.

**Practical CLI examples**

Use the AWS CLI to validate your understanding of service behavior and configuration.

Create a role trust policy for EC2:

```bash
aws iam create-role \
  --role-name EC2AppRole \
  --assume-role-policy-document file://trust-policy.json
```

Attach a managed policy:

```bash
aws iam attach-role-policy \
  --role-name EC2AppRole \
  --policy-arn arn:aws:iam::aws:policy/AmazonS3ReadOnlyAccess
```

Create an S3 bucket policy for public website content only when public access settings allow it:

```json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "AllowPublicReadForWebsiteContent",
      "Effect": "Allow",
      "Principal": "*",
      "Action": "s3:GetObject",
      "Resource": "arn:aws:s3:::example-website-bucket/*"
    }
  ]
}
```

Enable a CloudTrail trail:

```bash
aws cloudtrail create-trail \
  --name org-trail \
  --s3-bucket-name my-cloudtrail-logs
```

Create a Route 53 weighted record set:

```json
{
  "Comment": "Weighted routing example",
  "Changes": [
    {
      "Action": "UPSERT",
      "ResourceRecordSet": {
        "Name": "app.example.com",
        "Type": "A",
        "SetIdentifier": "blue",
        "Weight": 80,
        "AliasTarget": {
          "HostedZoneId": "Z35SXDOTRQ7X7K",
          "DNSName": "dualstack.alb-blue-123.us-east-1.elb.amazonaws.com",
          "EvaluateTargetHealth": true
        }
      }
    }
  ]
}
```

**High-yield exam reminders**

- Read the requirement for **availability**, **durability**, **latency**, and **cost** before choosing a service.
- Prefer **managed services** unless the question explicitly needs control or customization.
- Match the **data model** to the service: object, block, file, relational, or NoSQL.
- Use **temporary credentials** and **roles** for AWS workloads whenever possible.
- Think in terms of **decoupling** for scalable application design.
- Map disaster recovery answers to **RTO** and **RPO**.
- Treat security and observability as design requirements, not afterthoughts.

**Quick self-check before answering a scenario**

1. Identify the workload type: web, batch, analytics, storage, messaging, or hybrid.
2. Identify the constraint: cost, latency, scale, compliance, or recovery.
3. Eliminate services that solve a different problem.
4. Choose the simplest managed service that satisfies the requirement.
5. Verify whether the question asks for single-AZ, multi-AZ, or multi-Region behavior.

End of Notes