---
title: "FAQ and Exam Focus — AWS Certified Solutions Architect – Associate (SAA-C03)"
description: "Key concepts for SAA-C03 exam prep, with high-yield FAQs, service tradeoffs, and practical AWS architecture patterns."
author: Webnotes
order: 1
image: /content/assets/faq-and-exam-focus.png
keywords:
  - AWS Certified Solutions Architect – Associate (Exam SAA-C03)
  - study notes
  - exam prep
canonical: https://webnotes.site/content/aws-certified-solutions-architect-associate-complete-course-exam-saa-c03/faq-and-exam-focus/
twitterCard: summary_large_image
twitterSite: "@webnotes"
schemaType: TechArticle
date: 2026-04-26
lastModified: 2026-04-26
---
## **Course:** AWS Certified Solutions Architect – Associate Complete Course (Exam SAA-C03)

**FAQ and exam focus overview**

The SAA-C03 exam tests whether you can choose the right AWS service for a business requirement, not whether you can memorize every feature. You are usually solving for availability, fault tolerance, security, cost, and operational simplicity at the same time. The best exam answers often reflect the AWS Well-Architected Framework: design for failure, automate where possible, and pick managed services when they satisfy the requirement.

A useful way to study is to group questions by intent:

- **Availability**: multi-AZ, load balancing, replication, failover
- **Scalability**: Auto Scaling, managed databases, serverless, caching
- **Security**: IAM, KMS, Secrets Manager, security groups, private connectivity
- **Cost**: right-sizing, storage classes, Spot Instances, reserved capacity
- **Operations**: monitoring, logging, patching, backups, lifecycle policies

**How the exam usually frames questions**

Most questions describe a scenario and ask for the *best* solution. That means you should compare options by tradeoff, not by feature count.

- If the workload must survive an AZ failure, choose **multi-AZ** architecture.
- If the workload must survive a Region failure, choose **multi-Region** design.
- If the workload is spiky, prefer **Auto Scaling** or serverless over fixed capacity.
- If the workload is read-heavy, consider **Amazon CloudFront**, **ElastiCache**, or read replicas.
- If the workload needs low operational overhead, prefer managed services such as **RDS**, **DynamoDB**, **S3**, **Lambda**, and **ECS/Fargate**.

> If two answers both work, the exam often prefers the one that is simpler, more managed, and more resilient.

**High-yield service selection patterns**

A large part of SAA-C03 is mapping requirements to the right AWS service.

| Requirement | Common best-fit services | Why it is often correct |
|---|---|---|
| Durable object storage | `Amazon S3` | High durability, lifecycle policies, versioning, replication |
| Shared file storage | `Amazon EFS` | Managed NFS, multi-AZ, shared access for Linux workloads |
| Block storage for EC2 | `Amazon EBS` | Low-latency attached storage, snapshots, encryption |
| Relational database | `Amazon RDS` / `Aurora` | Managed backups, Multi-AZ, read replicas |
| Key-value / document / serverless NoSQL | `Amazon DynamoDB` | Fully managed, scalable, low ops |
| Content delivery | `Amazon CloudFront` | Edge caching, lower latency, origin offload |
| Private connectivity | `AWS PrivateLink`, `VPC endpoints` | Keeps traffic off the public internet |
| Centralized secrets | `AWS Secrets Manager` | Rotation and secure secret storage |
| Encryption keys | `AWS KMS` | Managed key lifecycle and integration |
| Event-driven processing | `Amazon SQS`, `SNS`, `EventBridge`, `Lambda` | Decoupling and asynchronous workflows |

**Common FAQ themes**

**1. When do you choose S3 versus EBS versus EFS?**

Use the storage service that matches the access pattern.

- **S3** is object storage for files, backups, logs, static assets, and data lakes.
- **EBS** is block storage attached to one EC2 instance at a time in a single AZ.
- **EFS** is shared file storage for multiple instances and Linux-based workloads that need NFS.

Practical examples:

```bash
aws s3 cp backup.tar.gz s3://my-backup-bucket/daily/
aws ec2 create-volume --availability-zone us-east-1a --size 100 --volume-type gp3
aws efs create-file-system --performance-mode generalPurpose
```

Key exam clue:

- If the question says “shared storage across multiple EC2 instances,” think **EFS**.
- If it says “boot volume” or “database volume,” think **EBS**.
- If it says “store objects, logs, or static content,” think **S3**.

**2. When do you choose RDS versus DynamoDB?**

Choose based on data model and access pattern.

- **RDS** fits relational data, joins, transactions, and SQL-based applications.
- **DynamoDB** fits key-value or document access, predictable low-latency reads and writes, and massive scale with minimal administration.

Exam clues:

- If the app needs complex queries, foreign keys, or ACID-style relational behavior, choose **RDS** or **Aurora**.
- If the app needs single-digit millisecond latency at scale and access by primary key, choose **DynamoDB**.
- If the question emphasizes “no database administration,” **DynamoDB** is often the stronger answer.

**3. When do you use Multi-AZ versus Read Replicas?**

These solve different problems.

- **Multi-AZ** is for high availability and automatic failover.
- **Read replicas** are for read scaling and, in some cases, disaster recovery support.

Important distinction:

- Multi-AZ keeps a synchronous standby for failover.
- Read replicas are typically asynchronous and can lag behind the primary.

> For exam questions about “automatic failover,” choose **Multi-AZ**.
> For “offload read traffic,” choose **read replicas**.

**4. When do you use CloudFront?**

Use **CloudFront** when you need:

- lower latency for global users
- caching of static or cacheable dynamic content
- origin protection and reduced load on S3 or ALB
- HTTPS at the edge

Common pairings:

- **S3 + CloudFront** for static websites and downloads
- **ALB + CloudFront** for web applications
- **Lambda@Edge** or **CloudFront Functions** for lightweight edge logic

Example origin setup:

```text
User -> CloudFront -> S3 bucket
User -> CloudFront -> Application Load Balancer -> EC2/ECS
```

**5. When do you use SQS versus SNS versus EventBridge?**

These are all messaging and event services, but they solve different problems.

- **SQS** is a queue for decoupling producers and consumers.
- **SNS** is pub/sub fanout for pushing notifications to multiple subscribers.
- **EventBridge** is event routing for SaaS, AWS services, and application events with filtering and rules.

Practical guidance:

- Use **SQS** when you need buffering, retries, and worker-based processing.
- Use **SNS** when one event must notify many targets.
- Use **EventBridge** when you need event-driven integration with filtering and routing.

Example architecture:

```text
Application -> SNS -> SQS queues -> worker fleets
Application -> EventBridge -> Lambda / Step Functions / SQS
```

**6. When do you use Auto Scaling?**

Use **EC2 Auto Scaling** when compute demand changes over time and you want to maintain capacity automatically.

- Scale out for traffic spikes.
- Scale in when demand drops.
- Pair with an **Application Load Balancer** for web tiers.

Typical pattern:

```text
Route 53 -> ALB -> Auto Scaling Group -> EC2 instances
```

Exam clue:

- If the question says “handle variable traffic without overprovisioning,” think **Auto Scaling** or serverless.
- If it says “replace unhealthy instances automatically,” think **Auto Scaling**.

**7. When do you use Lambda?**

Use **AWS Lambda** for event-driven, short-running, stateless compute.

Good fits:

- file processing after S3 upload
- API backends with API Gateway
- scheduled tasks
- stream or queue consumers

Not ideal when:

- you need long-running processes
- you need specialized OS-level control
- you need persistent local state

Example trigger flow:

```text
S3 upload -> Lambda -> DynamoDB update -> SNS notification
```

**8. When do you use EKS, ECS, or Fargate?**

For the exam, focus on the operational tradeoff.

- **ECS** is a managed container orchestration service.
- **EKS** is managed Kubernetes.
- **Fargate** removes server management for containers.

General exam guidance:

- Choose **Fargate** when you want containers without managing EC2 hosts.
- Choose **ECS** when you want a simpler AWS-native container platform.
- Choose **EKS** when the requirement explicitly mentions Kubernetes or Kubernetes tooling.

**Security and identity focus**

Security questions often test whether you understand the difference between authentication, authorization, and encryption.

- **IAM users, groups, roles, and policies** control access.
- **Roles** are preferred for AWS services and temporary credentials.
- **Security groups** are stateful instance-level firewalls.
- **Network ACLs** are stateless subnet-level filters.
- **KMS** manages encryption keys.
- **Secrets Manager** stores and rotates secrets.
- **SSM Parameter Store** is often used for configuration and can store secrets, but rotation is not its primary strength.

Practical CLI examples:

```bash
aws iam list-roles
aws kms list-keys
aws secretsmanager get-secret-value --secret-id prod/db-password
aws ssm get-parameter --name /app/config/db-host --with-decryption
```

Exam clues:

- If the question asks for temporary access to AWS resources, choose an **IAM role**.
- If it asks for encrypting data at rest with managed keys, choose **KMS**.
- If it asks for rotating database credentials automatically, choose **Secrets Manager**.

**Networking focus**

Networking questions usually test private connectivity, routing, and segmentation.

Core concepts:

- **VPC** is your isolated network boundary.
- **Subnets** are tied to a single Availability Zone.
- **Route tables** control traffic flow.
- **Internet Gateway** enables internet access for public subnets.
- **NAT Gateway** allows private subnet instances to reach the internet outbound only.
- **VPC endpoints** keep traffic to AWS services private.

Common exam patterns:

- Public-facing web tier in a public subnet, app and database tiers in private subnets.
- NAT Gateway for patching or package downloads from private instances.
- Interface endpoints for private access to services like Secrets Manager or CloudWatch.
- Gateway endpoints for S3 and DynamoDB.

Example private subnet pattern:

```text
Public subnet: ALB, NAT Gateway
Private subnet: EC2 app servers, RDS
Route 0.0.0.0/0 -> Internet Gateway for public subnet
Route 0.0.0.0/0 -> NAT Gateway for private subnet outbound
```

> A NAT Gateway is for outbound internet access from private subnets. It does not make private instances directly reachable from the internet.

**Storage and backup focus**

Storage questions often hinge on durability, lifecycle, and recovery objectives.

Important terms:

- **RPO** (Recovery Point Objective): how much data loss is acceptable
- **RTO** (Recovery Time Objective): how long recovery can take
- **Versioning**: preserves object versions in S3
- **Lifecycle policies**: move data to cheaper storage classes automatically
- **Snapshots**: point-in-time backups for EBS and RDS
- **Cross-Region replication**: copies data to another Region for resilience

Practical examples:

```bash
aws s3api put-bucket-versioning \
  --bucket my-bucket \
  --versioning-configuration Status=Enabled

aws s3api put-bucket-lifecycle-configuration \
  --bucket my-bucket \
  --lifecycle-configuration file://lifecycle.json
```

Example lifecycle policy idea:

- keep recent objects in `STANDARD`
- transition older objects to `STANDARD_IA`
- archive infrequently accessed data to `GLACIER` or `DEEP_ARCHIVE`

**Monitoring and operations focus**

The exam expects you to know how to observe and troubleshoot systems.

Key services:

- **CloudWatch Metrics** for performance data
- **CloudWatch Logs** for application and system logs
- **CloudWatch Alarms** for threshold-based actions
- **CloudTrail** for API auditing
- **AWS Config** for resource configuration history and compliance

Practical examples:

```bash
aws cloudwatch put-metric-alarm \
  --alarm-name HighCPU \
  --metric-name CPUUtilization \
  --namespace AWS/EC2 \
  --statistic Average \
  --period 300 \
  --threshold 80 \
  --comparison-operator GreaterThanThreshold
```

Exam clue:

- If the question asks “who changed this resource,” think **CloudTrail**.
- If it asks “is the resource compliant with a rule,” think **AWS Config**.
- If it asks “is the instance overloaded,” think **CloudWatch**.

**High-probability exam traps**

These are common answer choices that look similar but solve different problems.

- **S3 vs EBS**: object storage versus attached block storage
- **Multi-AZ vs Multi-Region**: availability within a Region versus disaster recovery across Regions
- **SQS vs SNS**: queueing versus fanout
- **Security group vs NACL**: stateful instance firewall versus stateless subnet filter
- **NAT Gateway vs Internet Gateway**: outbound-only private access versus public internet access
- **KMS vs Secrets Manager**: encryption keys versus secret storage and rotation
- **Read replica vs Multi-AZ**: read scaling versus failover
- **CloudFront vs ALB**: edge caching and global delivery versus regional application load balancing

**Decision shortcuts for scenario questions**

Use these shortcuts when you are under time pressure.

1. Identify the primary requirement: availability, cost, security, performance, or operations.
2. Identify the workload type: web app, batch job, analytics, storage, database, or messaging.
3. Eliminate answers that add unnecessary management.
4. Prefer managed services when they meet the requirement.
5. Prefer the simplest architecture that satisfies the stated constraints.

Examples:

- “Static website with global users” -> **S3 + CloudFront**
- “Private EC2 instances need package updates” -> **NAT Gateway**
- “Need to process uploads asynchronously” -> **S3 + Lambda** or **S3 + SQS + workers**
- “Need to survive AZ failure” -> **Multi-AZ**
- “Need to survive Region failure” -> **cross-Region replication** or **multi-Region design**

**Practical architecture examples**

A few common reference architectures appear frequently in exam questions.

**1. Highly available web application**

```text
Route 53
  -> CloudFront
  -> Application Load Balancer
  -> Auto Scaling Group in 2+ AZs
  -> RDS Multi-AZ
```

Why it works:

- CloudFront improves latency and offloads static content.
- ALB distributes traffic across healthy targets.
- Auto Scaling handles demand changes.
- RDS Multi-AZ provides failover.

**2. Event-driven file processing**

```text
S3 upload
  -> Event notification
  -> Lambda
  -> DynamoDB
  -> SNS notification
```

Why it works:

- S3 stores the file durably.
- Lambda reacts without servers.
- DynamoDB stores metadata at scale.
- SNS notifies downstream systems or users.

**3. Private application tier**

```text
Internet
  -> ALB in public subnet
  -> App servers in private subnet
  -> RDS in private subnet
  -> NAT Gateway for outbound updates
```

Why it works:

- Only the load balancer is public.
- App and database tiers stay private.
- NAT Gateway supports outbound-only internet access.

**What to memorize versus what to reason through**

Memorize the service roles and the most common tradeoffs. Reason through the architecture by matching the requirement to the service behavior.

Memorize:

- storage type differences
- HA versus DR terminology
- IAM versus security groups versus NACLs
- queue versus pub/sub versus event routing
- managed database versus NoSQL
- public versus private subnet behavior

Reason through:

- whether the workload is read-heavy or write-heavy
- whether the bottleneck is latency, throughput, or administration
- whether the requirement is resilience, isolation, or cost reduction
- whether the simplest managed service already satisfies the need

**Final exam mindset**

The best SAA-C03 answers usually do three things:

- satisfy the requirement exactly
- minimize operational burden
- preserve future flexibility without overengineering

If you are unsure, ask yourself:

- Does this answer solve the stated problem directly?
- Is there a more managed AWS service that does the same thing?
- Does the design match the required failure domain?
- Is the answer consistent with AWS best practices for security and least privilege?

End of Notes