---
title: "FAQ and Exam Focus for AWS Certified Solutions Architect – Associate (SAA-C03)"
description: "Key concepts for SAA-C03 exam prep, with high-yield AWS architecture patterns, security, resilience, and service selection guidance."
author: Webnotes
order: 1
image: /content/assets/faq-and-exam-focus.png
keywords:
  - AWS Certified Solutions Architect – Associate (Exam SAA-C03)
  - exam prep
  - study notes
  - AWS architecture
canonical: https://webnotes.site/content/aws-certified-solutions-architect-associate-complete-course-exam-saa-c03/faq-and-exam-focus/
twitterCard: summary_large_image
twitterSite: "@webnotes"
schemaType: TechArticle
date: 2026-04-26
lastModified: 2026-04-26
---
## **Course:** AWS Certified Solutions Architect – Associate Complete Course (Exam SAA-C03)

**How to approach SAA-C03 questions**

The exam tests whether you can choose the *best* AWS design for a scenario, not just a technically valid one. Read for constraints first: availability, durability, latency, cost, operational overhead, compliance, and migration effort.

- Identify the primary goal:
  - **High availability** usually points to Multi-AZ, load balancing, and managed services.
  - **Read scaling** often points to replicas, caching, or DynamoDB read design.
  - **Global latency** often points to CloudFront, Route 53, and regional placement.
  - **Decoupling** often points to SQS, SNS, EventBridge, or Step Functions.
- Watch for exam wording:
  - “least operational overhead” usually favors managed services.
  - “automatic failover” usually favors Multi-AZ or Route 53 health-based routing.
  - “lowest cost” may favor serverless or single-AZ where acceptable.
- Prefer the simplest architecture that satisfies all requirements.

**Core service-selection patterns**

Many questions are service-matching exercises. You should know the common “best fit” patterns and the tradeoffs behind them.

- **S3** for object storage, static websites, backups, data lakes, and event notifications.
- **EBS** for block storage attached to a single EC2 instance.
- **EFS** for shared POSIX file storage across multiple instances.
- **RDS** for managed relational databases with familiar engines.
- **Aurora** for higher-performance managed relational workloads with read scaling and HA features.
- **DynamoDB** for key-value and document workloads with predictable access patterns and massive scale.
- **Lambda** for event-driven compute with short-lived execution.
- **ECS/Fargate** for containerized workloads with reduced server management.
- **EKS** when you need Kubernetes compatibility and ecosystem control.
- **CloudFront** for CDN caching and edge delivery.
- **Route 53** for DNS, routing policies, and health checks.
- **SQS/SNS/EventBridge** for decoupling and event distribution.
- **Step Functions** for orchestrating multi-step workflows.

**S3 exam focus**

S3 appears frequently because it connects to security, durability, eventing, and cost questions. Know the difference between identity-based and resource-based access, and understand how encryption and public access controls interact.

- **Bucket policies** are resource-based policies attached to the bucket.
- **IAM policies** are identity-based policies attached to users, groups, or roles.
- **Block Public Access** is a safety control that can override public ACLs and bucket policies.
- **Versioning** protects against accidental overwrite and delete, but it does not replace backups.
- **Object Lock** supports WORM (write once, read many) retention for compliance use cases.
- **Presigned URLs** grant temporary access to a specific object without making the bucket public.
- **SSE-S3** uses Amazon-managed keys for server-side encryption.
- **SSE-KMS** uses AWS KMS (Key Management Service) keys and gives you auditability and key control.
- **SSE-C** uses customer-provided keys, but it is less common in exam scenarios.

A common policy mistake is granting `s3:ListBucket` on an object ARN. Listing is a bucket-level action, so it must target the bucket ARN and can be restricted by prefix using a condition.

```json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "ListReportsPrefixOnly",
      "Effect": "Allow",
      "Action": "s3:ListBucket",
      "Resource": "arn:aws:s3:::example-bucket",
      "Condition": {
        "StringLike": {
          "s3:prefix": "reports/*"
        }
      }
    },
    {
      "Sid": "ReadObjectsInReports",
      "Effect": "Allow",
      "Action": "s3:GetObject",
      "Resource": "arn:aws:s3:::example-bucket/reports/*"
    }
  ]
}
```

For S3 event notifications, remember that the destination service must also trust the bucket. For Lambda, the permission uses the lowercase `aws:SourceArn` condition key.

```bash
aws lambda add-permission \
  --function-name process-s3-event \
  --statement-id allow-s3-invoke \
  --action lambda:InvokeFunction \
  --principal s3.amazonaws.com \
  --source-arn arn:aws:s3:::example-bucket \
  --source-account 123456789012
```

For S3 to SQS, you configure the bucket notification and the queue policy. The queue policy should allow the specific bucket ARN and usually include `aws:SourceAccount` to reduce confused-deputy risk.

```json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "AllowS3SendMessage",
      "Effect": "Allow",
      "Principal": { "Service": "s3.amazonaws.com" },
      "Action": "sqs:SendMessage",
      "Resource": "arn:aws:sqs:us-east-1:123456789012:my-queue",
      "Condition": {
        "ArnEquals": {
          "aws:SourceArn": "arn:aws:s3:::example-bucket"
        },
        "StringEquals": {
          "aws:SourceAccount": "123456789012"
        }
      }
    }
  ]
}
```

**KMS and encryption patterns**

Encryption questions often combine storage, database, and cross-account access. You should know when AWS KMS is the right answer and how key policies differ from IAM policies.

- **KMS key policies** control who can use and administer the key.
- **Grants** provide temporary or scoped permissions for AWS services.
- **Envelope encryption** uses a data key to encrypt data and a KMS key to protect the data key.
- **Cross-account access** often requires both IAM permission and KMS key policy permission.
- **S3 SSE-KMS**, **EBS encryption**, and **RDS encryption** all rely on KMS-backed encryption.

Practical KMS CLI example:

```bash
aws kms create-key --description "Key for S3 reports"
aws kms create-alias --alias-name alias/reports-key --target-key-id <key-id>
aws kms encrypt \
  --key-id alias/reports-key \
  --plaintext fileb://report.txt \
  --output text \
  --query CiphertextBlob
```

A useful exam rule: if a service says “encrypted at rest with customer-managed keys,” think KMS. If the question asks for auditability, rotation control, or cross-account governance, KMS is usually the right fit.

**IAM, Organizations, and governance**

Security questions often involve more than one account. AWS Organizations centralizes multi-account management, while Service Control Policies (SCPs) set guardrails.

- **AWS Organizations** lets you group accounts into organizational units (OUs).
- **SCPs** define the maximum permissions an account or OU can use.
- **Consolidated billing** aggregates charges across accounts.
- **Delegated administrator** lets a member account manage a service for the organization.
- **IAM roles** are preferred for cross-account access and AWS service access.

Example SCP that denies disabling CloudTrail outside a security account:

```json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "DenyCloudTrailStop",
      "Effect": "Deny",
      "Action": [
        "cloudtrail:StopLogging",
        "cloudtrail:DeleteTrail"
      ],
      "Resource": "*"
    }
  ]
}
```

Use Organizations when the question mentions multiple business units, centralized security, billing separation, or policy guardrails. Use IAM roles when the question is about one workload assuming permissions in another account.

**RDS and Aurora exam focus**

RDS questions usually test availability, backups, read scaling, and operational behavior. Aurora questions often test cluster architecture, replicas, and serverless options.

- **Multi-AZ** is for high availability and automatic failover.
- **Read replicas** are for read scaling and can be promoted for disaster recovery, but promotion is operational and not the same as automatic HA.
- **Snapshots** are manual backups; **automated backups** support point-in-time recovery.
- **Maintenance windows** control when patching and minor updates occur.
- **Parameter groups** and **option groups** customize database behavior.
- **Aurora replicas** are part of the cluster architecture and support fast failover.
- **Aurora Serverless** is useful when demand is variable and you want less capacity management.

> Do not confuse Multi-AZ with read replicas. Multi-AZ is the primary answer for automatic failover. Read replicas are for scaling reads and can help with DR, but they are not a substitute for HA.

RDS example for a managed MySQL deployment with Multi-AZ:

```yaml
Resources:
  MyDB:
    Type: AWS::RDS::DBInstance
    Properties:
      DBInstanceClass: db.t3.medium
      Engine: mysql
      AllocatedStorage: 20
      MasterUsername: admin
      MasterUserPassword: "{{resolve:secretsmanager:mydb-secret:SecretString:password}}"
      MultiAZ: true
      StorageEncrypted: true
      KmsKeyId: alias/rds-key
```

Aurora uses a cluster model rather than the same `DBInstance`-centric HA pattern. If the question mentions Aurora, think cluster endpoints, reader endpoints, and replicas in the cluster.

**DynamoDB exam focus**

DynamoDB questions usually revolve around access patterns, scaling, and partition design. The exam often asks you to choose the database that avoids operational overhead while supporting predictable low-latency access.

- **Partition key** design drives performance and scalability.
- **Sort key** enables range queries within a partition.
- **GSI** (Global Secondary Index) supports alternate query patterns.
- **LSI** (Local Secondary Index) shares the partition key and changes the sort key.
- **On-demand capacity** is best for unpredictable traffic.
- **Provisioned capacity** is best when traffic is predictable and cost control matters.
- **Hot partitions** happen when too much traffic lands on one partition key.
- **DAX** (DynamoDB Accelerator) can reduce read latency for cacheable workloads.

Example table design for an order system:

```yaml
Resources:
  OrdersTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: Orders
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: CustomerId
          AttributeType: S
        - AttributeName: OrderId
          AttributeType: S
        - AttributeName: OrderStatus
          AttributeType: S
      KeySchema:
        - AttributeName: CustomerId
          KeyType: HASH
        - AttributeName: OrderId
          KeyType: RANGE
      GlobalSecondaryIndexes:
        - IndexName: StatusIndex
          KeySchema:
            - AttributeName: OrderStatus
              KeyType: HASH
          Projection:
            ProjectionType: ALL
```

If a scenario asks for “query by customer, date, and status,” design the table around those access patterns rather than trying to force relational joins. DynamoDB performs best when you know your queries in advance.

**Lambda, API Gateway, and Step Functions**

Serverless questions often test event sources, timeouts, retries, and orchestration. You should know where Lambda fits and where it does not.

- **Lambda** is ideal for short-running, event-driven tasks.
- **API Gateway** exposes HTTP APIs and can front Lambda or other backends.
- **Step Functions** orchestrates multi-step workflows with retries, branching, and error handling.
- **Timeouts** matter because Lambda has a maximum execution duration, so long-running jobs need another service.
- **Retries** can create duplicate processing, so idempotency matters.

A common pattern is API Gateway -> Lambda -> SQS for asynchronous processing. This keeps the API responsive while offloading work.

```yaml
Resources:
  ApiFunction:
    Type: AWS::Lambda::Function
    Properties:
      Runtime: python3.12
      Handler: app.handler
      Role: arn:aws:iam::123456789012:role/lambda-exec
      Timeout: 10
      Code:
        ZipFile: |
          def handler(event, context):
              return {"statusCode": 200, "body": "ok"}
```

Use Step Functions when the workflow has multiple steps, human approval, retries, or branching logic. Use Lambda alone when the task is simple and event-driven.

**Load balancing and traffic management**

Load balancer questions often ask you to choose between ALB, NLB, and Gateway Load Balancer. Know the traffic type and feature set.

- **ALB** (Application Load Balancer) works at Layer 7 and supports path-based and host-based routing.
- **ALB target types** include instances, IP addresses, Lambda, and containers.
- **Sticky sessions** keep a client bound to the same target when session affinity is required.
- **NLB** (Network Load Balancer) works at Layer 4 and is suited for very high throughput and static IP needs.
- **Gateway Load Balancer** is used for transparent insertion of third-party network appliances.

ALB example with target group and stickiness:

```yaml
Resources:
  AppTargetGroup:
    Type: AWS::ElasticLoadBalancingV2::TargetGroup
    Properties:
      Port: 80
      Protocol: HTTP
      VpcId: vpc-123456
      TargetType: instance
      HealthCheckPath: /health
      TargetGroupAttributes:
        - Key: stickiness.enabled
          Value: "true"
        - Key: stickiness.type
          Value: lb_cookie
```

Choose ALB when you need content-based routing, HTTP headers, or Lambda targets. Choose NLB when you need TCP/UDP performance, static IPs, or very low latency at Layer 4.

**Route 53 exam focus**

Route 53 questions often combine DNS routing with health checks and failover behavior. You should know alias records and when to use each routing policy.

- **Alias records** point to AWS resources and can be used at the zone apex.
- **Health checks** monitor endpoints and can drive failover routing.
- **Weighted routing** splits traffic by percentage.
- **Latency-based routing** sends users to the lowest-latency region.
- **Failover routing** sends traffic to a secondary endpoint when the primary is unhealthy.
- **Geolocation routing** routes based on user location.
- **Geoproximity routing** is used with traffic flow and biasing.

Example failover record set concept:

```text
Primary: app.example.com -> ALB in us-east-1
Secondary: app.example.com -> ALB in us-west-2
Health check monitors primary endpoint
If primary fails, Route 53 returns secondary
```

Use alias records for AWS resources like ALB, CloudFront, and S3 website endpoints. Use health checks when the question explicitly asks for automatic DNS failover.

**Disaster recovery terminology**

DR questions are common and often hinge on matching the right recovery objective to the right pattern. Know the difference between RTO and RPO.

- **RTO** (Recovery Time Objective) is how long recovery can take.
- **RPO** (Recovery Point Objective) is how much data loss is acceptable.
- **Backup and restore** has the lowest cost but the highest RTO.
- **Pilot light** keeps core components running and scales up during disaster.
- **Warm standby** keeps a smaller but functional environment ready.
- **Multi-site active-active** serves traffic from multiple sites at once.

A simple comparison:

| Pattern | Cost | RTO | RPO | Typical use |
|---|---:|---:|---:|---|
| Backup and restore | Low | High | High | Non-critical workloads |
| Pilot light | Low-medium | Medium | Low-medium | Faster recovery than backups |
| Warm standby | Medium | Low-medium | Low | Business-critical apps |
| Multi-site active-active | High | Very low | Very low | Global, highly available systems |

Use backup and restore when cost matters most. Use warm standby or active-active when the exam emphasizes fast recovery and minimal downtime.

**Containers: ECS, Fargate, and EKS**

Container questions usually ask you to choose between operational simplicity and platform control.

- **ECS** is AWS-native container orchestration.
- **Fargate** removes the need to manage EC2 worker nodes.
- **EKS** is managed Kubernetes for teams that need Kubernetes APIs and ecosystem compatibility.
- **EC2 launch type** gives you more control but more operational overhead.
- **Fargate** is a strong choice when the question says “no server management.”

Example ECS task definition snippet:

```json
{
  "family": "webapp",
  "networkMode": "awsvpc",
  "requiresCompatibilities": ["FARGATE"],
  "cpu": "256",
  "memory": "512",
  "containerDefinitions": [
    {
      "name": "app",
      "image": "123456789012.dkr.ecr.us-east-1.amazonaws.com/webapp:latest",
      "portMappings": [
        { "containerPort": 80, "protocol": "tcp" }
      ]
    }
  ]
}
```

Choose ECS/Fargate when you want simpler operations. Choose EKS when the requirement explicitly mentions Kubernetes or portability across Kubernetes tooling.

**Common exam traps**

The exam often uses wording that makes two answers look similar. The right choice usually depends on a small but important detail.

- **High availability** is not the same as **disaster recovery**.
- **Read scaling** is not the same as **automatic failover**.
- **Encryption at rest** is not the same as **encryption in transit**.
- **Public access blocked** is not the same as **private access granted**.
- **Caching** improves latency, but it does not replace a database design problem.
- **Asynchronous** processing improves responsiveness, but it adds eventual consistency.
- **Managed** services reduce operational burden, but they may limit customization.

When two answers both work, choose the one that:
- meets the requirement most directly,
- uses the fewest moving parts,
- minimizes operational overhead,
- and aligns with AWS best practices.

**Quick CLI and configuration reminders**

Use these patterns to reinforce exam memory and practical understanding.

```bash
# Inspect an S3 bucket policy
aws s3api get-bucket-policy --bucket example-bucket

# Enable versioning
aws s3api put-bucket-versioning \
  --bucket example-bucket \
  --versioning-configuration Status=Enabled

# Create a Route 53 health check
aws route53 create-health-check \
  --caller-reference "$(date +%s)" \
  --health-check-config Type=HTTPS,FullyQualifiedDomainName=app.example.com,Port=443,ResourcePath=/health
```

```bash
# Create a DynamoDB table with on-demand billing
aws dynamodb create-table \
  --table-name Orders \
  --attribute-definitions AttributeName=CustomerId,AttributeType=S AttributeName=OrderId,AttributeType=S \
  --key-schema AttributeName=CustomerId,KeyType=HASH AttributeName=OrderId,KeyType=RANGE \
  --billing-mode PAY_PER_REQUEST
```

**Final exam strategy**

Treat each question as a design decision under constraints. Start with the requirement, eliminate answers that violate it, and then compare the remaining options by cost, resilience, and complexity.

- If the question says “automatic failover,” think Multi-AZ, Route 53 failover, or managed HA.
- If it says “query by access pattern,” think DynamoDB design.
- If it says “temporary access to one object,” think presigned URL.
- If it says “shared file system,” think EFS.
- If it says “event-driven processing,” think Lambda, SQS, SNS, or EventBridge.
- If it says “global content delivery,” think CloudFront.
- If it says “policy guardrails across accounts,” think Organizations and SCPs.

The best exam answers are usually the ones that are secure, scalable, highly available, and operationally simple at the same time.

End of Notes